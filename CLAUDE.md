# jetron-constructor — конструктор футбольной формы (jetronsport.ru)

История и полный прежний текст — `docs/HISTORY-jetron-constructor-2026-09-23.md` (побайтовая копия от 23.09).
Сжато 23.09 (53 262 → ≤22 000 Б), потолок 25 600 Б, дописывать только ВЗАМЕН. Раньше — `docs/HISTORY-jetron-constructor-2026-09-15.md`
(78 677 Б), прочие `docs/HISTORY-*` и `docs/ПЕРЕДАЧА-*`: волны правок, замеры, опровергнутое. Искать там.

Vanilla JS + Fabric.js v6, 8 линеек, 45 расцветок, зоны (номер / фамилия / лого), редактор `?zones=edit`;
заказ через profi.ru, заказчик он же WP-админ. ⚠️ **НЕ ПУТАТЬ с `tshirt-constructor`** (ФУТБОЛКИ, тот же
заказчик): принты по см, ivory, плотности, «перёд/зад» — там; у формы один вид.
📌 **Первым:** `docs/ДЕПЛОЙ-боевой.md` (выкладка + «Живые предупреждения», до правки боевого);
`docs/ГРАБЛИ-инструментов.md` (эталон долей 1200, композитный кадр, сетки ACF по `pa_model`, бирочная шкала,
headless-обход мёртвых Preview MCP / Claude in Chrome).

## 💰 Деньги и состав

- Основной этап + админ-панель приняты 02.08.2026, оплачены.
- **Второй заказ** (нормализация мокапов + две линейки): **25 000 ₽**, предоплата получена, 🔴 **сумма не
  записана** — остаток вписывает Андрей, не выдумывать. 🔴 Ниже 25 000 не опускаться (расчёт 30 000, снижено
  при ДОБАВЛЕННЫХ Волне и Фаворите). 🔴 **Доплат по форме НЕТ:** 12-15.09, «Внешний вид», сортировка моделей —
  в 25 000, цену за них не называли.
- **Доработка 19.09 «Шрифт/Цвет»: 6 000 ₽ → итого по форме 31 000 ₽. 🔴 Счёт НЕ выставлен.**
- 🔴 КП по админке ФУТБОЛОК не отправлено: 18 000 ₽ отдельно / 30 000 ₽ за обе.
- Состав 22.08: Чемпион 6, Легенда 6, New 5, Space 6, Star 5, Winner 7, Волна 5, Фаворит 5 (коралла нет,
  тёмно-бирюзовая = зелёная). Правда — только `scripts/mockup-map.json`; исходники `C:/Users/Андрей/Downloads/`
  (копии ` (1)` отбрасывать). ⚠️ «Разрез 45 → 90 файлов» не возвращать (опровергнут 23.08). ⚫ Закрытое до 31.08 — в HISTORY.

## 🔴 Долги

**Андрей, письма по очереди:** `docs/сообщение-клиенту-19.md` **с 5 снимками** `docs/screens-2026-09-04/` →
`-20` → `-21` → `-22` (🔴 пропуск: сумма к переводу, вписывает Андрей) → **`-23` с вложением
`docs/Инструкция_редактор_зон.docx`** (вопрос про латиницу при русском тексте в логотипе; абзац про зоны
перемерить). В письме про меню признать: пункты вернули МЫ 12.09. ✅ №13, 16, 17, 18, 24 (15.09) отправлены.
⚫ №14 и №15 НЕ слать.
🔴 **Не сообщено клиенту:** карточка «Футболки» 14.09, скорость 15.09, **выкладки 20.09** (снимки
`docs/screens-2026-09-20/`), **четыре замечания 22.09**. `-25` устарело в описании кнопок — переписать; письмо
про шрифт устарело в части «заказ ушёл в работу». 🔴 **Выставить счёт.**
⚠️ Любое письмо: про свотчи −20 % не писать, оговорки №13 про витрину не повторять, **всё названное числом
или «работает» перемерить перед отправкой** (04.09 протухло за сутки).
**За клиентом:** узкие рамки номера на шортах — растянуть на белом доноре + «Разнести по линейкам»; новые формы
настраивает сам. По №24: телефон и браузер покупателя, запись экрана, «корзина или оформление», узкий столбец
корзины на телефоне (314 px из 390).
**Хвосты:** 6 — КП админки футболок (Андрей); 7 — «Таблица размеров» на телефоне: область 125×16 px при
`padding: 0`, пара строк `stand.css`, не согласовано; 8 — woff2 клубных шрифтов 1,63 → 0,47 МБ, ждёт решения;
9 — уменьшенная копия логотипа в черновик, предложено. 🔴 **Заказы 15-19.09 могли уйти в печать чужим
шрифтом — сверить до печати.** 🟡 `no-cache` для HTML `/constructor/` — обсудить с владельцем.

## 🌿 Git и выкладка

- `github.com/Andrrreyiv/jetron-constructor`. **Замер 23.09 (без fetch):** `origin/main` = `ad19905` (PR #34,
  20.09). 🔴 **`feat/knopki-shrift-cvet-2026-09-20` = `feb2ff2`, запушена, впереди на 7 коммитов (22.09),
  в `main` НЕ влита, PR не открыт** — демо отстаёт от боевого. Локальный `main` = `24b2ed1` (отстал).
- 🔴 В `main` только через PR, кнопку жмёт человек. ⚠️ `git push origin <ветка>:main` проходит мимо
  `dangerous-actions-blocker.mjs` — дырка, не разрешение. ⚠️ Хэш — только `git rev-parse origin/main`,
  «влито» — `git merge-base --is-ancestor`, не логом. ⚠️ Токены `gh`/MCP мертвы (401): `git push` через
  Credential Manager, API — `git credential fill` + curl.
- ⚠️ Намеренно не закоммичен `src/config/mock-config.json.bak2-20260913`. 🔴 Правка `hero.php` 14.09 не в репозитории.
- **Демо:** Pages из `main` → `andrrreyiv.github.io/jetron-constructor/`. ⚠️ **Мерж обновляет ТОЛЬКО демо.**
  Каталога демо не видит (цены из конфига, кнопки «в карточку» нет). sha256 демо — после снятия CR (в репо LF,
  в копии CRLF). Прочих открытых PR нет (#10, #31 закрыты).
- 🔴 **Боевой** `jetronsport.ru/constructor/`, рамка на странице 3443. Канал — **WP File Manager (elFinder),
  НЕ git**, порядок — в ДЕПЛОЕ; `deploy/` едет только руками. **Битый PHP кладёт весь сайт**; товар 3446 не
  удалять; меню ДВА (ID 3 и 55).
- **Проверка:** sha256 каждого файла против локального и raw ветки. PHP: линт `token_get_all` ДО записи, бэкап,
  сверка `cmd=get` (по HTTP PHP не отдаётся), три страницы на 200. 🔴 Новый файл — `cmd=mkfile` ДО `put`.
  ⚠️ `const` в `Runtime.evaluate` роняет второй вызов. ⚠️ Каталог из терминала не снять (капча `1R AntiBot`).
- **Токены 24.09: везде `?v=20260924a`** (index.html, stand.css и все модули — бампаются одной заменой
  `20260922a`→новый по `index.html` + `src/js` + `src/css`). ⚠️ Бампать ВСЕ модули (`app.browser.js`
  со своим `?v=` в `main.browser.js`, `max-age=604800`), иначе вхолостую. ⚠️ `mock-config.json`
  (`no-store`) не бампать. 📌 **Рамка 3443 = страница «Онлайн-конструктор формы»**
  (`/onlajn-konstruktor-formy/`), несёт свой токен в `<iframe src>`; как её править — в ДЕПЛОЕ.
- 🔴 HTML `/constructor/` без `cache-control`, только ETag → «старый HTML + свежий JS» (06.09 без свотчей),
  лечит `core/ModelHost.js`. ⚠️ **Мерить боевой только со сброшенным кэшем.**

## 🔴 Состояние (на 24.09)

**Разработки нет, всё выложено и проверено на живом.** Тесты **240/240** + python **43/43**.

🟢 **24.09 два голосовых клиента закрыты и ВЫЛОЖЕНЫ** (8 файлов + токен рамки 3443, снимок
`docs/screens-2026-09-24/`). Боевой — **PHP 8.3.33; mbstring, getimagesizefromstring,
IMAGETYPE_WEBP есть** (замер серверным линтом).

- **Шрифты по алфавиту.** Порядок задаётся ОДИН раз — `упорядочитьШрифты()` в `AdminOverrides.js`
  (через него идёт список и админки, и покупателя); `jetron_admin_sort_fonts()` нужен лишь чтобы
  сам `admin.json` совпал с экраном (нормализуется при добавлении/удалении шрифта). Кириллические
  первыми и в прежнем порядке (fonts[0] — запасной шрифт по умолчанию), клубные по алфавиту с числами.
  ☠️ **`strnatcasecmp` ПРОПУСКАЕТ пробелы — им сравнивать нельзя:** на живых 43 шрифтах админка дала
  «Manchester City» впереди «Man City», то есть у владельца и покупателя были РАЗНЫЕ списки. Свой
  `jetron_admin_font_cmp()` повторяет `localeCompare(…, {numeric:true, sensitivity:'base'})`.
  ⚠️ **На 25 шрифтах дефект не виден, сверять только на живом списке.** Стерегут
  `tests/test_font_order_php.py` и тест про пробел в `AdminOverrides.test.js`.
- **Защита полей ввода** (его вопрос «вы их как-то защищали?»). Уже было: цену считает сервер, разбор
  по белым спискам (`jetron_orders_parse`), `sanitize_*` на входе и `esc_html`/`esc_url` на выводе,
  `escapeHtml` в браузере, логотипы только `data:image/*` ≤8 МБ и ≤10 шт, имя из серверного UUID,
  админка за `manage_options` + нонс. Замер: `<img src=x onerror=…>` в «Фамилию» и `"><script>`
  в «Номер» → 0 внедрённых тегов, не выполнился. Добавлено: потолок спецификации 4000 знаков,
  `jetron_looks_like_image()` у обоих приёмников файлов, `maxlength` 24/3/40.
  ⚠️ **Номер режется тремя знаками — ИЗМЕНЕНИЕ поведения** (раньше «12345» печаталось на спине;
  три знака обрезал только образец превью шрифта), согласование за клиентом. 🔴 Не наше: приёмник
  логотипов в ТЕМЕ и запрет исполнения PHP из каталога загрузок (хостинг).
- 🟡 Не наше, сказать клиенту: Akeeba ругается «Backup restoration leftovers detected» — остатки
  восстановления в корне сайта, плагин зовёт это риском.
- 🔴 **Клиенту не отвечено ни по 22.09, ни по 24.09.** Ответ по 24.09 написан в переписке; цену
  Андрей не называл. Про защиту кода от копирования: закрыть полностью нельзя, ценность в каталоге
  и настройках на сервере, обфускация — отдельная задача.

- **19.09 шрифт по умолчанию** (макет уходил системным sans): ленивая загрузка 15.09 (`c0c83e2`) брала `fontId`
  только после клика. Правка: `_шрифтыЧерновика()`, `_шрифтыМакета()`, ожидание в `_composeMockupCanvas()`;
  `5cf5d96`, `fee8771`. ☠️ `document.fonts.check('95px rpl')` = `true` и без шрифта — смотреть состав `document.fonts`.
- **20.09 сегмент «Шрифт | Цвет»** (оплачиваемые 6 000; `.seg`/`.seg-btn.active`, как «Детская | Взрослая»).
  Три выкладки: две кнопки `d3f90be` → подпись → сегмент, подпись ушла в подсказку поля (правило общее
  с футболками), подсказку «Напишите название клуба или спонсора» сняли сознательно. ⛔ Список выезжает ВНИЗ
  внутри карточки, окна поверх макета нет; открыта одна панель (`_fcOpen`).
  ☠️ **Крючок ленивой загрузки шрифтов — на `.opt-fc-btn`**: потерять = вернуть дефект 15.09. ⚠️ `display:flex`
  бьёт `hidden` — нужен `.font-list[hidden], .swatches[hidden]{display:none}`. ⛔ **Белый фон полю текста НЕ
  давать.** ☠️ Специфичность: `.panel input[type="text"]` (0,2,1) бьёт `.opt-in-text` (0,1,0). Стережёт
  `tests/BootFonts.test.js`.
- 🟡 Цвета шрифта клиент правит сам: Настройки → «Jetron цвета» (`options-general.php?page=jetron-colors`),
  строка `id|Название|#hex`, 7 цветов.
- **22.09 четыре замечания** (mu-plugins `jetron-orders.php`, `jetron-constructor.php` + 5 файлов фронта; бэкапы
  `scratchpad/php-backup/`, снимки `docs/screens-2026-09-22/`):
  1. ☠️ Сервер списывал МЕНЬШЕ показанного (1090/2290 вместо 1480/2680). **Урок: цену считает сервер** —
     замены написания (Legend = «Легенда»; синий→Голубой, салатовый→Зелёный, лаймовый→Жёлтый) и цена линейки
     перенесены в `jetron_orders_line_aliases`, `jetron_orders_color_aliases`, `jetron_orders_line_price`;
     порядок: карточка → линейка → прайс. ☠️ **Таблицы замен ОБЯЗАНЫ совпадать с `CatalogPrices.js`.**
  2. Меты «Расчёт конструктора / сервера», «Проверено сервером» скрыты; ⚠️ оба числа идут
     в `jetron_orders_log`. «Логотипов: N» — мета темы.
  3. Макет в заказе картинкой (`woocommerce_order_item_display_meta_value`, `woocommerce_admin_order_item_thumbnail`).
  4. ☠️ Webp делал НАШ `prepareImage()` (> 1,5 МБ → 1600 px webp). Исходник теперь едет в заказ; ⚠️ оригиналы
     в `this.logoOriginals`, НЕ в `optCache` (черновик 3 МБ). Лимиты боевого 999 МБ, но base64: 13 → 17,9 МБ
     в POST — при жалобах вводить потолок.
  🔴 **Клиенту не отвечено:** webp — наша причина; «открыть в вкладке и скачать» качества не теряет.
- **15.09 шрифт и логотипы в заказе** (`resolveFont`; `jetron_logos[]` → медиатека → `additional.logos`).
  ⚠️ Приёмник — ТЕМА: `themes/jetronsport/inc/woocommerce/class-wc-cart-logo-jetronsport.php`, наша правка строк
  54, 56, бэкап `THEME-backup-20260915-215654.php`; ⚠️ **обновление темы её затрёт — перепроверять**. Заказ
  с логотипами на живом не проверялся.
- Ранее: 15.09 скорость (3 010 → 1 495 КБ); 14.09 карточка «Футболки» (мёртвая `#posters` не тронута — сторож
  `$jtn_side_live`); 13.09 зоны на 45 форм; ✅ 10.09 приёмка боевого без дефектов, кроме п. 7.

## ☠️ Запреты и грабли

**Боевой и данные**
- 🔴 **Не трогать `zones.json`, пока клиент с ним работает:** сервер пишет файл ЦЕЛИКОМ (`jetron_zones_write`),
  наша запись затрёт его правки; сначала `Last-Modified`. Защита — `zonesSaveGuard`, статус `ok / missing / failed`.
  Корневой `zones.json` — переопределения поверх `mock-config.json`; потеря файла роняет на `zoneTemplate`.
- 🔴 **Порядок моделей — в ОБОИХ файлах:** боевой `admin.json` и `mock-config.json` (иначе «Вернуть исходный
  каталог» откатит). Минифицированный `admin.json` = `json.dumps(ensure_ascii=False, separators=(',',':'))`;
  `JSON_PRETTY_PRINT` от плагина — штатно. ⚠️ После проверки на боевом порядок вернуть.
- ☠️ `python скрипт | iconv … || python скрипт` запускает скрипт ДВАЖДЫ и затирает бэкап — без `||`.

**Тема (`jetron-v2/hero.php`)**
- ☠️ **Правка значения в теме НЕ действует:** `hero.php:68` берёт `$jtn_links` из ACF (`jtn_dp()`, мета
  `jtn_dp_<имя>` страницы ID 2). Адрес — в КОНТЕНТЕ, поле `field_jtn_dp_link_tshirt`: 📌 клиент правит сам.
  Схема полей — `jetron-v2/design-fields.json`.
- ⚠️ Стиль — инлайном в `hero.php`, НЕ в `preview-current.css` (перегенерируется). Накладка —
  `<a class="jtn2-side-hit">` прямым потомком `<article>`. ⚠️ Стрелку не трогать (`!important`). `.jtn2-side`
  скрыт ниже 1025 px — на телефоне вход в конструктор через бургер и подвал.
- ⚠️ Меню не WP, вёрстка в `<nav class="jtn2-nav">`. Правило 13.09: **шапка без конструкторов, бургер
  С конструкторами, подвал как был**; `a.jtn2-calc` оставлена намеренно.
- `hero.php` — 29 247 Б, sha `6c7d1ccb13e9f902`; бэкапы `C:\tmp\hero-prod-20260914-065007.php`,
  `C:/tmp/jetron-backup-2026091{2,3}/`.

**Админка WP и mu-плагин**
- ⚠️ `form.submit()` падает — `HTMLFormElement.prototype.submit.call(форма)`. ⚠️ Вкладку задавать явно:
  `&tab=models`, `&tab=view` (дефолт `prices`). 🔴 Поля вида продублированы в PHP (`jetron_admin_view_fields()`),
  стережёт `tests/Appearance.test.js`. ⚠️ Число моделей снимать ДО `array_splice`.

**Код конструктора**
- ⚠️ Рамка шорт (`shorts_number`/`shorts_logo`) — только в `?zones=edit` (`addEditorFrame`); в
  `placementOptions` НЕЛЬЗЯ (станет опцией и заденет цену). Рамки на восьми донорах переставлять не нужно.
- ⚠️ **Зоны — только ВНУТРИ линейки** (кадр 836 у Champion, 937 у New), `tests/test_zonesets_sync.py`.
  Пересобрали мокапы — разносить зоны заново и пересчитать каталог (связи в коде нет). После расстановки зон
  картинки не менять. «Синий3» и рамки зон у покупателей исчезают ТОЛЬКО при выкладке (жили в боевых файлах).
- ⚠️ «Разнести по линейкам» заполняет только ПУСТЫЕ; красная «Разнести от этой расцветки» ЗАТИРАЕТ линейку,
  **отката нет**.
- 🔴 **«Логотип слишком тяжёлый…» убрана владельцем — не возвращать.** Черновик 3 МБ (`DRAFT_MAX_BYTES`),
  лишние логотипы теряются молча; `DraftWiring.test.js` перевёрнут намеренно. Сообщение про неработающее
  хранилище осталось.
- 🔴 **Сказать клиенту:** латинский шрифт (`juventus` +15) с кириллицей → откат на `rpl` (`resolveFont`).
  Шрифт логотипов по спине — дефолт (`c.fontId ||`).
- ⚠️ `applyColorHexes` идёт ДО `buildColorPicker()` — палитра перерисовывается в `цены.then`. Рамку 1px красить
  прозрачным, не `border: none`; вид — токены `--stage-bg`, `--card-*`, отступы узких экранов — медиазапросы
  `stand.css` (стр. 1162, 1251). Порядок карточек — только `placementOptions` в `mock-config.json`.
  `UPLOAD_ACCEPT` = `image/*,.png,.jpg,.jpeg,.webp,.gif,.bmp,.svg` (расширения чинят мобильные выборщики);
  `prepareImage()` на MIME не смотрит.
- ⚠️ Кадры линеек 836..937 (`TOP_PX` 60), холст 439..501; решает ПРОПОРЦИЯ, формула —
  `docs/ПЕРЕДАЧА-2026-09-01-правки-15-28.md`.
- ⚠️ Полоса и обрезанный H1 на телефоне — тема (`.page-content` 3443). Жёлтый боевого `#ffde00` (каталог).
  Кнопки «в карточку» нет у ВОЗРАСТА (детский 45/45, взрослый 36/45) — не дефект.

**Замеры — как не получить ложь**
- ⚠️ До «у клиента сломано X» — открыть конструктор глазами. 🔴 **Пять профилей** (1440 / 1045 / 768 /
  375 с тачем / 360), телефон — только с `hasTouch`/`isMobile`.
- ⚠️ `.opt-body` есть в DOM только у РАСКРЫТОЙ опции (на свёрнутой `input[type=file]` = 0 — не «поля нет»).
  Загрузку шрифтов — по `app.fontsReady`.
- ⚠️ Черновик `jetron:draft:v1` искажает замер — сбрасывать `app.optCache` (`localStorage.removeItem` из CDP
  даёт `SecurityError`). Возрасты — `ageOptionsNow()` (`priceForAge` нет).
- ⚠️ Опровергнуто: «HTTP/1.1» (h2), «зоны затёрлись» (`2f325f1`), «зоны champion-white уехали» (промах у Star).
  Прочее — `docs/ГРАБЛИ-инструментов.md`.
- ⚠️ **Вёрстку тесты не стерегут** — после CSS смотреть глазами.

## 📂 Где что лежит

- **Запуск:** `npx --yes serve .` или `python -m http.server 8778`; `?zones=edit` — к КОРНЮ. 🔴 Preview MCP
  и Claude in Chrome отвалились — мерим headless Chromium (код в ГРАБЛЯХ).
- **Тесты:** `npm test` **240/240**, `python -m pytest` **43 passed**, набор целиком. Хуки `window.__jetronApp`,
  `window.__jetronEditor`. ⚠️ Красное снимать только `npm test`: `tdd-guard` не видит `node --test <файл>`
  и блокирует следующую правку. PHP — линт
  `Get-ChildItem deploy\*.php | %{ & 'C:\tools\php82\php.exe' -l $_.FullName }`.
- **Файлы:** `src/js/ui/` (`app.browser.js`, `canvas.browser.js`, `zone-editor.browser.js`), `src/js/core/`
  (`CanvasFit`, `ZoneManager`, `TextColor`, `CatalogPrices`, `SizeMatch`, `ZoneOverrides`, `Appearance`,
  `AdminOverrides`, `ModelHost`), `src/config/mock-config.json`,
  `zones.json`, `src/css/stand.css`, `scripts/` + `mockup-map.json`. Голосовые — `docs/voice-client-2026-*.txt`,
  снимки `docs/screens-2026-09-*/`, бэкапы боевого `C:/tmp/jetron-backup-2026*/`.
- ⚫ **Не переспрашивать** (`-volna5-otvet.md`): пропорция A, New розовая, общая палитра, адреса карточек
  (`_renderLineBadge()` внутри `updatePrice()`), бирочные S и M (`73d5996`), шесть ответов 28.08, хвост каталога,
  письма №7, №9-№12, созвон, меню закрыто 06.09, крестик удалён (`tests/ExitButton.test.js`).
