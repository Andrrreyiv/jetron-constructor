# jetron-constructor — конструктор футбольной формы (jetronsport.ru)
Обновлено: 2026-09-02. 🟢 Файл ужат 02.09 с 34 223 байт под потолок 25 600: копия ДО сжатия и всё
снятое — `docs/HISTORY-jetron-constructor-2026-09-02-volna5-otvet.md`, прежние версии —
прочие `docs/HISTORY-*.md`.
⚠️ Файл у потолка: дописывать только ВЗАМЕН, снятое и опровергнутое не возвращать.
📌 **Первым читать `docs/ПЕРЕДАЧА-2026-09-02-волна-5-ШАГ1-код.md`** (состояние кода и что доделать),
затем `-волна-5-рамки.md` (постановка и арифметика), `-волна-4.md`,
`-2026-09-01-правки-15-28.md`, `docs/ПЕРЕДАЧА-2026-08-{31*,30,29,28}.md`, `docs/КОММЕНТАРИИ-КЛИЕНТА-2026-08-31.md`.

---

## 🟢 ВОЛНА 5, ШАГ 1 — СДЕЛАН И ПРОМЕРЕН 02.09. Не закоммичен, не на демо: нужны ветка и PR

🔴 **Первым — `docs/ПЕРЕДАЧА-2026-09-02-волна-5-ШАГ1-код.md`**: что правлено (6 файлов, ветки нет),
числа «было/стало», три места, где легко наступить снова.
Коротко: рамка стала ОДНА (ПК было 8 ширин карточки и 6 высот → **1 и 1**; телефоны 8 высот → 1),
Чемпион не тронут ни на пиксель, зоны не трогаются вовсе, `npm test` **92/0**, `pytest` **41**.
🟢 **Зазор ряд → чернила просел от «одной рамки» (→ 11.7/14.5/14.6/17.6/12.6) и вылечен:**
`.views padding-top` 30→**33** у ≤560 плюс новое 5px у ≤900, стало **14.5/17.5/17.8/20.6/17.6** —
база 13.1/16/17/19/17 превышена всюду. ⚠️ Промежуточных **32 не хватало** (на 375 было 16.8
против 17), видно ТОЛЬКО замером. `.localdev/` удалена, код замера — хвост `ГРАБЛИ-инструментов.md`.
🔴 **Осталось: ветка → PR → кнопку жмёт владелец**, затем сверка sha256 демо с `origin/main`.
Ниже — постановка задачи и арифметика, по которым это писалось.

## 🔴 ВОЛНА 5 (02.09, 13:07) — постановка и арифметика решения

Клиент: «Чемпион — идеальные размеры. Нажимаю New — **картина просто рушится**… **почему мы
не можем одну рамку белую сделать и туда вставить все картиночки?**» — `docs/voice-client-2026-09-02.txt`.

⚠️ **Симптом был ЗЕРКАЛЬНЫЙ, и замер на одном устройстве врал про другое:** на **ПК** держалась
высота 465 и гуляла **ширина** 439…501, на **телефоне** держалась ширина 388 и гуляла **высота**
360…411. Клиент смотрит с телефона и был прав дословно. 🔴 **На этом уже обожглись:** померив
только 1366×768, написали в черновик «на New отступ снизу не растёт» — ложь про его экран.
**Мерить ВСЕГДА пять профилей.**

🟢 **Боевой проверен дважды (файлы и экран) — копировать оттуда НЕЧЕГО, там ХУЖЕ** (20 размеров
и 16 пропорций против 8 и 8). Гипотеза «на боевом уже выровнено» опровергнута.

🟢 **Письмо про рамки ОТПРАВЛЕНО, 13:44 и 13:52 клиент ОТВЕТИЛ: «Да, Андрей, всё правильно вы
говорите» — план принят.** Разбор — ПЕРЕДАЧА волны 5, раздел «Ответ клиента». Три факта:
1) его рецепт — холст 900×1200 у всех, картинку центровать и НЕ масштабировать; 2) работа делится
на **два шага**, шаг 2 «формы внутри растянуть по рамке» он готов делать сам — **выравнивание
размеров форм из приёмки ВЫНУТО**; 3) единственное условие к шагу 1 — «**верхние кнопки слишком
близко не приближались**» (это зазор волны 4; именно он и просел, см. блок про шаг 1 выше).
⚠️ **Сигнал: «долго и сложно, наверное придётся мне это всё подгонять»** — он забирает работу
себе. Ответ нужен со СРОКОМ, а не с новым разбором.

🔴 **Буквальные 900×1200 брать нельзя, объяснять числами** (он описывал СПОСОБ, а не требовал
эти цифры): решает только ПРОПОРЦИЯ. 900×1200 даёт **349 px на ПК (−30 % к Чемпиону) и 517 на
телефоне (+44 %)**, а 900/836 даёт **500 и 360 у всех** — ровно нынешнего Чемпиона, которого он
назвал идеальным. Выкладка — в комментарии `CanvasFit.js`.
✅ **Реализовано: пропорция 900/836, высоту линейки оставить, ширину добить белым.**
⚠️ Star и после правки останется с двойным отступом снизу: его держит сырое фото `Star чёрный`.
🔴 Порядок 1) `Star чёрный` → 2) рамки → 3) зоны — жёсткий, разбор под таблицей задач.

## 🟢 ВОЛНЫ 1-4 закрыты и на демо, разработки не осталось (разборы — ПЕРЕДАЧИ волны 4 и 01.09)

🟢 **PR #4-#9 влиты владельцем, `origin/main` = `b383ff4`** (#8 `d4f833c` — код волны 4, #9 — письмо
№15); ⚠️ открыт **PR #10**, только документация. На ЖИВОМ адресе sha256 демо = `origin/main`
у `index.html`, `stand.css`, `app.browser.js`; телефон 320/360/375/412/768: зазор ряд → чернила
**13.5 / 16.5 / 17.5 / 19.5 / 17** (было 1.1 / 4 / 5 / 7 / 17 и крестик везде), три пилюли высотой 36
(ни одна не переносится), крестика нет, перелив 0. `npm test` **89/0**, `pytest` **39 passed**.
Три правки волны 4: **крестик удалён** из JS и CSS (запереть покупателя нельзя: конструктор — iframe
на странице WP 3443, шапка сайта всегда снаружи; сторож `tests/ExitButton.test.js`) ·
**`white-space: nowrap`** у `.line-badge-pill` · **`.views{padding-top:30px}`** в `max-width: 560px`
(было 18), перелив **0 у восьми линеек на 1366×768**. 📌 Урок «узкий viewport — это НЕ телефон»
(без `hasTouch`/`isMobile` пилюли 28.3 px вместо 36) — в ГРАБЛЯХ.
🟢 Клиент: «в остальном всё хорошо, всё прекрасно» — волны 1-3 приняты.
- 🟢 Волны 1-3: токен буста **`20260901a`** в 21 месте, сторож `tests/PickerRedraw.test.js` (+2 теста);
  на демо после #6 зазор до правого края **0**, ряд 36 px, свотч **36×36**, палитра ПК столбиком
  26 px, sha256 демо = `origin/main` у **СЕМИ** файлов (те три плюс `canvas.browser.js`,
  `TextColor.js`, `mock-config.json`, `zones.json`).
- 🔴 **Боевой `jetronsport.ru/constructor/` СТАРЫЙ**, мерж его не обновляет: 02.09 sha256 разошлись
  у `stand.css`, `app.browser.js`, `zones.json` — нужна ручная выкладка (п. 5).
- ⚠️ Две ловушки замера (зазор мерить до края РЯДА, прокрутку — на настоящем скроллере) — в ГРАБЛЯХ.

### Осталось человеческих рук

🟢 **Письмо по волне 5 отправлено 02.09, ответ клиента получен** (см. выше). Текст письма —
в ПЕРЕДАЧЕ волны 5, раздел «Готовый текст клиенту».
🔴 **Осталось ответить на его ответ:** согласиться с целью дословно, показать два числа
(ПК −30 % против «Чемпиона не трогаем») и назвать СРОК. Текста ещё нет.
🔴 **Отправить письмо №15** (`docs/сообщение-клиенту-15.md`) — 14 пунктов, закрывает волны 2, 3 и 4.
⚠️ Оно уже неполно: заканчивается словами «осталось два пункта, и оба за вами», а волна 5 добавила
третий, который за нами. Решить: слать №15 и ответ по волне 5 порознь (честнее: отчёт и ответ
на вопрос — разные вещи) либо слить в одно.
⚫ **№14 ОТМЕНЕНО, не слать:** его пункты 7 и 9 обещают, что крестик «уменьшен и сдвинут наверх»,
а волна 4 убрала крестик совсем — отправить значит соврать про первое, что клиент увидит.
⚠️ В №15 п. 9 обязан остаться честным про сдвиг картинки на 18 px; про уменьшение свотчей
на 20 % клиенту не писать (36 px ниже минимума 44 px — осознанная уступка его просьбе).
🟢 №13 отправлено 01.09 и не отменяется (прежняя редакция — `62d375e`).

---

## 🎯 Что осталось, кроме мержа и письма

| # | Что | На ком | Блокер |
|---|---|---|---|
| 1 | 🔴 Исходник **`Star чёрный`** без гетр и бутс — **единственный живой дефект картинок** (пункт 3 первой волны сделан у 44 из 45, мержем #5 НЕ закрыт; у Space чистка сработала: 1101 → 1035 → после среза 879) | **клиент** | сырое фото вместо композита; пока не придёт — линейка Star держит кадр 954 (был 1110), покупатель видит гетры, и Star не выровняется по п. 2 |
| 2 | 🟢 **ВОЛНА 5, шаг 1: одна пропорция рамки у всех восьми линеек** — **сделано и промерено 02.09**, тесты 92/0 и 41, рамка одна, Чемпион не тронут, зазор выше базы; ⚫ пересчёт долей зон отменён, он не нужен. ⚠️ Шаг 2 «растянуть формы внутри рамки» клиент вынес отдельно и готов делать сам | мы, **осталось ветка → PR → кнопка владельца** | не закоммичено; разбор — `docs/ПЕРЕДАЧА-2026-09-02-волна-5-ШАГ1-код.md` |
| 3 | Восемь карточек-доноров с расстановкой зон | **клиент** | ⚠️ ждёт п. 2, иначе вся ручная расстановка в мусор |
| 4 | Размножение зон донор → вся линейка | мы, **код не написан** | ждёт п. 3; между линейками нельзя, кадры у линеек разные |
| 5 | Выкладка на боевой + чистка ОБОИХ меню (ID 3 и ID 55) | мы, по команде | строго по `docs/ДЕПЛОЙ-боевой.md`; «синий3» и рамки зон у покупателей исчезнут только здесь |
| 6 | КП по админке конструктора ФУТБОЛОК (18 000 / 30 000 ₽) | **Андрей** | не отправлено |

🔴 **Порядок 1 → 2 → 3 жёсткий, цена нарушения на клиенте:** правка `Star чёрный` меняет высоту
кадра линейки, от высоты считается добивка ширины (п. 2), а зоны заданы **долями от кадра** —
расставит раньше п. 2, переносить придётся все восемь карточек руками.

---

## ⚠️ Не переоткрывать — вынесено в хвост `docs/ГРАБЛИ-инструментов.md`

Шесть тем, каждая проверена замером, заново не расследовать: пункт 9 (ряд плашки на вороте) · ряд
плашки шире картинки на 13 px с каждой стороны на ПК — НЕ баг · «медиазапрос не повышает
специфичность» · «зоны champion-white уехали вверх» — не подтверждено 02.09 · отступ номера = 1 px ·
что осознанно не делали (прокрутка панели настроек, бахрома у 3 шрифтов из 23).
⚠️ **Долг по тестам: вёрстку тесты не стерегут вообще, покрыт 1 пункт раскладки из 7** — после
правки CSS смотреть глазами, зелёный `npm test` не значит тут ничего.

Продукт: конструктор футбольной ФОРМЫ (vanilla JS + Fabric.js v6), 8 линеек, зоны (номер / фамилия /
лого), редактор `?zones=edit`; заказ через profi.ru, заказчик он же WP-админ. ⚠️ **НЕ ПУТАТЬ
с `tshirt-constructor`** (ФУТБОЛКИ, репо того же заказчика): принты по см, ivory, плотности,
библиотека принтов — туда; кнопки «перёд/зад» ТОЛЬКО там, у формы один вид.

---

## 🧩 Контракт кадра и зон — целиком в HISTORY `-volna5-otvet.md`

**Открывать ДО правки зон, мокапов или `run_normalize.py`:** высота кадра по ЛИНЕЙКЕ, два прохода
нормализации, пересчёт зон, три места хранения, резолв, WebP q92, снятый диагноз «окно жёсткое 900×1200».
⚠️ **Кадры линеек СМЕНИЛИСЬ: 992..1110 → 836..954** (`TOP_PX` 216→60, `СДВИГ_ФОРМЫ` = 156), холст
на экране вырос до **439..501 px**; прежние числа про кадр 992 и холст 465 — состояние ДО этого.
⚠️ **`y_new = y_old × 1200 / H` верна, только когда кадр меняется СНИЗУ** — после среза верхней
полосы 01.09 формула другая; она, `h_new` и `СДВИГ_ФОРМЫ` = 156 (`scripts/normalize_mockups.py`) —
в `docs/ПЕРЕДАЧА-2026-09-01-правки-15-28.md`.

---

## 💰 Деньги и статус

- **Основной этап** сдан и принят **02.08.2026**, оплачен; **админ-панель конструктора формы**
  сделана, установлена на боевой, оплачена.
- **Второй заказ** (нормализация мокапов + две новые линейки): **25 000 ₽, 5 рабочих дней**;
  `docs/otklik-klientu-2026-08-23.md` отправлено 26.08, предоплата получена, в работе.
  🔴 **Ниже 25 000 ₽ не опускаться.** Путь цифры: 40 000 ₽ (25-33 ч) − «разрез» (10 000 ₽) = 30 000 ₽
  → Андрей сам снизил до 25 000 ₽, хотя ДОБАВИЛИСЬ Волна и Фаворит с нуля и перестановка зон по 45.
  Уступка, а не расчёт по объёму.
- 🔴 **КП по админке конструктора ФУТБОЛОК не отправлено:** 18 000 ₽ отдельно / 30 000 ₽ за обе.
- ⚫ Всё закрытое на прошлых этапах (комментарии 24.07 и правки 27.07, цена из карточки WooCommerce,
  админ-панель, размеры, касса и `wp_jetron_orders`, аудит 31.07, `73d5996`, промах зон 31.08,
  дефект п. 13) — в HISTORY `-volna5-otvet.md`, заново не проверять.

## 📋 Текущий заказ

**Состав согласован 22.08: 8 линеек, 45 расцветок** (Чемпион 6, Легенда 6, New 5, Space 6, Star 5,
Winner 7, Волна 5, Фаворит 5; коралл не заводим, тёмно-бирюзовая = зелёная). Единственный источник
правды по составу и по паре «исходник → цель» — `scripts/mockup-map.json`, второй список рядом
неизбежно разъедется. Исходники клиента на диске, повторно не просить: `C:/Users/Андрей/Downloads/`,
⚠️ копии с ` (1)`, ` (2)` отбрасывать по скобке.
⚠️ **Этап «разрез 45 → 90 файлов» в объём НЕ возвращать** — клиент опроверг его 23.08.
⚫ История пяти пунктов письма (все закрыты, зоны клиент снял с нас 28.08, осталась выкладка) —
в HISTORY 28.08 и `-volna5-otvet.md`.

---

## 📂 Где что лежит

- **Git:** `github.com/Andrrreyiv/jetron-constructor`, `origin/main` = **`b383ff4`** (PR #9; до него
  #8 `d4f833c` — волна 4, #7 `2b3b67c`, #6 `42bdda5`, #5 `c351f9c`, #4 `4ed553c`), слитые ветки
  удалены. ⚠️ Смотреть `origin/main`: локальный отстаёт после мержа. ⚠️ У `gh` и GitHub MCP токен
  протух (`Bad credentials`), рабочий — в Credential Manager: `git push` идёт, API — через
  `git credential fill` + urllib.
- **Запуск:** `npx --yes serve .` (`.claude/launch.json`, `jetron-preview`, порт 8778), ⚠️ `?zones=edit`
  цеплять к КОРНЮ. 🔴 01.09 Preview MCP и Claude in Chrome отвалились оба, мерили headless Chromium
  (код — в ГРАБЛЯХ); демо `github.io` из браузера не открыть.
- **Тесты** 02.09 на коде волны 4: `npm test` **89/0**, `python -m pytest` **39 passed** (+1 дал
  `tests/ExitButton.test.js`). Гонять набор целиком, иначе провал сбора не виден. Рантайм-хуки
  `window.__jetronApp`, `window.__jetronEditor` (`?zones=edit`).
- **Ключевые файлы:** `src/js/ui/` — `app.browser.js` (панель, рендер, плашка, `gridForAge`),
  `canvas.browser.js` (Fabric: placeText/placeImage, `_seatNumber`), `zone-editor.browser.js`;
  `src/js/core/` — `ZoneManager.js` (`fitInkToRect`), `TextColor.js`, `CatalogPrices.js`, `SizeMatch.js`;
  `src/config/mock-config.json`, корневой `zones.json`, `src/css/stand.css`; `scripts/` —
  `run_normalize.py`, `normalize_mockups.py`, `rewrite_catalog.py`, `mockup-map.json`.
- Голосовые `docs/voice-client-2026-*.txt` (свежая `-09-02`), размеры — `docs/РАЗМЕРЫ-КОНФЛИКТ-2026-07-30.md`.

### Два таргета деплоя
- **Демо:** GitHub Pages из `main` (workflow'ов нет) → https://andrrreyiv.github.io/jetron-constructor/
  Деплой = слияние ветки в `main` **кнопкой PR**, ~минуту, замер сходится с локальным (кадры 836..954).
  ⚠️ Каталога демо не видит: цены падают на конфиг, кнопка «перейти в карточку» ведёт в РАЗДЕЛ
  каталога — решение, которое клиент опроверг 28.08. Показывать только с этой оговоркой, она в **№13**.
- 🔴 **Боевой:** `https://jetronsport.ru/constructor/` (WordPress/WooCommerce). Канал — **WP File
  Manager (elFinder), НЕ git**, нужен залогиненный Chrome с расширением Claude. **Порядок —
  в `docs/ДЕПЛОЙ-боевой.md`**; опаснее всего: битый PHP кладёт весь сайт, товар 3446 не удалять,
  меню ДВА (ID 3 и ID 55).

---

## ❓ Клиент — осталось только живое

🔴 **При выкладке чистим ОБА меню, ID 3 (десктоп) и ID 55 (бургер)** — ответ 31.08 «везде убрать,
оставить только в футере», правка одного на второе не переносится. ⚠️ Клик «Сохранить меню» через JS
блокируется классификатором прав: кликать инструментом `computer` по координатам после свежего
скриншота.
⚠️ **«Синий3» и рамки зон у покупателей исчезнут ТОЛЬКО при выкладке:** «синий3» в `mock-config.json`
не было никогда, она жила в корневом `zones.json` и в `admin.json` на боевом. (Оба блокирующих
вопроса закрыты голосовыми 31.08 и в коде: `72ab2f5`, отступ уточнён `13a38af`.)
🆕 **Размножение зон** (п. 3-4 таблицы, разбор — `docs/ПЕРЕДАЧА-2026-08-31.md`): блок корневого
`zones.json` с карточки-донора копируется на все формы той же `line`, без математики и **по команде**,
а не на каждой пересборке — внутри линейки кадр один. Между линейками НЕЛЬЗЯ: кадры разные.
🟢 Палитра слева на ПК сделана; номер на груди ведомый по цвету (30.08) и шрифту (31.08),
`src/js/core/TextColor.js`. Клиенту молчим до выкладки.
⚫ **Снятое, НЕ переспрашивать** — в HISTORY `-volna5-otvet.md`: пропорция (вариант A), New розовая,
общая палитра, адреса карточек (⚠️ `_renderLineBadge()` живёт внутри `updatePrice()`), бирочные
S и M (`73d5996`), шесть ответов 28.08, хвост каталога клиента, письма **№7 и №9-№12**, созвон.

## ⚠️ Живые предупреждения

- 🔴 **Сливать в `main` через PR, кнопку жмёт человек** (`gh` без учётки, GitHub закрыт доменной
  политикой Claude in Chrome). ⚠️ `git push origin <ветка>:main` проскакивает мимо
  `dangerous-actions-blocker.mjs` (ловит только буквальное `git push origin main`) — это дырка
  в шаблоне, а не разрешение.
- 🔴 **ВСЕ предупреждения по боевому — в `docs/ДЕПЛОЙ-боевой.md`, раздел «Живые предупреждения»;
  открывать ДО первой правки боевого**, дубля списка здесь нет — он разъезжался с оригиналом.
- ⚠️ **Перед утверждением «у клиента сломано X» открыть конструктор глазами.** Чтение конфига без
  взгляда на экран дважды дало ложь: «31 расцветка без зон» (наследуют `zoneTemplate`) и «22 файла
  одинаковые на переде и заде» (вид один, кадр композитный) — вторую поймал клиент.
- ⚠️ Переиспользуемые замеры (эталон долей 1200, композитный кадр, сетки ACF по `pa_model`, бирочная
  шкала), грабли инструментов и headless-обход мёртвых Preview MCP / Claude in Chrome —
  в `docs/ГРАБЛИ-инструментов.md`. Не переделывать: цифры замерены, грабли проверены.
