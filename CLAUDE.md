# jetron-constructor — конструктор футбольной формы (jetronsport.ru)

Обновлено: 2026-09-15. Потолок 25 600 Б. ⚠️ Дописывать только ВЗАМЕН, снятое не возвращать.
📚 **История целиком, побайтово — `docs/HISTORY-jetron-constructor-2026-09-15.md`** (78 677 Б,
sha256 `E8EA18F1…`): пересказы волн правок, протоколы замеров, опровергнутые гипотезы, списки
сделанного по датам. Искать там, а не расследовать заново.

Продукт: конструктор футбольной ФОРМЫ (vanilla JS + Fabric.js v6), 8 линеек, 45 расцветок, зоны
(номер / фамилия / лого), редактор `?zones=edit`; заказ через profi.ru, заказчик он же WP-админ.
⚠️ **НЕ ПУТАТЬ с `tshirt-constructor`** (ФУТБОЛКИ, тот же заказчик): принты по см, ivory, плотности,
библиотека принтов — туда; кнопки «перёд/зад» ТОЛЬКО там, у формы один вид.

📌 **Открывать первым:** `docs/ДЕПЛОЙ-боевой.md` — выкладка + раздел «Живые предупреждения», до первой
правки боевого; дубля списка здесь нет намеренно, он разъезжался с оригиналом. `docs/ГРАБЛИ-инструментов.md`
— переиспользуемые замеры (эталон долей 1200, композитный кадр, сетки ACF по `pa_model`, бирочная
шкала), грабли инструментов, headless-обход мёртвых Preview MCP / Claude in Chrome; не переделывать.
Прежние разборы, заново не расследовать: `docs/HISTORY-jetron-constructor-2026-09-08.md`,
`ПЕРЕДАЧА-2026-09-07-комментарии-клиента.md`, `-09-04-*`, `-09-02-волна-5-*`, `-09-01-правки-15-28`,
`-08-{31*,30,29,28}`, `КОММЕНТАРИИ-2026-08-31`, `ПРОВЕРКА-2026-09-09.md`, HISTORY `-volna5-otvet.md`
(контракт кадра и зон).

## 💰 Деньги и состав

- Основной этап и админ-панель конструктора формы сданы, приняты 02.08.2026, оплачены.
- **Второй заказ** (нормализация мокапов + две линейки): **25 000 ₽**, предоплата получена.
  🔴 **Ниже 25 000 ₽ не опускаться:** по расчёту 30 000 ₽, Андрей снизил сам, хотя ДОБАВИЛИСЬ Волна
  и Фаворит — уступка, а не объём. 🔴 **Доплат по форме НЕТ:** всё сделанное 12-15.09 либо наши
  недоработки, либо входило в объём; «Внешний вид» и сортировка моделей тоже в рамках 25 000,
  цену за них клиенту НЕ называли.
- 🔴 **КП по админке конструктора ФУТБОЛОК не отправлено:** 18 000 ₽ отдельно / 30 000 ₽ за обе.
- **Состав согласован 22.08: 8 линеек, 45 расцветок** (Чемпион 6, Легенда 6, New 5, Space 6, Star 5,
  Winner 7, Волна 5, Фаворит 5; коралл не заводим, тёмно-бирюзовая = зелёная). Единственный источник
  правды по составу и парам «исходник → цель» — `scripts/mockup-map.json`, второй список разъедется.
  Исходники на диске, не просить: `C:/Users/Андрей/Downloads/` (копии с ` (1)` отбрасывать).
  ⚠️ **«Разрез 45 → 90 файлов» в объём НЕ возвращать** — опровергнут клиентом 23.08.
- ⚫ Закрытое на прошлых этапах (правки 24-27.07, цена из WooCommerce, админ-панель, размеры, касса,
  аудит 31.07, промах зон 31.08) — в HISTORY, заново не проверять.

## 🔴 Незакрытые долги

**Руки Андрея.** Отправить письма: **`docs/сообщение-клиенту-19.md` — С ПЯТЬЮ СНИМКАМИ**
`docs/screens-2026-09-04/` (просил дословно, без вложений первый пункт бессмыслен), затем `-20`,
`-21`, `-22`, **`-23` — ОБЯЗАТЕЛЬНО с вложением `docs/Инструкция_редактор_зон.docx`**. ✅ №13, №16,
№17, №18 отправлены, повторно не слать. ✅ **№24 ОТПРАВЛЕНО 15.09** (шрифт в заказе, файл логотипа,
пустое поле «Логотипы», три вопроса по корзине) — повторно не слать; ждём от клиента телефон и браузер
того покупателя, запись экрана и уточнение «корзина или уже оформление». 🟡 За клиентом также ответ,
делать ли узкий столбец в корзине на телефоне (314 px при экране 390) — спрошено в №24. ⚫ **№14 и №15 НЕ слать:** №15 замещено целиком, №14 обещает
крестик, которого волна 4 лишила. 🔴 **В `-22` ПРОПУСК:** клиент просит сумму к переводу, а у нас
записано только «предоплата получена» без числа — остаток Андрей вписывает сам, не выдумывать.
В `-23` задан вопрос (запрещать ли латинские шрифты при русском тексте в логотипе) и есть абзац
про зоны, его перемерить, если письмо уйдёт не сегодня. ⚠️ В письме про меню признать, что пункты
туда вернули **МЫ** 12.09, клиент не передумал. 🔴 Про правку 14.09 (карточка «Футболки») и про
скорость 15.09 клиенту НЕ написано. Выставить счёт.

**⚠️ Живо для ЛЮБОГО письма:** про уменьшение свотчей на 20 % не писать; оговорки про витрину
и «раздел каталога» из №13 не повторять (устарели); **числа и причины переносить только после
сверки с кодом**; **перемерять всё, что письмо называет числом или словом «работает»** — 04.09
за сутки протухли и «сделано», и «подождите».

**За клиентом:** рамки номера на шортах узкие (наследие старой механики) — растянуть на белом доноре
и нажать «Разнести по линейкам». Новые формы настраивает сам (04.09: «картинки не трогаем»).

**Хвосты.** ✅ Пп. 1-5 закрыты (`Star чёрный`, волна 5 шаг 1, доноры зон, размножение донор → линейка,
выкладка 08-15.09). Осталось: **6** — КП по админке футболок, на Андрее, не отправлено. **7** —
«Таблица размеров» на ТЕЛЕФОНЕ, область нажатия **125×16 px** при `padding: 0` (соседи 46-82); пара
строк в `stand.css`, не согласовано с клиентом, нужна отдельная выкладка. **8** — woff2 для клубных
шрифтов, 1,63 МБ → 0,47 МБ (замерено), ждёт решения. **9** — уменьшенная копия логотипа в черновик
(PNG с прозрачностью), убрала бы молчаливую потерю; предложено, не делалось.

## 🌿 Git и выкладка

- `github.com/Andrrreyiv/jetron-constructor`. 🟢 **13.09 всё влито: PR #32 смержен**,
  `origin/main` = `24b2ed1`, локальный `main` совпадает; ветка `fix/shorts-number-frame-2026-09-12`
  вошла целиком, расхождения «ветка впереди main» больше нет.
- 🟢 **20.09 ВСЁ ВЛИТО: `origin/main` = `ad19905`**, ветка `feat/knopki-shrift-cvet-2026-09-20`
  целиком в `main` (проверено `git merge-base --is-ancestor`, впереди 0), открытых PR нет.
  Демо пересобралось и отдаёт `?v=20260920c`.
- ⚫ **PR #33 влит 15.09: `origin/main` был `f483e8d`**, ветка `docs/merge-2026-09-13` (`3bd518a`)
  целиком в `main` (проверено `git merge-base --is-ancestor`, не логом). Демо пересобралось и отдаёт
  `v=20260915c`. ⚫ Прежняя запись «висит чужой PR #31 “Вторая выкладка 09.09”» УСТАРЕЛА: он закрыт,
  как и #10, открытых PR нет (замер GitHub API) — заново не искать.
  ⚠️ Мерж обновляет ТОЛЬКО демо, боевой не трогает.
- ⚠️ Намеренно не закоммичен `src/config/mock-config.json.bak2-20260913` — под `.gitignore`
  (`*.bak-*`) не попал из-за «bak2». 🔴 **Правка `hero.php` от 14.09 НЕ в репозитории:** локальной
  копии темы нет, канал — File Manager.
- 🔴 **Сливать в `main` только через PR, кнопку жмёт человек** (`gh` без учётки, GitHub закрыт
  доменной политикой Claude in Chrome). ⚠️ `git push origin <ветка>:main` проскакивает мимо
  `dangerous-actions-blocker.mjs` (тот ловит только буквальное `git push origin main`) — дырка
  в шаблоне, а **не** разрешение.
- ⚠️ **Хэш брать только `git rev-parse origin/main`:** 09.09 запись «origin/main = a3e7807» взята
  из лога HEAD и увела на полдня. ⚠️ Токены `gh`/MCP мертвы (401), рабочий — в Credential Manager:
  `git push` идёт, API — через `git credential fill` + curl.
- **Демо:** GitHub Pages из `main` (workflow'ов нет) → `andrrreyiv.github.io/jetron-constructor/`,
  деплой = слияние кнопкой PR. ⚠️ **Мерж обновляет ТОЛЬКО демо, боевой не трогает.** ⚠️ Каталога демо
  не видит: цены падают на конфиг, кнопки «в карточку» с 07.09 нет вовсе (нет адреса — нет кнопки).
  ⚠️ Сверять демо по sha256 **после снятия CR** (в репозитории LF, в копии CRLF).
- 🔴 **Боевой:** `jetronsport.ru/constructor/` (WordPress/WooCommerce). Канал — **WP File Manager
  (elFinder), НЕ git**, нужен залогиненный Chrome с расширением Claude; порядок в ДЕПЛОЕ. Опаснее
  всего: **битый PHP кладёт весь сайт**, товар 3446 не удалять, меню ДВА (ID 3 и ID 55).
  ⚠️ `deploy/` едет на боевой ТОЛЬКО руками.
- **Токены 22.09:** модули `?v=20260922a`, `stand.css` `?v=20260920c` (CSS 22.09 не правился) (токен свой, но 20.09
  совпал с модулями — CSS правился вместе с ними). ⚠️ `mock-config.json` идёт с `cache: 'no-store'` — **токен ему не бампать**. ⚠️ Рамка
  конструктора на странице 3443 несёт свой токен, подтягивать вместе с модулями.
- 🔴 **Новому файлу нужен `cmd=mkfile` ДО `put`.** ⚠️ `const` в `Runtime.evaluate` роняет второй
  вызов. ⚠️ **PHP по HTTP не отдаётся**, сверять только чтением через файловый менеджер (`cmd=get`),
  сразу после заливки PHP проверять три страницы на 200. ⚠️ **Каталог с боевого из терминала
  не снять** — `admin-ajax.php` уводит на капчу `1R AntiBot`.
- 🔴 **`/constructor/` идёт БЕЗ `cache-control`, только с ETag** (а `.js`/`.css` с `max-age=604800`) —
  браузер держит HTML днями, отсюда класс «старый HTML + свежий JS»: 06.09 покупатель видел
  конструктор **без единого свотча** при верных файлах и пустой консоли. `core/ModelHost.js` лечит
  класс; 🟡 обсудить с владельцем `no-cache` для HTML. ⚠️ **Мерить боевой ТОЛЬКО со сброшенным кэшем.**

## 🔴 Состояние

🟢 **19.09 ВЫЛОЖЕНО И ПРОВЕРЕНО НА ЖИВОМ: дефект шрифта по умолчанию закрыт.** Покупатель,
не открывавший палитру «Шрифт», получал макет системным sans, тогда как спецификация писала
«шрифт: РПЛ». Причина — ленивая загрузка 15.09 (`c0c83e2`): стартовый список собирался из одного
`optCache`, где `fontId` появляется ТОЛЬКО после клика по палитре, а отрисовка подставляет дефолт
неявно (`c.fontId || this.defaultFontId()`), поэтому FontFace `rpl` не регистрировался вовсе.
Клиент поймал 18.09 голосовым на живом заказе («САМОТАЕВ», Space Белый): «надо нажать на RPL,
и тогда он применится». Правка: дефолт всегда в `_шрифтыЧерновика()`, новый `_шрифтыМакета()`,
ожидание шрифтов в `_composeMockupCanvas()` перед сведением PNG (через него идут оба потребителя
макета — корзина и «Скачать макет»). Ветка `fix/default-font-lazy-2026-09-19`, коммиты `5cf5d96`
и `fee8771`. ⚫ Влито в `main` 20.09.
**Замер боевого до и после**, headless с чистым кэшем, палитру НЕ трогали:
`rpl` в `document.fonts` не было → есть; «САМОТАЕВ» кеглем 95: 524 px (запасной sans 528) → **398 px**;
путь покупателя через рамку 3443 проверен целиком, ошибок консоли 0.
**Выложено 6 позиций:** `index.html`, `main.browser.js`, `app.browser.js`, `canvas.browser.js`,
`zone-editor.browser.js` (все сошлись по sha256) и токен рамки на странице 3443
(`/constructor/?v=20260913a` → `?v=20260919a`, REST по регламенту).
Токен модулей **`v=20260915c` → `v=20260919a`**; `stand.css` не трогали, у него свой `20260913b`.
⚠️ Бампать пришлось ПЯТЬ файлов: `app.browser.js` импортируется из `main.browser.js` со своим `?v=`,
а модули отдаются с `max-age=604800` — без этого выкладка прошла бы вхолостую.
🔴 **Заказы с 15.09 по 19.09 могли уйти в печать чужим шрифтом — сверить до печати.**
⚠️ **`document.fonts.check('95px rpl')` возвращает `true`, даже когда шрифта НЕТ** — как сторож
не годится, проверять составом `document.fonts` или шириной строки. Потому дефект и жил незамеченным.

🟢 **20.09 ВЫЛОЖЕНО И ПРОВЕРЕНО НА ЖИВОМ: вход в настройки надписи — две кнопки.**
Клиент голосовым 19.09 15:14: покупательница не нашла, где править шрифт (серая ссылка «Шрифт
и цвет» не читалась как настройка); голосовым 15:30 выбрал вариант **две кнопки** — «давайте две
кнопки, чтобы у человека глаза не разбегались». 💰 **6 000 ₽ согласовано, итого по форме 31 000 ₽;
🔴 счёт НЕ выставлен** (сумму предоплаты мы не знаем, остаток Андрей вписывает сам).
Сделано: кнопки «Шрифт · <имя>» и «Цвет · кружок» в одну строку, по виду как поля фамилии и номера,
текущее значение видно ДО нажатия. ⛔ Механика не тронута: список выезжает вниз ВНУТРИ карточки
и сдвигает блоки под собой, всплывающего окна поверх макета нет (на телефоне закрыло бы форму —
клиент переспрашивал дважды). Открыта всегда ОДНА панель; состояние живёт в памяти (`_fcOpen`),
потому что `renderAll({keepCards:true})` карточки не пересобирает — имя шрифта и кружок цвета
на кнопках обновляются вручную.
☠️ **Крючок ленивой загрузки шрифтов переехал с `details.opt-font` на кнопку `.opt-fc-btn`** —
потерять его значит вернуть дефект 15.09, когда в заказ уезжал макет чужим шрифтом. Стерегут
`tests/BootFonts.test.js` (разметка входа + гашение панелей). ⚠️ **`display: flex` перебивает
атрибут `hidden`**: без `.font-list[hidden], .swatches[hidden] { display: none }` панели раскрыты
всегда и карточка растёт вдвое.
**Замер боевого, кэш выключен, пять профилей:** высота карточки **190 → 195 px** на 1440 (клиенту
обещали 211 — вышло лучше), на телефоне 375 с тачем 220; кнопки 165×44 на ПК и 163×46 на телефоне,
всегда в один ряд, длинное имя шрифта обрезается многоточием. Шрифты при раскрытии 3 → 25, `rpl`
есть, 23 образца, активный РПЛ; «Цвет» гасит список и даёт 7 свотчей; путь покупателя через рамку
3443 пройден целиком, ресурсов с ошибкой 0. Снимки — `docs/screens-2026-09-20/`.
**Выложено 7 позиций:** `index.html`, `stand.css`, `main.browser.js`, `app.browser.js`,
`canvas.browser.js`, `zone-editor.browser.js` (sha256 сошлись 6 из 6 с локальным и с raw ветки)
и токен рамки 3443 (`?v=20260919a` → `?v=20260920a`, REST). Ветка
`feat/knopki-shrift-cvet-2026-09-20`, коммит `d3f90be`. ⚫ **20.09 влито в `main` (`ad19905`).**
🔴 **Руки Андрея:** отправить клиенту снимки `docs/screens-2026-09-20/` с сообщением, что сделано
по его выбору; выставить счёт.
🟡 **Второй вопрос клиента из того же голосового — про цвета — отвечен замером его админки:**
палитра цвета шрифта правится в **Настройки → «Jetron цвета»** (`options-general.php?page=jetron-colors`,
заголовок «Палитра цвета текста конструктора»). Формат — строка на цвет `id|Название|#hex`
(`gold|Золотой|#d4af37`), применяется сразу; сейчас 7 цветов, админка и боевой `colors.json` сходятся.
⚠️ Письмо про починку шрифта (в переписке, не отправлено) устарело в части «заказ уже ушёл
в работу» — клиент сказал, что с покупательницей договорился сам.

🟢 **20.09 ВТОРАЯ ВЫКЛАДКА: у поля текста появилась подпись «Добавить текст».**
Клиент тем же днём: «а где поле добавить текст, как-то его выделить, а то его по факту нет».
☠️ **Это про ФОРМУ, а не про футболки** — сначала отнёс жалобу к футболкам по приложенным
снимкам и ошибся: просьба про футболки пришла ПОЗЖЕ. Речь о карточках `text_or_upload`
(«Логотип на груди большой» и «Логотип на спине под номером»): у кнопки «Загрузить логотип»
подпись есть, а над полем текста не стояло НИЧЕГО — безымянная плашка, и покупатель не понимал,
что туда можно писать. Теперь подпись стоит теми же словами, которыми поле называет сам клиент.
⛔ **Белый фон полю НЕ давать** (пробовал и откатил): карточка ВЫКЛЮЧЕННОЙ опции тоже белая,
поле в ней сливается; прежний кремовый различим и на белой, и на зелёной карточке. Сторож
в `tests/BootFonts.test.js` держит и подпись, и запрет белого фона.
☠️ **Каскад, две ловушки подряд:** `.opt-in-text { background: #fff }` молча не применялся —
общий `.panel input[type="text"]` имеет специфичность (0,2,1) против (0,1,0). Специфичность,
а не порядок. (Та же семья, что и промах в футболках, где медиа-правило стояло выше по файлу.)
**Выложено 6 файлов, sha256 сошлись 6 из 6**, токен модулей **`20260920a` → `20260920b`**,
рамка 3443 подтянута (вхождение одно, длина 1714 не изменилась). Замер стенда на четырёх
профилях: подпись на месте, переполнения нет, высота карточки 309-354 px. Тесты **231/231**.
Снимок боевого — `docs/screens-2026-09-20/forma-pole-teksta-POSLE.png`, до правки —
`-DO.png`. 🔴 Клиенту не написано, что готово.

🟢 **20.09 ТРЕТЬЯ ВЫКЛАДКА: вход в настройки — сегмент «Шрифт | Цвет», подпись ушла в поле.**
Клиент голосовым 14:42 (говорил про ФУТБОЛКИ, но правило общее, применено в обоих конструкторах):
«убрать надпись добавить текст, она не нужна, просто мы внутри в этом поле напишем добавить текст,
и люди и так поймут»; «вместо грудь и спина… кнопку шрифт в белом фоне залить, а цвет сделать
залипшим». Сделано: две кнопки-поля заменены сегментом `.seg` + `.seg-btn.active` — тем же, что
«Детская | Взрослая»; выбранная залита белым, под подписью стоит значение (имя шрифта, кружок
цвета). Отдельная подпись над полем убрана, слова стали подсказкой поля.
⚠️ **Подсказка «Напишите название клуба или спонсора» снята сознательно:** она объясняла
содержание, а человек не понимал самого факта, что здесь можно писать.
**Выложено 6 файлов, sha256 сошлись 6 из 6**, токен **`20260920b` → `20260920c`**, рамка 3443
подтянута (вхождение одно, длина 1714). Тесты **231/231**. Замер живого 390 с тачем: сегмент
внутри карточки, кнопки 160×54, залипание одно на двоих, список шрифтов и палитра гасятся.
Снимок — `docs/screens-2026-09-20/boevoj-forma-segment.png`.
🔴 Клиенту не написано, что готово; письмо `docs/сообщение-клиенту-25.md` УСТАРЕЛО в части
описания кнопок — переписать перед отправкой.

🟢 **22.09 ЧЕТЫРЕ ЗАМЕЧАНИЯ КЛИЕНТА (три голосовых + текст), все закрыты и выложены.**

**1. ☠️ Сервер списывал МЕНЬШЕ показанного.** Конструктор показывал форму 1480 ₽ / итого 2680,
а в корзину уходило 1090 / 2290 («прайс конфига, позиция не найдена»). Причина: у браузера
(`CatalogPrices.js`) есть две механики, которых не было на сервере — таблицы замен написания
(линейка **Legend заведена в WooCommerce как «Легенда»**, у трёх расцветок цвет в товаре иной:
синий→Голубой, салатовый→Зелёный, лаймовый→Жёлтый) и **запасная цена ПО ЛИНЕЙКЕ**. Сервер
сравнивал строго и падал сразу на общий прайс. Обе перенесены в `jetron-orders.php`
(`jetron_orders_line_aliases`, `jetron_orders_color_aliases`, `jetron_orders_line_price`),
порядок источников теперь общий: карточка расцветки → цена линейки → прайс конфига, и источник
пишется в разбор. ⚠️ **Таблицы замен ОБЯЗАНЫ совпадать с `CatalogPrices.js`** — разъедутся,
дефект вернётся. Проверено на выгрузке живого каталога (90 позиций) и НА БОЕВОМ временным
файлом (только чтение): заказ клиента 1480/2680 «карточка товара», несуществующая расцветка
1480 «цена линейки».

**2. Дублирующие меты убраны** (голосовое 11:08: «полностью копирует верхнюю часть»):
«Расчёт конструктора», «Расчёт сервера», «Проверено сервером» больше не пишутся И прячутся
при показе у прежних заказов (`woocommerce_order_item_get_formatted_meta_data`). Из базы ничего
не удалено, ⚠️ **оба числа по-прежнему идут в журнал `jetron_orders_log`** — расхождения ловим там.
⚠️ «Логотипов: N» — мета ТЕМЫ, не наша.

**3. Макет показывается картинкой.** Файл был цел (200, image/jpeg, 130 КБ) — в заказе печаталась
только ссылка. Фильтр `woocommerce_order_item_display_meta_value` рисует картинку со ссылкой
(работает и в письме покупателю), `woocommerce_admin_order_item_thumbnail` заменяет пустую
миниатюру позиции. Новые заказы пишут мету «Макет» без «(PNG)»: браузер отдаёт и jpeg.
Замер заказа 5762: было 6 строк меты, стало 3, картинок макета 2.

**4. ☠️ Логотипы уезжали в webp — виноват НАШ код, а не конвертер сайта.** Клиент просил
«обойти сайт», но пережимает `prepareImage()`: всё тяжелее `compressOverMB` (1,5 МБ) вписывается
в 1600 px и кодируется в webp. Теперь исходник едет в заказ, пережатая копия остаётся холсту.
⚠️ **Оригиналы лежат в `this.logoOriginals`, ОТДЕЛЬНО от `optCache`:** тот целиком уходит
в черновик (`snapshotOf`), а у черновика лимит 3 МБ. Плата: после перезагрузки страницы
черновик вернёт пережатую копию, оригинал живёт до конца вкладки.
**Лимиты боевого замерены:** `post_max_size` и `upload_max_filesize` по **999 МБ**, память 1 ГБ,
`max_input_time` 1200 с — сервер не мешает. ⚠️ Но base64 раздувает: замер живого показал
исходник 13 МБ → **17,9 МБ в POST**; три таких логотипа это ~50 МБ на медленном мобильном канале.
Если начнутся жалобы на долгую отправку — вводить потолок на исходник.

**Выложено:** `jetron-orders.php` и `jetron-constructor.php` (mu-plugins, серверный линт
`token_get_all` ДО записи, sha сошлись, бэкапы в `scratchpad/php-backup/`), пять файлов фронта,
токен **`20260920c` → `20260922a`**, рамка 3443 подтянута. Тесты **231 → 232**.
Снимки заказа — `docs/screens-2026-09-22/`.
🔴 **Клиенту не отвечено.** В ответе обязательно сказать, что webp — наша причина, а не его
настройка сайта, и что «открыть файл в новой вкладке и скачать» качества НЕ теряет: скачивается
тот же файл, что лежит на сервере.

**Остальной разработки не осталось, всё выложено и проверено на живом.** Тесты **231/231** + python **42/42**.
🟢 **15.09 вечер, выложено и проверено сквозняком: шрифт в заказе + логотипы в заказ.** Спецификация
печатает ФАКТИЧЕСКИ нарисованный шрифт (через `resolveFont` — иначе подпись соврёт при латинице
с кириллицей); исходники уходят полем `jetron_logos[]`, плагин кладёт их в медиатеку и отдаёт ID
в `additional.logos`. ⚠️ **Приёмник НЕ НАШ и был написан до нас:**
`themes/jetronsport/inc/woocommerce/class-wc-cart-logo-jetronsport.php` сам пишет метки
«Логотипов»/`logos` и рисует ссылки в заказе — нам осталось только отдать ID. 🟢 **Дефект темы ВЫЛЕЧЕН
15.09:** `explode(',','')` возвращал `['']`, и «Логотипы:» с пустой ссылкой печаталось на КАЖДОМ заказе
без логотипов; теперь `array_filter(array_map('trim', ...))` + `if ($logos)`, строки 54 и 56. Линт
прогнан ДО заливки, бэкап `THEME-backup-20260915-215654.php`; замер после: заказы 5690/5682/5668/5667
дают 0 блоков «Логотипы». ⚠️ Это файл ТЕМЫ, в репозитории его нет — **обновление темы затрёт правку**,
после обновлений перепроверять. ⚠️ Случай «заказ С логотипами» не проверен: такого заказа ещё не было.
⚠️ Проверено
только на лёгком файле: несколько исходников по 2-5 МБ в одном POST могут упереться в `post_max_size`,
тогда логотипы грузить отдельным запросом. Замер на живом: «(шрифт: Oswald)» в спецификации,
«Нанести логотипы (кол-во): 1» в корзине, ошибок консоли 0.
Подробности каждой волны — в HISTORY. 15.09 скорость: шрифты ленивые, цены каталога в фон, монограмма
параллельно — страница 3 010 → **1 495 КБ**, холст на 1,6 Мбит/с 12 967 → **4 865 мс**, сторож
`tests/BootFonts.test.js`. 14.09 карточка «Футболки» на главной ведёт на конструктор футболок,
кликабельна ЦЕЛИКОМ (была мёртвая `#tshirts`; у «Постеров» та же мёртвая `#posters`, не тронута
намеренно — сторож `$jtn_side_live` пропускает только настоящие адреса). 13.09 зоны разнесены
на 45 форм, «Разнести от этой расцветки», «Внешний вид» и сортировка моделей в админке, порядок
карточек панели, приём `.webp`, «воздушный» фон, привязка шрифта логотипов к спине, меню, инструкция.
12.09 номер на шортах двигается РАМКОЙ, цифры не срезаются, полоса на телефоне убрана, защита
`zones.json`. ✅ 10.09 приёмочный прогон боевого — дефектов нет, кроме п. 7.

## ☠️ Запреты и грабли — самое ценное

### Боевой и данные клиента
- 🔴 **Не трогать `zones.json`, пока клиент с ним работает.** Сервер пишет файл ЦЕЛИКОМ и старый
  не читает (`jetron_zones_write`), мерж только на клиенте — наша запись затрёт его правки. Перед
  работой с зонами смотреть `Last-Modified` боевого файла. Защита — `zonesSaveGuard` и статус
  загрузки `ok / missing / failed`: одна битая запись отвергает файл целиком, и пустая база затирала
  разметку ВСЕХ форм.
- ⚠️ **Корневой `zones.json` — ПЕРЕОПРЕДЕЛЕНИЯ поверх `mock-config.json`** (`applyZoneOverrides`
  по `z.key`), а не источник зон: потеря файла роняет форму на `zoneTemplate`, а не обнуляет.
- 🔴 **Порядок моделей править в ОБОИХ файлах:** боевой `admin.json` (перекрывает конфиг,
  `out.forms = listOr(...)`) и `mock-config.json` (иначе «Вернуть исходный каталог» откатит).
  ⚠️ Боевой `admin.json` был **минифицирован в одну строку**, текстовая перестановка там не работает;
  пересборка `json.dumps(ensure_ascii=False, separators=(',',':'))` воспроизводит его байт-в-байт.
  ⚠️ Плагин переписывает `admin.json` ЦЕЛИКОМ и сохраняет его `JSON_PRETTY_PRINT` — это штатный
  формат, не раздувание.
- ⚠️ **Проверяя перестановку моделей на боевом, возвращать порядок обязательно** — прогон 13.09
  оставил боевой изменённым.
- ☠️ **`python скрипт | iconv … || python скрипт` запускает скрипт ДВАЖДЫ**, и второй прогон
  перезаписал бэкап уже изменённым файлом. Скрипты, пишущие бэкап, звать одной командой, без `||`.

### Тема сайта (`jetron-v2/hero.php`)
- ☠️ **Правка значения в теме НЕ действует.** `hero.php:68` перекрывает весь массив `$jtn_links`
  значениями из ACF: `$jtn_links[$key] = jtn_dp('link_'.$key, $url)`, а `jtn_dp()`
  (`jetron-v2/design-content.php`) берёт мету `jtn_dp_<имя>` **со страницы ID 2** и падает
  на запасное значение из темы, только если меты нет. Адрес живёт в КОНТЕНТЕ, а не в коде. Поле ACF
  `field_jtn_dp_link_tshirt` («Главный блок (новый дизайн)», ярлык «Футболки»). 📌 **Это и есть ответ
  клиенту на «где настраиваются эти параметры» — он правит сам.** Схема 123 полей —
  `jetron-v2/design-fields.json`.
- ⚠️ **Стиль класть инлайном в `hero.php`, а НЕ в `preview-current.css`:** тот 368 КБ вывода панели
  дизайна и переживёт нашу правку только до первой перегенерации.
- ⚠️ Накладка на карточку — `<a class="jtn2-side-hit">` прямым потомком `<article>`: обёрткой нельзя
  (стрелка сама `<a>`, вышла бы ссылка в ссылке), `::after` на стрелке не годится
  (`.jtn2-side-card>div` уже `position:relative`, накрыло бы только текст).
- ⚠️ **Стрелку не трогать:** её держат полтора десятка правил с `!important`.
- ⚠️ **`.jtn2-side` скрыт `display:none` ниже 1025 px** — карточек на телефоне и планшете нет вовсе,
  и так было до нас. На мобильном в конструктор ведут бургер и подвал.
- ⚠️ **Меню — не WP-меню** (`li.menu-item` = 0), вёрстка захардкожена в `<nav class="jtn2-nav">`.
  Действует правило 13.09: **шапка без конструкторов, бургер С конструкторами, подвал как был**;
  прежнее правило 31.08 «везде убрать, оставить только в футере» УСТАРЕЛО. Подвал — единственный вход
  покупателя. Ключи `$jtn_links` и кнопка-CTA `a.jtn2-calc` на главной оставлены намеренно. Тема
  прячет бургер: `@media (min-width:1025px){.jtn2-menu-btn,.jtn2-mnav{display:none!important}}`.
- Последний известный `hero.php` — **29 247 Б**, sha `6c7d1ccb13e9f902`; бэкапы боевого —
  `C:\tmp\hero-prod-20260914-065007.php` и `C:/tmp/jetron-backup-2026091{2,3}/hero.php*`.

### Админка WP и mu-плагин
- ⚠️ **`form.submit()` в админке WP падает** — `submit_button()` рисует `<input name="submit">`,
  и свойство формы указывает на КНОПКУ, а не на метод. Звать
  `HTMLFormElement.prototype.submit.call(форма)`.
- ⚠️ **Мерить админку ТОЛЬКО с явной вкладкой:** «Цвета кружков в фильтре» — `&tab=models`,
  «Внешний вид» — `&tab=view`, дефолтная вкладка `prices`.
- 🔴 **Список полей вида продублирован в PHP** (`jetron_admin_view_fields()`) — иначе никак: форму
  рисует PHP, а применяет браузер. Разъезд стережёт `tests/Appearance.test.js`.
- ⚠️ **Страховку «число моделей до перестановки» легко написать вхолостую:** сравнение после
  `array_splice` всегда сходится, счёт снимать ДО.

### Код конструктора
- ⚠️ **Рамки для шорт не существовало:** ключей `shorts_number`/`shorts_logo` нет в `placementOptions`,
  поэтому `renderZones` их не рисует (фильтр `usable`). Добавить туда НЕЛЬЗЯ — станут покупательскими
  опциями и заденут панель заказа и цену. Рамка создаётся ТОЛЬКО в `?zones=edit` (`addEditorFrame` +
  `EDITOR_FRAME_KEYS`), покупателю её нет.
- ⚠️ **Переставлять рамки на восьми донорах клиенту НЕ нужно** (поправка к прежнему предупреждению):
  `resolveFrameBox` и `resolveBrandBox` при сохранённой записи возвращают одно и то же; меняется
  только поведение в редакторе и дефолт для 37 неразмеченных форм.
- ⚠️ **Зоны переносить только ВНУТРИ линейки** — там кадр один (836 у Champion против 937 у New).
  Держит `tests/test_zonesets_sync.py`, он читает пиксели файлов. **Пересобрали мокапы — разносить
  заново:** 03.09 кадр Star уехал 954→878 и зоны разъехались на 18-60 px. ⚠️ **Пересборка мокапов
  ОБЯЗАНА тянуть пересчёт каталога**, связи в коде НЕТ, держит только тот же сторож. ⚠️ После
  расстановки зон картинки менять нельзя.
- ⚠️ **«Разнести по линейкам»** (`propagateZonesToLines`) заполняет только ПУСТЫЕ расцветки; красная
  **«Разнести от этой расцветки»** (`propagateFromDonor`) ЗАТИРАЕТ остальные расцветки линейки донора,
  с подтверждением, **отката нет**. Это разные сценарии, старую не трогать.
- 🔴 **Надпись «Логотип слишком тяжёлый…» убрана по решению владельца — не возвращать «по здравому
  смыслу».** Она говорила правду: черновик ограничен 3 МБ (`DRAFT_MAX_BYTES`), логотипы лежат
  в `optCache` исходными байтами (пережимать нельзя — прозрачный фон), и со ВТОРОГО тяжёлого логотипа
  они выбрасываются **молча**. Тест `DraftWiring.test.js` перевёрнут намеренно. Сообщение про
  неработающее хранилище осталось — там теряется весь набор.
- 🔴 **Предел, клиенту сказать:** латинский шрифт (`juventus` и ещё 15) + кириллица → откат на `rpl`
  (`resolveFont`, защита от пустых квадратов). Запрет латиницы при кириллице НЕ делался — ограничил
  бы выбор. ⚠️ Привязка шрифта логотипов к спине — **дефолт, а не жёстко** (`c.fontId ||`).
- ⚠️ **`applyColorHexes` внутри `loadCatalogPrices` красит кружки и рассчитан идти ДО
  `buildColorPicker()`** — раз цены грузятся позже, палитра перерисовывается в `цены.then(...)`.
- ⚠️ **Рамку 1px нельзя убирать через `border: none`, только красить прозрачным:** её ширину держит
  `content-box`, а размер карточки ставит JS. Вид настраивается токенами `--stage-bg`, `--card-bg`,
  `--card-border`, `--card-shadow`, `--card-shadow-hover`. ⚠️ Отступы на УЗКИХ экранах остаются
  из медиазапросов (строки 1162 и 1251 `stand.css`), настройка их намеренно не трогает.
- ⚠️ Порядок карточек панели задаёт ТОЛЬКО массив `placementOptions` в `mock-config.json`
  (`AdminOverrides.js` его не трогает); цены в админке лежат по ID, перестановка их не задевает.
- `UPLOAD_ACCEPT` = `image/*,.png,.jpg,.jpeg,.webp,.gif,.bmp,.svg`: расширения рядом с маской чинят
  мобильные выборщики, не понимающие групповую маску. `prepareImage()` на MIME не смотрит вообще.
- ⚠️ **Кадры линеек СМЕНИЛИСЬ: 992..1110 → 836..937** (`TOP_PX` 216→60), холст **439..501 px**;
  прежние числа (кадр 992, холст 465) — состояние ДО. ⚠️ `y_new = y_old × 1200 / H` верна только при
  смене кадра СНИЗУ, иначе формула из `docs/ПЕРЕДАЧА-2026-09-01-правки-15-28.md`.
  ⚠️ Решает **ПРОПОРЦИЯ**, абсолютные числа не значат ничего.
- ⚠️ **Полоса на телефоне лечилась ВНЕ репозитория:** конструктор растянут на 100vw правкой 27.07,
  а белый `.page-content` страницы 3443 остался с `padding: 25px`. ⚠️ Заголовок H1 обрезается там же —
  это тема сайта, в репозитории не лечится.
- ⚠️ Жёлтый на боевом `#ffde00`, а не наш `#ffd400`: оттенок ведёт каталог клиента («Цвет для иконки»),
  привязка так задумана — сравнивать экран с каталогом, а не с `admin.json`.
- ⚠️ **«Синий3» и рамки зон у покупателей исчезают ТОЛЬКО при выкладке:** «синий3» в `mock-config.json`
  не было никогда, она жила в корневом `zones.json` и в `admin.json` на боевом.
- 🔴 **Кнопка «в карточку» НЕ сломана:** её нет у ВОЗРАСТА, а не у расцветки — детский 45/45,
  взрослый 36/45; ответ клиенту `docs/сообщение-клиенту-20.md`.

### Замеры — как не получить ложь
- ⚠️ **Перед «у клиента сломано X» открыть конструктор глазами:** чтение конфига дважды дало ложь
  («31 расцветка без зон» — наследуют `zoneTemplate`; «22 файла одинаковые» — вид один). Состояние
  ПОСЛЕ загрузки бывает верным, а по дороге рамка гуляет.
- 🔴 **Мерить ВСЕГДА пять профилей** (1440 / 1045 / 768 / 375 с тачем / 360): симптом зеркальный —
  на ПК ширина, на телефоне высота. ⚠️ **Телефон только с тач-эмуляцией:** узкое окно без
  `hasTouch`/`isMobile` — НЕ телефон (пилюли 28,3 px вместо 36).
- ⚠️ **Тело карточки опции (а с ним палитра шрифтов и поле файла) есть в DOM только у РАСКРЫТОЙ
  опции:** `optionCardHtml` рисует `.opt-body` лишь при `openOpt === opt.id`. На свёрнутой панели
  `document.querySelectorAll('input[type=file]')` даёт 0 и читается как «поля нет».
- ⚠️ **`document.fonts.check('16px юникод')` НЕ годится как признак загрузки** — отдаёт `true` и для
  неизвестного семейства. Смотреть `app.fontsReady`.
- ⚠️ **Замер привязки врёт, если в профиле лежит ЧЕРНОВИК от прошлого теста** (`jetron:draft:v1`):
  у опции оказывается «свой» шрифт, и привязка выглядит несработавшей. `localStorage.removeItem`
  из CDP отдаёт `SecurityError`; сбрасывать `app.optCache` в памяти.
- ⚠️ `priceForAge` **не существует** — возрасты мерить только `ageOptionsNow()`, тем же методом, что
  рисует кнопки; попытка мерить несуществующим дала ложные «везде есть».
- ⚠️ **Опровергнуто замером, не переоткрывать:** «HTTP/1.1, 6 соединений» (сайт на **h2**) ·
  «зоны затёрлись после обновления» (`2f325f1` единственный) · «зоны champion-white уехали» (промах
  у Star). Ещё шесть тем и три ловушки замера — в хвосте `docs/ГРАБЛИ-инструментов.md`.
- ⚠️ **Долг по тестам: вёрстку тесты не стерегут вообще, покрыт 1 пункт раскладки из 7** — после
  правки CSS смотреть глазами, зелёный `npm test` тут не значит ничего.

## 📂 Где что лежит

- **Запуск:** `npx --yes serve .` или `python -m http.server 8778`, ⚠️ `?zones=edit` цеплять к КОРНЮ.
  🔴 Preview MCP и Claude in Chrome отвалились оба, мерим headless Chromium (код — в ГРАБЛЯХ).
- **Тесты:** `npm test` **220/220**, `python -m pytest` **42 passed**; гонять набор целиком, иначе
  провал сбора не виден. Рантайм-хуки `window.__jetronApp`, `window.__jetronEditor`.
  ⚠️ **Красное снимать только `npm test`:** репортёр `tdd-guard` подключён к npm-скрипту, прогон
  `node --test <файл>` guard НЕ видит и блокирует следующую правку чужим выводом. ⚠️ PHP вне обоих
  наборов — линт `Get-ChildItem deploy\*.php | %{ & 'C:\tools\php82\php.exe' -l $_.FullName }`.
- **Ключевые файлы:** `src/js/ui/` (`app.browser.js`, `canvas.browser.js`, `zone-editor.browser.js`),
  `src/js/core/` (`CanvasFit`, `ZoneManager`, `TextColor`, `CatalogPrices`, `SizeMatch`,
  `ZoneOverrides`, `Appearance`, `AdminOverrides`, `ModelHost`), `src/config/mock-config.json`,
  корневой `zones.json`, `src/css/stand.css`, `scripts/*.py` + `mockup-map.json`.
  Голосовые/снимки — `docs/voice-client-2026-*.txt`, `docs/screens-2026-09-*/`.
  Бэкапы боевого — `C:/tmp/jetron-backup-2026*/`.
- ⚫ **Снятое, НЕ переспрашивать** (HISTORY `-volna5-otvet.md`): пропорция (вариант A), New розовая,
  общая палитра, адреса карточек (⚠️ `_renderLineBadge()` живёт внутри `updatePrice()`), бирочные
  S и M (`73d5996`), шесть ответов 28.08, хвост каталога клиента, письма **№7 и №9-№12**, созвон,
  меню закрыто 06.09, крестик удалён волной 4 (сторож `tests/ExitButton.test.js`).
