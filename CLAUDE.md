# jetron-constructor — конструктор футбольной формы (jetronsport.ru)
Обновлено: 2026-08-31. **Прежняя версия (42 КБ) побайтово — `docs/HISTORY-jetron-constructor-2026-08-31.md`**;
ранее `-08-28.md`, `-08-26.md`. Ход работы по дням — `docs/ПЕРЕДАЧА-2026-08-30.md`, `-08-29.md`,
`-08-28.md`. Опровергнутые выводы и снятые вопросы сюда не возвращать: они живут в HISTORY.

🟢 **31.08: разработки в проекте не осталось.** Всё закоммичено (`59f5a2d`), влито в `main`
через PR #2 (`512e4ed`), демо пересобрано и проверено. `npm test` **83/0**,
`python -m pytest` **34 passed**.
🔴 **Дальше нельзя без двух ответов клиента** — `docs/сообщение-клиенту-12.md`.

Онлайн-конструктор футбольной ФОРМЫ (vanilla JS + Fabric.js v6): 8 линеек, зоны нанесения
(номер / фамилия / лого), редактор зон `?zones=edit` для админа. Заказ через profi.ru;
заказчик русскоязычный, он же WP-админ сайта.

⚠️ **НЕ ПУТАТЬ с `tshirt-constructor`** — конструктор ФУТБОЛОК, отдельный продукт того же
заказчика и отдельное репо. Принты по см / ivory / плотности / библиотека принтов — туда.
Кнопки «перёд/зад» есть ТОЛЬКО там; у формы один вид.

---

## 🧩 Контракт кадра и зон (главное, что изменилось 30-31.08)

**Высота кадра мокапа больше не константа, а считается по ЛИНЕЙКЕ:** `line_canvas` = самая
высокая форма линейки + `TOP_PX` 216 + `BOTTOM_PX` 40. Прежних `TOP_MARGIN` / `FILL_HEIGHT` нет.
Ширина как была 900, так и осталась. Замер с диска и с демо: Champion 992 · Legend 1006 ·
Фаворит 1025 · Winner 1027 · Волна 1031 · New 1093 · Space 1101 · Star 1110.
`scripts/run_normalize.py` переписан в ДВА прохода: кадр зависит от всех расцветок линейки,
поэтому bbox собираются до рендера. Прогон — 45 файлов, битых 0, нарушений 0.

⚫ **Почему понадобилось (диагноз снят замером 29.08, обратно не возвращать):** я говорил
клиенту, что окно жёсткое 900×1200 и картинку надо гнать под пропорцию окна — неправда.
`CanvasView.setBackground` (`canvas.browser.js:49-59`) вписывает по ШИРИНЕ и двигает высоту холста
под картинку (замер: фон 1100×1200 увёл высоту 800 → 654.5 при ширине 600). **Белую полосу
под шортами рисовали МЫ**, добивая каждый кадр белым до 1200.

**Зоны заданы долями 0..1 ОТ ХОЛСТА,** поэтому пересчитаны: `y_new = y_old × 1200 / H`,
`x` и `w` не трогаем. 1200 = `ЭТАЛОННАЯ_ВЫСОТА`, на ней калиброван `zoneTemplate`; сам шаблон
НЕ трогали — он и эталон, и запасной вариант.

⚠️ **Ключ, почему пересчёт ничего не сдвинул на экране:** `y_new × H = y_old × 1200` —
абсолютный пиксель сохраняется точно. Пиксели формы тоже не сдвинулись: рамка краски совпала
с блобами git у 45 из 45, ушла только белая добивка. Замер `champion-white`: `back_number`
0.229→0.277, `chest_number` 0.3166→0.383 — ровно ×1.2097, расстановка клиента цела; за холст
не вышла ни одна зона (максимум по линейкам 0.8757..0.9799). ⚠️ Наложение номера на шортах
у Star было и раньше — неточность общего шаблона, не регресс: абсолютные пиксели те же,
а зоны клиент расставляет сам.

**Три места, где это живёт** (`scripts/rewrite_catalog.py`, тесты `tests/test_rewrite_catalog.py`):
- `наборы_зон` + `расставить_наборы` — шаблон размножен в `zoneSets` по 8 линейкам, всем 45 формам
  проставлен `zoneSet = line`. ⚠️ Без второго шага движок молча падает на общий шаблон, весь
  пересчёт проходит мимо, а промах виден только глазами.
- `пересчитать_переопределения` — ручная расстановка клиента перенесена тем же множителем.
  ⚠️ Формы с НЕИЗВЕСТНОЙ высотой кадра возвращаются нетронутыми: у `champion-sinij3` картинка чужая
  (`konstruktor2.jpg`), множитель выбрасывает три зоны на шортах за нижний край (0.9265 → 1.147).
  Это и есть вопрос №2 клиенту.
- корневой **`zones.json`** — то, что `main.browser.js:46` тянет `fetch`-ем в
  `config.zoneOverrides` (под `validateOverrides`). ⚠️ **Переопределения живут ТАМ, а не в
  `mock-config.json`.** Сейчас два ключа: `champion-white`, `champion-sinij3`.

⚠️ **Резолв зон:** `form.zones || config.zoneSets[form.zoneSet] || config.zoneTemplate`, затем
`applyZoneOverrides(...)` (`app.browser.js:65-71`). `formZones` — **геттер, не метод**;
`zonesFor(view)` принимает имя ВИДА ('front'), а не formId.

🟢 Мокапы **WebP q92** (29 МБ → 3.3 МБ), расширение связано одной константой
`normalize_mockups.FORMAT` — конфиг и диск разъехаться не могут.
⚠️ **Подложку #EFEEEC как у эталона НЕ заливали, хотя обсуждалось:** исходники на белом, между
колонками белый фон, перекраска задела бы белые расцветки (Champion, Star, Legend).
Клиенту это не обещано.

---

## 💰 Деньги и статус

- **Основной этап** (сам конструктор): сдан и принят **02.08.2026**, оплачен.
- **Второй заказ** (нормализация мокапов + две новые линейки): **25 000 ₽, 5 рабочих дней.**
  Письмо `docs/otklik-klientu-2026-08-23.md` отправлено 26.08, предоплата получена, в работе.
  🔴 **Ниже 25 000 ₽ не опускаться.** Путь цифры: 40 000 ₽ (25-33 ч) − «разрез» (10 000 ₽)
  = 30 000 ₽ → Андрей сам снизил до 25 000 ₽, хотя ДОБАВИЛИСЬ Волна и Фаворит с нуля
  и перестановка зон по всем 45. Это уступка, а не расчёт по объёму.
- **Админ-панель конструктора формы** — сделана, установлена на боевой, оплачена.
- 🔴 **КП по админке конструктора ФУТБОЛОК не отправлено:** 18 000 ₽ отдельно / 30 000 ₽ за обе.

Закрыто на прошлом этапе, разбор — в HISTORY, заново не проверять: комментарии 24.07 и правки
27.07 · цена из карточки WooCommerce · админ-панель (проверена кликами 29.07) · размеры из
карточки линейки · касса и журнал `wp_jetron_orders` (подделка `jetron_total` не проходит) ·
аудит 31.07 · **дефект Winner `sizeGrid = null`** (сетка у 62 позиций из 62, коммит `73d5996`).

---

## 📋 Текущий заказ

Пять пунктов письма закрыты: 1-4 (нормализация, заливка, Волна и Фаворит с нуля, «убрать Рич»,
«Веном → Фаворит») плюс пересчёт зон. Расстановку зон клиент снял с нас 28.08 — расставит сам.
Осталась проверка на боевом, она упирается в два его ответа. Формулировки — HISTORY 28.08.

**Состав согласован 22.08: 8 линеек, 45 расцветок** (Чемпион 6, Легенда 6, New 5, Space 6,
Star 5, Winner 7, Волна 5, Фаворит 5; коралл не заводим, тёмно-бирюзовая = зелёная). Источник
правды по составу и по паре «исходник → цель» один — `scripts/mockup-map.json`; второй список
рядом неизбежно разъедется. Исходники клиента уже на диске, повторно не просить:
`C:/Users/Андрей/Downloads/`, ⚠️ копии с ` (1)`, ` (2)` отбрасывать по скобке. Чёрный Star позже.

⚠️ **Этап «разрез 45 → 90 файлов» в объём НЕ возвращать** — клиент опроверг его 23.08.

---

## 📂 Где что лежит

- **Git:** `https://github.com/Andrrreyiv/jetron-constructor`. `main` = **`512e4ed`** (merge PR #2),
  дерево `main` совпадает с деревом `fix/jetron-size-grid-acf` (`59f5a2d`) байт-в-байт.
  Рабочее дерево чистое, всё запушено.
- **Локальный запуск:** `npx --yes serve -l 8777 .` (`.claude/launch.json`, конфигурация `jetron`),
  открывать `http://localhost:8777/`. ⚠️ `serve` роняет query на `/index.html` — `?zones=edit`
  цеплять к корню.
- **Тесты:** `npm test` → **83/0**; `python -m pytest` → **34 passed** (гонять весь набор, иначе
  провал сбора не виден). Рантайм-хуки: `window.__jetronApp`, `window.__jetronEditor` (при `?zones=edit`).
- **Ключевые файлы:** `src/js/ui/` — `app.browser.js` (панель, рендер, плашка, `gridForAge`),
  `canvas.browser.js` (Fabric: placeText/placeImage, `_seatNumber`), `zone-editor.browser.js`;
  `src/js/core/` — `ZoneManager.js` (`fitInkToRect`), `TextColor.js`, `CatalogPrices.js`,
  `SizeMatch.js`; `src/config/mock-config.json`, корневой `zones.json`, `src/css/stand.css`;
  `scripts/` — `run_normalize.py`, `normalize_mockups.py`, `rewrite_catalog.py`, `mockup-map.json`.
- Расшифровки голосовых: `docs/voice-client-2026-08-29.txt`, `-08-28`, `-08-23`, `-08-18-и-22`,
  `-07-30-vecher`. Разбор размеров: `docs/РАЗМЕРЫ-КОНФЛИКТ-2026-07-30.md`.

### Два таргета деплоя
- **Демо:** GitHub Pages из `main` (workflow'ов нет) → https://andrrreyiv.github.io/jetron-constructor/
  Деплой = слияние ветки в `main` **кнопкой PR**, пересборка ~минуту. **Сверено 31.08 после PR #2:**
  токен `v=20260831a`, все 45 путей из живого конфига отдают 200 (битых 0), линеек 8 / расцветок 45,
  Rich и Venom отсутствуют, `zoneSets` 8 и `zoneSet` у всех 45 форм, `zones.json` отдаётся с обоими
  ключами, кадры на демо 992..1110 по линейкам.
  ⚠️ Каталога демо не видит: цены падают на конфиг, а кнопка «перейти в карточку» — на ссылку
  в РАЗДЕЛ каталога, то есть ровно на решение, которое клиент опроверг 28.08. Показывать демо
  только с этой оговоркой; она уже вписана в письмо №11.
  ⚠️ Кириллица в URL: проверять списком через `tr -d '\r'` и percent-кодировать, иначе curl
  ответит «Malformed input to a URL function» и покажет ложные битые ссылки.
- 🔴 **Боевой:** `https://jetronsport.ru/constructor/` (WordPress/WooCommerce). Канал — **WP File
  Manager (elFinder), НЕ git**, нужен залогиненный Chrome с расширением Claude.
  **Порядок, нонс, байт-сверка, ID страниц и меню, список mu-плагинов, касса, линт PHP —
  в `docs/ДЕПЛОЙ-боевой.md`. Открывать его ДО первой правки боевого, наизусть не делать.**
  Оттуда же самое опасное: битый PHP кладёт весь сайт, `cmd=put` не создаёт новых файлов,
  `encoding=scheme` не слать никогда, товар 3446 не удалять, меню ДВА (ID 3 и ID 55).

---

## ❓ Что спросить у клиента

🔴 **Два вопроса БЛОКИРУЮТ выкладку — `docs/сообщение-клиенту-12.md`** (слать вторым, после №11,
отдельным сообщением, без картинок). Оно намеренно НЕ содержит слова «готово»: смотреть пока
негде, и «готово» прозвучало бы приглашением сходить на сайт.

1. **Отступ у номера сверху.** Вариантов ровно два, выбирает он: ВНУТРИ поля (цифра опускается
   и на столько же уменьшается) или СДВИГОМ (размер прежний, но низ выходит за поле и может
   налезть на лого). 28.08 он просил поле не уменьшать, поэтому наугад не берём. Спросить заодно:
   на сколько и у какого номера — отступ на груди, спине и шортах сейчас ОБЩИЙ.
   ⚠️ `tests/ZoneManager.test.js:141-143` держит `NUMBER_TOP_INSET_RATIO < 0.05` — потолок
   пересматривать осознанно, когда придёт ответ. ⚠️ Там же строка 30 хардкодит `{900, 1200}`.
2. **Расцветка «синий3» в Champion.** Заведена им самим через админку на `konstruktor2.jpg`,
   в состав 22.08 не входит, исходника у нас нет; перенести расстановку нельзя (расчёт выше).
   Либо это проба и её убираем, либо он шлёт исходник и расставляет зоны заново. В том же письме
   предупреждение: новый каталог заменит всё, что он добавлял через админку руками.

🔴 **Третий вопрос — меню.** На боевом ДВА меню (ID 3 десктоп, ID 55 бургер), правка одного
не переносится на второе: убирать конструкторы только из бургера или из верхнего тоже?
Он был в письме №10, то есть в отменённом; задать отдельно.

🔴 **Он дважды попросил созвон** («могу ссылку на Яндекс Телемост кинуть», «рекомендую
созвониться… много будем туда-сюда гонять»). Ждёт ответа Андрея; в письме согласие написано.

🔴 **Актуальны только письма №11 и №12:** `docs/сообщение-клиенту-11.md` (1 383 знака, **без
картинок** — `docs/сравнение-кадра-29-08.png` иллюстрирует снятый диагноз), затем №12.
⚫ Письма **№7, №9 и №10 ОТМЕНЕНЫ**; `docs/вопрос-*.png` не нужны.

🟢 **Третий комментарий 30.08 закрыт кодом, клиенту молчим до выкладки:** цвет номера на груди
привязан к цвету текста на спине (`src/js/core/TextColor.js`, `setOptData` в `app.browser.js`).
Своей карточки цвета у грудного номера нет, поэтому смена цвета на спине обязана перекрашивать его
сразу, а не при следующем вводе номера.

**Снято, НЕ переспрашивать** (цитаты и разбор — в HISTORY):
- **Пропорция** — закрыта 29.08, вариант A: композит «перёд + зад», картинки заливаем как есть.
  На «уйдёт ли отступ» честный ответ **нет**: пустота снизу это геометрия (пара видов 1.05-1.34
  против холста 0.75, упирается ширина), зазор между видами уже медианно 5 px.
- **New розовая** — сделает сам · **общая палитра** — остаётся.
- **Адреса карточек расцветок** — не собираем: кнопка берёт адрес из того же ответа каталога,
  откуда цена и сетка (`'url' => get_permalink()` → `resolveFormProductUrl`). ⚠️ Ссылка зависит
  от возраста, поэтому `_renderLineBadge()` живёт внутри `updatePrice()` — вынести обратно значит
  оставить кнопку на карточке прежнего возраста.
- **Бирочные S и M** — `73d5996` переучил конструктор читать таблицу КОНКРЕТНОЙ модели из ACF
  на `pa_model`, сетка приходит у 62 из 62. ⚠️ Замер считал СТРОКИ, а не ячейки; всплывут
  прочерки — смотреть таблицу клиента на боевом, а не менять код.
- Отвечено 28.08: зоны расставляет он сам · кнопка ведёт в карточку расцветки, НЕ в каталог
  (`catalog.lineSlugs` не нужен, кнопка вправо наверх) · поле номера не уменьшать · карточки
  Волны и Фаворита есть давно · названия расцветок Волны верные · эталон логотипов пришлёт
  ПОСЛЕ заливки. Макет выбора — `C:/Users/Андрей/Новый.png`, реализуемо, это только интерфейс.

Хвост каталога клиента, не наш баг: нет карточек взрослого Champion, Legend и Фаворит,
у них цена и сетка падают на запасные из конфига.

---

## ⚠️ Живые предупреждения

- 🔴 **`admin.json` на боевом ЗАМЕЩАЕТ каталог целиком, а не дополняет** (`jetron-admin.php`:
  `out.forms = listOr(out.forms, admin.forms, isForm)`). Живой `admin.json` держит СТАРЫЕ
  43 расцветки, включая Rich, Venom и `champion-sinij3` на `konstruktor2.jpg`. **Залить новый
  `mock-config.json` и ничего не заметить — реальный сценарий: пока `admin.json` не пересобран,
  на сайте не изменится ничего.** Пересобирать после ответа про «синий3», не раньше.
- 🔴 **Корневой `zones.json` и мокапы выкладывать ТОЛЬКО вместе.** Доли пересчитаны под новые
  высоты (992..1110); положить его поверх старых мокапов (холст 1200) = сдвинуть всю расстановку
  клиента вниз на 21 %. Обратный порядок так же плох.
- 🔴 **Сливать в `main` через PR, кнопку жмёт человек.** `gh` без учётки (`Bad credentials`),
  GitHub закрыт доменной политикой Claude in Chrome. ⚠️ `git push origin <ветка>:main` проскакивает
  мимо регулярки `~/.claude/hooks/dangerous-actions-blocker.mjs` (ловит только буквальное
  `git push origin main`) — это дырка в шаблоне, а не разрешение. Рефспек — только по явной
  команде владельца и после проверки fast-forward.
- 🔴 **Сайт за 1R AntiBot.** `curl` на `/shop/`, `/wp-json/…`, `/product-sitemap.xml` редиректит
  на `/1r-captcha/`, `fetch` из браузера тоже ловит антибот. Каталог и конфиги снимать **только
  НАВИГАЦИЕЙ** в Claude in Chrome; статика в `/constructor/` по curl идёт нормально.
  ⚠️ Антибот режет REST **PUT и DELETE** (своя HTML со статусом 200, запись молча не происходит) —
  обновлять только POST-ом. И отдаёт 200 на что угодно: удаление проверять СПИСКОМ, а не статусом.
- 🔴 **Кеш iframe.** Без версии в src браузер отдаёт СТАРЫЙ index.html, хотя файлы на сервере новые.
  **Каждый деплой, меняющий index.html, требует буста `?v=`:** импорты ESM в index.html,
  main.browser.js, src iframe на 3443. Проверять страницей БЕЗ ручного `?cb=`, иначе проблема
  маскируется; диагностика — сравнить токен из `iframe.contentWindow.document` с серверным.
  ⚠️ Токен в **20 местах** (index.html 2, main.browser.js 5, app.browser.js 9, canvas 2,
  zone-editor 2), сейчас везде `v=20260831a` — бустить все разом, иначе часть модулей приедет старой.
- 🔴 **Цена — из карточки WooCommerce, не из браузера:** сервер пересчитывает её заново из
  спецификации, присланный `jetron_total` хранится только для сверки. ⚠️ Конструктор шлёт
  `grandTotal` за ВСЕ комплекты, а `set_price` в Woo — цена ЗА ШТУКУ: сервер считает один комплект,
  умножает WooCommerce. ⚠️ Слаг «футбольные футболки» в маппинг НЕ добавлять — сломает цены:
  та категория это верх отдельно, а конструктор продаёт КОМПЛЕКТ.
- ⚠️ **Перед утверждением «у клиента сломано X» открыть конструктор глазами.** Чтение конфига
  без взгляда на экран дважды дало ложь: «31 расцветка без зон» (наследуют `zoneTemplate`) и
  «22 файла одинаковые на переде и заде» (вид один, кадр композитный) — вторую поймал клиент.
- ⚠️ Остальные предупреждения по боевому (линт PHP, `cmd=put`, `encoding=scheme`, транзиент цен,
  ACF по имени подполя, классификатор прав, короткая сессия WP-админки) — в `docs/ДЕПЛОЙ-боевой.md`.

---

## 📊 Замеры и грабли инструментов — вынесены

**`docs/ГРАБЛИ-инструментов.md`** — переиспользуемые замеры (холст в конфиге и почему 1200 там
только эталон долей; композитный кадр и пропорции 0.48-0.62 / 1.05-1.34; размерные сетки ACF
по `pa_model` с term_id и подполями; бирочная шкала против российского размера) плюс грабли
инструментов (Edit/Write `Not logged in`, autocrlf и сверка с HEAD-блобами, кириллица в git и в
URL, `javascript_tool` и top-level await, вкладка в фоне, `app.views` это Map, Claude Preview MCP
на 127.0.0.1 и родительский `launch.json`, смена формы через `buildViews` и объект в `setOptData`,
расшифровка голосовых, tdd-guard).

Ничего из этого не переделывать заново: цифры замерены, грабли проверены на себе.
