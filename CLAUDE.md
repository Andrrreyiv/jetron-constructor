# jetron-constructor — конструктор футбольной формы

Статус-хендофф. Обновлено: 2026-07-23. Self-contained контекст для любой сессии/аккаунта.

## Что это
Онлайн-конструктор футбольной формы (vanilla JS + Fabric.js v6): 8 линеек (Champion, Legend, New, Rich, Space, Star, Venom, Winner), зоны нанесения (номер/фамилия/лого), редактор зон `?zones=edit`.

⚠️ НЕ ПУТАТЬ с `tshirt-constructor` (конструктор ФУТБОЛОК) — отдельный продукт того же заказчика, отдельное репо. Вопросы про принты по см/ivory/плотности — туда.

## Деплой (два таргета)
- Демо: https://andrrreyiv.github.io/jetron-constructor/ (GitHub Pages, деплой = push в main)
- Боевой: jetronsport.ru/constructor/ (WordPress/WooCommerce) — деплой через WP File Manager elFinder `cmd=put`. НИКОГДА не слать `encoding=scheme` (NUL-байт ломает JSON.parse). После деплоя байт-верификация против `git show HEAD:<path>` со снятием `\r`.
- ESM-кеш: у всех импортов версия `?v=` — при изменении модуля бустить её во ВСЕХ местах импорта.

## Состояние 23.07
- HEAD `4ab299a` на обоих origin/GitHub Pages; на боевом — `fa5871f` (child-цену 4ab299a на боевой НЕ заливали).
- Плашка линейки — ТЕКСТОВАЯ (`Champion →`, `_renderLineBadge` в `src/js/ui/app.browser.js`), по клику ведёт в каталог. Так и остаётся (решение 23.07), картинки-кнопки не будет.
- Цена изделия на боевом будет подтягиваться из карточки товара (заказчик, 23.07) → `prices.form` в `src/config/mock-config.json` (adult/child 1280/1280) — демо-заглушка.
- Тесты: `npm test`, 35/35.

## Открытые хвосты
1. Слаги категорий в `config.catalog.lineSlugs` — ПРЕДПОЛОЖЕНИЕ (`/product-category/champion/`...). Ждём реальные URL категорий от заказчика → обновить конфиг → перезалить оба таргета.
2. «Без категории» светится в каталоге WooCommerce боевого (товар конструктора без категории) — заказчику отправлена инструкция назначить категорию в админке.
3. Качество части мокапов (спины, оверсайз) — дорабатывает дизайнер заказчика.

## Грабли
- tdd-guard: stub-first (пустой export → красный тест → реализация); слеп к голому `node --test`.
- Windows autocrlf: рабочая копия CRLF — сравнивать деплой с HEAD-блобами, не с рабочим деревом.
