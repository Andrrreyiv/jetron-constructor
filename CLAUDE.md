# jetron-constructor — конструктор футбольной формы (jetronsport.ru)
Обновлено: 2026-09-03. 🟢 Ужимался 02.09 и 03.09 под потолок 25 600; копии ДО сжатия и всё снятое
— в `docs/HISTORY-jetron-constructor-*.md` (свежие `-2026-09-03` и `-2026-09-02-volna5-otvet`).
⚠️ Файл у потолка: дописывать только ВЗАМЕН, снятое и опровергнутое не возвращать.
📌 **Первым читать `docs/ПЕРЕДАЧА-2026-09-02-волна-5-ШАГ1-код.md`** (состояние кода и что доделать),
затем `-волна-5-рамки.md` (постановка и арифметика), `-волна-4.md`,
`-2026-09-01-правки-15-28.md`, `docs/ПЕРЕДАЧА-2026-08-{31*,30,29,28}.md`, `docs/КОММЕНТАРИИ-КЛИЕНТА-2026-08-31.md`.

---

## 🟢 ВОЛНА 5, ШАГ 1 — СДЕЛАН, ПРОМЕРЕН И НА ДЕМО. PR #11-#14 влиты, `origin/main` = `0c8d4f4`

🔴 **Первым — `docs/ПЕРЕДАЧА-2026-09-02-волна-5-ШАГ1-код.md`**: что правлено, числа «было/стало»,
три места, где легко наступить снова. Коротко: рамка ОДНА (ПК было 8 ширин карточки и 6 высот →
**1 и 1**; телефоны 8 высот → 1), Чемпион не тронут, зоны не трогаются.
🟢 **Зазор ряд → чернила просел от «одной рамки» (→ 11.7/14.5/14.6/17.6/12.6) и вылечен:**
`.views padding-top` 30→**33** у ≤560 плюс новое 5px у ≤900; база 13.1/16/17/19/17 превышена
всюду, текущие числа — в блоке Star ниже. ⚠️ Промежуточных **32 не хватало** (на 375 было 16.8
против 17), видно ТОЛЬКО замером. Код замера — хвост `ГРАБЛИ-инструментов.md`.
⚠️ Демо после мержа сверять по sha256, но после `tr -d '\r'`:
в репозитории LF, в рабочей копии CRLF, иначе ложное расхождение; по той же причине `git checkout`
меняет размер этого файла на ~230 байт.

## 🟢 03.09 закрыто и влито: проблеск карточки (PR #13) и композит `Star чёрный` (PR #14)

🔴 **Проблеск. Финальный снимок его НЕ ловит, только покадровый** (50 мс, наблюдатель ДО клика):
при смене расцветки карточка на 375 px уходила `326 → 468 → 326` и стояла неправильной 200-950 мс
на 14 расцветках из 15 — это и есть «рамка гуляет». Причина: размер ставился ПОСЛЕ
`await fabric.FabricImage.fromURL()`, а `468 = 351×(1200/900)` — дефолт холста Fabric. Правка:
`_applyCardSize()` в конце конструктора и в начале `setBackground` ДО `await`; сторож — тест
«размер карточки не зависит от картинки» на всех восьми кадрах. ⚠️ **Быстрый локальный сервер
проблеск СКРЫВАЕТ** — мерить с торможением (`page.route`, 1500 мс), приём в ГРАБЛЯХ.
🔴 **Star. Диагноз мерился, а не читался в коде:** `fitted_height` считает по РАМКЕ КРАСКИ, а не
по размеру файла. У четырёх нормальных Star она давала 775-778, у сырого `Star чёрный` (900×900,
гетры и бутсы) — **854**, и +76 px доставалось ВСЕМ пяти расцветкам («двойной отступ снизу»).
Новое фото 1200×900, bbox (86,44,1066,862) → 751. Кадр линейки **954 → 878**, самой высокой стала
New 937. Правка — одна строка в `scripts/mockup-map.json`; прогон `run_normalize.py`: 45 файлов,
битых 0, изменились РОВНО пять Star (остальные 40 байт-в-байт = детерминированность).
🟢 **После мержа:** демо — пять Star байт-в-байт по sha256, 900×878. Зазор шести профилей
**14.9/17.5/18.6/20.6/18.6** при базе 13.1/16/17/19/17, карточка 1 ширина и 1 высота на КАЖДОМ,
перелив на 1366 = 0. ⚠️ Токен буста (`20260902b`) НЕ трогали: висит на JS/CSS, мокапы идут
без `?v=`, их обновляет ETag.
⚠️ `gh` и MCP = Bad credentials (401 на ОБОИХ токенах), PR открывает владелец кнопкой.

🔴 **ХВОСТ ТОЙ ЖЕ ПРАВКИ, найден и вылечен 03.09 — ветка `fix/zonesets-star-878`, ЖДЁТ PR.**
Кадр 954→878 переписал КАРТИНКИ (`run_normalize.py`), а доли зон считает ДРУГОЙ скрипт
(`rewrite_catalog.py`), и его не прогнали: все 10 зон Star делились на прежние 954 и уезжали
ВВЕРХ на 18.6..60.1 px (фамилия 18.6, номер на груди 31.1) на всех ПЯТИ расцветках. Семь линеек
чистые. Сторож `tests/test_zonesets_sync.py`, `pytest` **42**. ⚠️ **Пересборка мокапов ОБЯЗАНА тянуть пересчёт каталога — связи в коде НЕТ**,
её держит только этот тест.

🔴 **Осталось по волне 5: отправить №16 и выбрать в нём концовку со СРОКОМ** — он уже сказал
«придётся мне это всё подгонять». Ниже — постановка, по которой письмо писалось.

## 🔴 ВОЛНА 5 — постановка (целиком в HISTORY `-volna5-otvet.md`)

🟢 План принят клиентом 02.09 («Да, Андрей, всё правильно вы говорите»), письмо про рамки отправлено.
🔴 **Два числа и вся выкладка — в шапке `CanvasFit.js`:** буквальные 900×1200 (его рецепт) дают
349 px на ПК (−30 % к Чемпиону) и 517 на телефоне; принятое 836/900 — 500 и 360, ровно нынешний
Чемпион, который ему «идеальные размеры». Решает ПРОПОРЦИЯ, абсолютные цифры не значат ничего.
⚠️ **Сигнал: «долго и сложно, наверное придётся мне это всё подгонять»** — он забирает шаг 2 себе.
Ответ нужен со СРОКОМ, а не с новым разбором. Единственное его условие к шагу 1 — «верхние кнопки
слишком близко не приближались» (тот самый зазор).
🔴 **Мерить ВСЕГДА пять профилей:** симптом был ЗЕРКАЛЬНЫЙ (на ПК гуляла ширина, на телефоне
высота), и померив только 1366×768 мы написали клиенту ложь про его экран.

## 🟢 ВОЛНЫ 1-4 закрыты и на демо, разработки не осталось (разборы — ПЕРЕДАЧИ волны 4 и 01.09)

🟢 **PR #4-#9 влиты владельцем**; ⚫ **PR #10 закрыть, он лишний:** коммит `0bab575` вошёл в #11.
Волна 4: **крестик удалён** из JS и CSS (запереть покупателя нельзя: конструктор — iframe на
странице WP 3443, шапка всегда снаружи; сторож `tests/ExitButton.test.js`) · `nowrap`
у `.line-badge-pill` · `.views{padding-top:30px}` при `max-width: 560px`.
🟢 Клиент: «в остальном всё хорошо, всё прекрасно» — волны 1-3 приняты, сторож `PickerRedraw.test.js`.
- 🔴 **Боевой `jetronsport.ru/constructor/` СТАРЫЙ**, мерж его не обновляет: 02.09 sha256 разошлись
  у `stand.css`, `app.browser.js`, `zones.json` — нужна ручная выкладка (п. 5).
- ⚠️ Три ловушки замера в ГРАБЛЯХ: зазор мерить до края РЯДА · прокрутку на настоящем скроллере ·
  узкий viewport без `hasTouch`/`isMobile` — НЕ телефон (пилюли 28.3 px вместо 36).

### Осталось человеческих рук

🔴 **Отправить письмо №16** (`docs/сообщение-клиенту-16.md`, `81bed43`) — закрывает волны 2-5
разом: рамка, Чемпион, проблеск, зазор, Star 45/45 плюс все 14 пунктов №15 без потерь.
🔴 **Внутри одно решение Андрея — две концовки, оставить ОДНУ:** **А** (шаг 2 берём мы, вписать
СРОК вместо `___`) или **Б** (шаг 2 за клиентом, он сам сказал «придётся мне это всё подгонять»).
⚫ **№15 и №14 НЕ слать:** №15 замещено целиком (его концовка «осталось два пункта, и оба за
вами» уже неверна), №14 обещает крестик «уменьшен и сдвинут наверх», которого волна 4 лишила.
⚠️ Живо для ЛЮБОГО будущего письма: п. 12 честен про сдвиг картинки на 18 px; про уменьшение
свотчей на 20 % не писать (36 px ниже минимума 44 px — осознанная уступка его просьбе); три
оговорки про витрину из №13 не повторять — он их читал. 🟢 №13 отправлено 01.09 (`62d375e`).
⚠️ **Причины и числа в письмо переносить только после сверки с кодом:** черновик №16 уверял, что
Star 954 «тянул белое поле для всех», хотя рамку держит САМЫЙ НИЗКИЙ кадр (Чемпион 836), а Star
ужимало ДО 87.6 % её ширины. Три таких места, разбор — в `81bed43`.

---

## 🎯 Что осталось, кроме письма

| # | Что | На ком | Блокер |
|---|---|---|---|
| 1 | ✅ **`Star чёрный`** — 03.09 клиент прислал композит, картинки закрыты 45 из 45 | закрыто | кадр Star **954 → 878**, самой высокой линейкой стала New 937 |
| 2 | ✅ **ВОЛНА 5, шаг 1: одна пропорция рамки у всех восьми линеек** — сделано, промерено, НА ДЕМО (PR #11-#14); ⚫ доли зон волна 5 не трогала. Проблеск на смене расцветки вылечен и влит. ⚠️ Шаг 2 клиент делает сам | закрыто | `docs/ПЕРЕДАЧА-2026-09-02-волна-5-ШАГ1-код.md` |
| 3 | Восемь карточек-доноров с расстановкой зон | **клиент** | 🟢 п. 1 и 2 закрыты — расставлять МОЖНО, кадры больше не поедут |
| 4 | Размножение зон донор → вся линейка | мы, **код не написан** | ждёт п. 3; между линейками нельзя, кадры разные. 🟢 Предпосылка «внутри линейки кадр один» проверена замером 03.09 и закреплена тестом |
| 5 | Выкладка на боевой + чистка ОБОИХ меню (ID 3 и ID 55) | мы, по команде | строго по `docs/ДЕПЛОЙ-боевой.md`; «синий3» и рамки зон у покупателей исчезнут только здесь |
| 6 | КП по админке конструктора ФУТБОЛОК (18 000 / 30 000 ₽) | **Андрей** | не отправлено |

🟢 **Порядок 1 → 2 → 3 отработан, ворота открыты.** Он был жёстким потому, что правка
`Star чёрный` меняет высоту кадра линейки, от неё считается добивка ширины (п. 2), а зоны
заданы **долями от кадра**: расставь клиент зоны раньше — переносить все восемь карточек руками.
Оба шага сделаны ДО расстановки, цену нарушения не заплатили. ⚠️ Правило живо: новый исходник
мокапа снова двинет кадр, после расстановки зон менять картинки нельзя.

---

## ⚠️ Не переоткрывать — вынесено в хвост `docs/ГРАБЛИ-инструментов.md`

Шесть тем, каждая проверена замером, заново не расследовать: пункт 9 (ряд плашки на вороте) · ряд
плашки шире картинки на 13 px с каждой стороны на ПК — НЕ баг · «медиазапрос не повышает
специфичность» · «зоны champion-white уехали вверх» — не подтверждено ни 02.09, ни сверкой 03.09
(промах был у Star, Champion чист) · отступ номера 1 px ·
осознанно не делали (прокрутка панели настроек, бахрома у 3 шрифтов из 23).
⚠️ **Долг по тестам: вёрстку тесты не стерегут вообще, покрыт 1 пункт раскладки из 7** — после
правки CSS смотреть глазами, зелёный `npm test` не значит тут ничего.

Продукт: конструктор футбольной ФОРМЫ (vanilla JS + Fabric.js v6), 8 линеек, зоны (номер / фамилия /
лого), редактор `?zones=edit`; заказ через profi.ru, заказчик он же WP-админ. ⚠️ **НЕ ПУТАТЬ
с `tshirt-constructor`** (ФУТБОЛКИ, тот же заказчик): принты по см, ivory, плотности, библиотека
принтов — туда; кнопки «перёд/зад» ТОЛЬКО там, у формы один вид.

---

## 🧩 Контракт кадра и зон — целиком в HISTORY `-volna5-otvet.md`

**Открывать ДО правки зон, мокапов или `run_normalize.py`:** высота кадра по ЛИНЕЙКЕ, два прохода
нормализации, пересчёт зон, три места хранения, резолв, WebP q92, снятый диагноз «окно 900×1200».
⚠️ **Кадры линеек СМЕНИЛИСЬ: 992..1110 → 836..937** (`TOP_PX` 216→60, `СДВИГ_ФОРМЫ` = 156), холст
вырос до **439..501 px**; прежние числа про кадр 992 и холст 465 — состояние ДО этого.
⚠️ **`y_new = y_old × 1200 / H` верна, только когда кадр меняется СНИЗУ** — после среза верхней
полосы 01.09 формула другая; она, `h_new` и `СДВИГ_ФОРМЫ` = 156 (`scripts/normalize_mockups.py`) —
в `docs/ПЕРЕДАЧА-2026-09-01-правки-15-28.md`.

---

## 💰 Деньги и статус

- **Основной этап** сдан и принят **02.08.2026**, оплачен; **админ-панель конструктора формы**
  установлена на боевой и оплачена.
- **Второй заказ** (нормализация мокапов + две новые линейки): **25 000 ₽, 5 рабочих дней**;
  отправлено 26.08, предоплата получена, в работе. 🔴 **Ниже 25 000 ₽ не опускаться:**
  40 000 ₽ (25-33 ч) − «разрез» (10 000 ₽) = 30 000 ₽ → Андрей сам снизил до 25 000 ₽, хотя
  ДОБАВИЛИСЬ Волна и Фаворит с нуля и перестановка зон по 45. Уступка, а не расчёт по объёму.
- 🔴 **КП по админке конструктора ФУТБОЛОК не отправлено:** 18 000 ₽ отдельно / 30 000 ₽ за обе.
- ⚫ Всё закрытое на прошлых этапах (комментарии 24.07 и правки 27.07, цена из карточки WooCommerce,
  админ-панель, размеры, касса, аудит 31.07, промах зон 31.08) — в HISTORY, заново не проверять.

## 📋 Текущий заказ

**Состав согласован 22.08: 8 линеек, 45 расцветок** (Чемпион 6, Легенда 6, New 5, Space 6, Star 5,
Winner 7, Волна 5, Фаворит 5; коралл не заводим, тёмно-бирюзовая = зелёная). Единственный источник
правды по составу и по паре «исходник → цель» — `scripts/mockup-map.json`, второй список рядом
разъедется. Исходники клиента на диске, повторно не просить: `C:/Users/Андрей/Downloads/`,
⚠️ копии с ` (1)`, ` (2)` отбрасывать по скобке.
⚠️ **Этап «разрез 45 → 90 файлов» в объём НЕ возвращать** — клиент опроверг его 23.08.
⚫ История пяти пунктов письма (все закрыты, зоны клиент снял с нас 28.08, осталась выкладка) —
в HISTORY 28.08 и `-volna5-otvet.md`.

---

## 📂 Где что лежит

- **Git:** `github.com/Andrrreyiv/jetron-constructor`, `origin/main` = **`0c8d4f4`** (PR #14, Star
  чёрный; до него #13 `8fb73f9` проблеск, #11 `7ad4aad` волна 5). 🔴 PR не открыты у ДВУХ веток:
  `fix/zonesets-star-878` (`06b78a0`, зоны Star) и `docs/posle-merzha-pr14` (письмо №16 и статус).
  ⚠️ Смотреть `origin/main`: локальный отстаёт после мержа. ⚠️ Токены `gh`/MCP мертвы (401),
  рабочий — в Credential Manager: `git push` идёт, API — через `git credential fill` + urllib.
- **Запуск:** `npx --yes serve .` или `python -m http.server 8778`, ⚠️ `?zones=edit` цеплять к КОРНЮ.
  🔴 Preview MCP и Claude in Chrome отвалились оба, мерим headless Chromium (код — в ГРАБЛЯХ).
- **Тесты** на 03.09: `npm test` **93/0**, `python -m pytest` **42 passed**; гонять набор целиком,
  иначе провал сбора не виден. Рантайм-хуки `window.__jetronApp`, `window.__jetronEditor`.
- **Ключевые файлы:** `src/js/ui/` — `app.browser.js` (панель, рендер, плашка, `gridForAge`),
  `canvas.browser.js` (Fabric: placeText/placeImage, `_seatNumber`, `_applyCardSize`),
  `zone-editor.browser.js`; `src/js/core/` — `CanvasFit.js`, `ZoneManager.js` (`fitInkToRect`),
  `TextColor.js`, `CatalogPrices.js`, `SizeMatch.js`; `src/config/mock-config.json`, корневой
  `zones.json`, `src/css/stand.css`; `scripts/` — `run_normalize.py`, `normalize_mockups.py`,
  `rewrite_catalog.py`, `mockup-map.json`. Голосовые `docs/voice-client-2026-*.txt` (свежая `-09-02`).

### Два таргета деплоя
- **Демо:** GitHub Pages из `main` (workflow'ов нет) → https://andrrreyiv.github.io/jetron-constructor/
  Деплой = слияние ветки в `main` **кнопкой PR**, ~минуту, замер сходится с локальным.
  ⚠️ Каталога демо не видит: цены падают на конфиг, кнопка «перейти в карточку» ведёт в РАЗДЕЛ
  каталога — решение, клиент опроверг 28.08. Показывать с этой оговоркой, она в **№13**.
- 🔴 **Боевой:** `https://jetronsport.ru/constructor/` (WordPress/WooCommerce). Канал — **WP File
  Manager (elFinder), НЕ git**, нужен залогиненный Chrome с расширением Claude. **Порядок —
  в `docs/ДЕПЛОЙ-боевой.md`**; опаснее всего: битый PHP кладёт весь сайт, товар 3446 не удалять,
  меню ДВА (ID 3 и ID 55).

---

## ❓ Клиент — осталось только живое

🔴 **При выкладке чистим ОБА меню, ID 3 (десктоп) и ID 55 (бургер)** — ответ 31.08 «везде убрать,
оставить только в футере», правка одного на второе не переносится. ⚠️ Клик «Сохранить меню» через JS
блокируется классификатором прав: кликать инструментом `computer` по координатам после скриншота.
⚠️ **«Синий3» и рамки зон у покупателей исчезнут ТОЛЬКО при выкладке:** «синий3» в `mock-config.json`
не было никогда, она жила в корневом `zones.json` и в `admin.json` на боевом.
🆕 **Размножение зон** (п. 3-4 таблицы, разбор — `docs/ПЕРЕДАЧА-2026-08-31.md`): блок корневого
`zones.json` с донора копируется на все формы той же `line`, без математики и **по команде** —
внутри линейки кадр один. Между линейками НЕЛЬЗЯ: кадры разные.
🟢 Палитра слева на ПК сделана; номер на груди ведомый по цвету и шрифту (`core/TextColor.js`).
Клиенту молчим до выкладки.
⚫ **Снятое, НЕ переспрашивать** — в HISTORY `-volna5-otvet.md`: пропорция (вариант A), New розовая,
общая палитра, адреса карточек (⚠️ `_renderLineBadge()` живёт внутри `updatePrice()`), бирочные
S и M (`73d5996`), шесть ответов 28.08, хвост каталога клиента, письма **№7 и №9-№12**, созвон.

## ⚠️ Живые предупреждения

- 🔴 **Сливать в `main` через PR, кнопку жмёт человек** (`gh` без учётки, GitHub закрыт доменной
  политикой Claude in Chrome). ⚠️ `git push origin <ветка>:main` проскакивает мимо
  `dangerous-actions-blocker.mjs` (ловит только буквальное `git push origin main`) — это дырка
  в шаблоне, а не разрешение.
- 🔴 **ВСЕ предупреждения по боевому — в `docs/ДЕПЛОЙ-боевой.md`, раздел «Живые предупреждения»;
  открывать ДО первой правки боевого**, дубля списка здесь нет — он разъезжался с оригиналом.
- ⚠️ **Перед утверждением «у клиента сломано X» открыть конструктор глазами.** Чтение конфига без
  взгляда на экран дважды дало ложь: «31 расцветка без зон» (наследуют `zoneTemplate`) и «22 файла
  одинаковые на переде и заде» (вид один, кадр композитный) — вторую поймал клиент. 🔴 То же про
  время: состояние ПОСЛЕ загрузки может быть верным, а по дороге рамка гуляет (03.09).
- ⚠️ Переиспользуемые замеры (эталон долей 1200, композитный кадр, сетки ACF по `pa_model`, бирочная
  шкала), грабли инструментов и headless-обход мёртвых Preview MCP / Claude in Chrome —
  в `docs/ГРАБЛИ-инструментов.md`. Не переделывать: цифры замерены, грабли проверены.
