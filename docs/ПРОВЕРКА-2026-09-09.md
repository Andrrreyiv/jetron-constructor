# Сквозная проверка боевого 09.09 — все последние комментарии клиента

Замер живого `https://jetronsport.ru/constructor/` после выкладки 09.09.
Условия: CDP, кэш выключен (`Network.setCacheDisabled`), `localStorage` очищен перед
прогоном, адрес без query (с query боевой ловит антибот). **Ошибок в консоли за все
прогоны — 0.** Скрипты замера: `C:\tmp\jetron_check_all_20260909.py`, `-all2-`, `-cart-`,
`-modal-`, `-plugin-`.

## Семь комментариев 07.09

| # | Что просил клиент | Замер на боевом |
|---|---|---|
| 1 | в корзине висит зачёркнутый текст | 🟢 спецификация чистая: ни «(цвет: …)», ни «Размерная категория», ни строки про лого Jetron. Живой текст: `Модель: Champion Белый / Размер: XS (140-150) / Комплектов: 2 / Нанесения: нет / Гетры: да / Расчёт конструктора: форма 1 090 ₽, гетры 450 ₽; за комплект 1 540 ₽; ИТОГО 3 080 ₽` |
| 2 | в корзине транслит вместо русского | 🟢 mu-плагин `wp-content/mu-plugins/jetron-constructor.php` на боевом **байт-в-байт равен репозиторию** (sha `3aa8c80c9bc97078`, 8 490 Б): подписи «Конфигурация» и «Макет» на месте, транслита (`Konfiguraciya`, `Maket`, `Raschet`) нет |
| 3 | в корзине пусто вместо картинки макета | 🟢 `mockupDataURL('image/jpeg')` отдаёт **33 КБ** JPEG; хук миниатюры `jetron_cart_item_thumbnail` в плагине на месте |
| 4 | «жёлтая форма показывается под зелёным» | 🟢 `legend-yellow` есть, `lime` нет нигде: 0 в палитре, 0 в формах. ⚠️ Оттенок на боевом **`#ffde00`**, а не наш `#ffd400` — его даёт каталог клиента через «Цвет для иконки», привязка так и задумана |
| 5 | «кнопка в карточку — страница не найдена» | 🟢 плашка из трёх пилюль, кнопка «в карточку →» на месте. Адреса карточек: **детских 45 из 45**, взрослых **36 из 45** — у 9 расцветок взрослого товара в каталоге нет, кнопка не рисуется по конструкции |
| 6 | «поменял цвет в админке — квадратик не поменялся» | 🟢 **14 свотчей из 14** совпали с палитрой каталога, чужих оттенков на экране нет |
| 7 | сохранять настройки между перезагрузками | 🟢 после перезагрузки вернулись форма `new-white`, комплектов 3, гетры включены |

## Пять требований из голосовых 09.09

| # | Что просил клиент | Замер на боевом |
|---|---|---|
| 8 | «по умолчанию у нас идут все детские» | 🟢 конструктор открывается на детской (`ageCategory = child`) |
| 9 | «зачем показывать взрослую, если её у нас в природе нет» | 🟢 на Чемпионе кнопка ровно одна, «Детская»; взрослой нет вовсе |
| 10 | «цены приравниваем к фактическим» | 🟢 белый Фаворит, взрослая = **1 680 ₽** (был запасной прайс 1 280 ₽). Цена берётся по линейке из каталога, второй таблицы цен нет |
| 11 | размер выбирать до сборки макета, таблица одна | 🟢 блок стоит выше конструктора, «Таблица размеров» рядом; в ней 7 строк, клик выбрал «XS (140-150)» и размер тут же встал в бар. Та же таблица внутри заказа: 7 строк, строки кликабельны (`cursor: pointer`) |
| 12 | «в этих кнопках уберём цены… покрупнее… детская влево» | 🟢 подписи без «₽», шрифт 16 px, высота 44, первая кнопка — `data-age="child"` |

## Что перемерять при следующих правках

- ⚠️ **Селекторы, на которых я споткнулся:** палитра слева — `.pcolor` (а `.color-sw` живёт
  в другом месте и на главном экране его нет); кнопка карточки — `button.line-badge-cta`,
  не ссылка; модалка заказа — `.order-overlay` / `.order-card`; `lastOrder` появляется
  ТОЛЬКО внутри `showOrder()`, снаружи он пуст, и «пустой текст корзины» читается как дефект,
  хотя это промах замера.
- ⚠️ Оттенки на боевом теперь ведёт каталог, поэтому наш hex в `admin.json` может законно
  расходиться с экраном. Сравнивать надо экран с палитрой каталога, а не с нашим файлом.
