# Сообщение клиенту №22 — выравнивание строки размера (09.09, вечер)

📌 Отвечает на голосовое клиента 09.09 в 14:18 (расшифровка — `docs/voice-client-2026-09-09.txt`,
третий блок): «оставим этот блок второй… комплектация отдельным блоком, нормально» и «таблица
размеров и размеры вот как бы влево и вправо прям выброшены, приклеены… вот по этой границе
и таблицу размеров, и справа то же самое… засунем их внутрь нашего единого концептуала».

## 🟢 УСЛОВИЕ ВЫПОЛНЕНО — письмо можно слать как есть

1. ✅ **PR #30 влит**, `origin/main` = `da8d6c0`.
2. ✅ **Боевой выложен: два файла, побайтовая сверка 2 из 2, провалов 0** — `index.html`
   и `src/css/stand.css`. Бэкап боевых версий ДО перезаписи — `C:/tmp/jetron-backup-20260909b/`.
3. ✅ **Бампнут `?v=` у `stand.css` до `20260909d`** — иначе вернувшийся покупатель неделю
   видел бы старую раскладку (`max-age=604800`).
4. ✅ **Замер на ЖИВОМ боевом:** «Таблица размеров» слева `861.5` при заголовке карточки
   `860.8`, «Размер …» справа `1198.5` = правая внутренняя граница карточки; на телефоне
   `21.5` при заголовке `20.8` и `353.7` справа. Все шесть файлов конструктора на боевом
   равны `origin/main` (6 из 6), поведение не поехало (Чемпион — одна кнопка «Детская»,
   белый Фаворит взрослая 1 680 ₽), **ошибок в консоли 0**. Снимки —
   `docs/screens-2026-09-09/boevoy-vyravnivanie-{desktop,mobile-375}.png`.

## 🔴 ПЕРЕД ОТПРАВКОЙ: вписать сумму

Клиент дословно: «я зоны расставляю и всё, денежку там скажете, сколько я переведу».
**У нас нигде не записано, сколько именно пришло предоплатой** — только «предоплата получена»
при общей цене 25 000 ₽. Сумму остатка Андрей подставляет сам в последний абзац,
я её не выдумываю. Если предоплата была 40 %, остаток 15 000 ₽, но это ПРЕДПОЛОЖЕНИЕ,
а не запись — сверить с поступлением.

## ⚠️ Что в письме намеренно не сказано

- Про причину сдвига (17,5 px = рамка 1,5 + внутренний отступ 16) — клиенту не нужно,
  ему нужен результат.
- Про то, что «Комплектация» осталась нетронутой: он сам это решил, лишний раз
  подтверждать его же слова словами «я ничего не делал» не стоит.

---

Дмитрий, добрый день! Сделал последний штрих, всё уже на сайте.

Строку с размером подтянул в общий ряд: «Таблица размеров» теперь стоит ровно под
«Фамилия и номер», а «Размер …» справа выровнен по краю карточек. Раньше и то и другое
липло к самому краю панели и выбивалось из сетки, теперь всё в одной рамке. Проверил на
компьютере и на телефоне.

«Комплектацию» оставил отдельным блоком, как вы сказали.

Обновлять страницу вручную не нужно, файлы получили новые адреса и подтянутся сами.

По зонам всё готово, можно расставлять. Условия те же: одна карточка в каждой линейке,
восемь штук, дальше я размножу на всю линейку сам. И после расстановки картинки форм
менять уже нельзя, новая картинка сдвинет кадр и зоны уедут.

По оплате: остаток ____ ₽, реквизиты прежние.
