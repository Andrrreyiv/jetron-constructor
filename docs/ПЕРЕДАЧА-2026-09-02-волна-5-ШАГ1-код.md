# ПЕРЕДАЧА 02.09.2026, вечер — волна 5, шаг 1: код написан, проверка не закончена

Читать вторым, после `CLAUDE.md` проекта. Постановка задачи, слова клиента и арифметика выбора
пропорции — в `docs/ПЕРЕДАЧА-2026-09-02-волна-5-рамки.md`, здесь только состояние кода.

## Статус одной строкой

Шаг 1 («белая рамка одна и намертво зафиксирована») **реализован, промерен и закрыт**: рамка одна
на всех шести профилях, найденный собственной проверкой регресс зазора вылечен и перемерен, оба
набора тестов зелёные (**92/0** и **41 passed**). Ничего не закоммичено: ветки нет, всё лежит
в рабочем дереве. Осталось только: ветка → PR → кнопку жмёт владелец.

## Что именно правлено (6 файлов)

| Файл | Правка |
|---|---|
| `src/js/core/CanvasFit.js` | новые `export const FRAME_ASPECT = 836 / 900` и `fitCanvasInCard(cardWidth, imgAspect)`; над ними — комментарий с дословной цитатой клиента, восемью высотами кадров и разбором, почему буквальные 900×1200 брать нельзя |
| `src/js/ui/canvas.browser.js` | в конструкторе `this.wrapEl = canvasEl.parentElement` и `this.displayWidth`; в `setBackground` вместо `capWidthByHeight(this.canvas.getWidth(), img.height/img.width, …)` — расчёт карточки по `FRAME_ASPECT` и холста через `fitCanvasInCard`, затем проставление `style.width/height` карточке |
| `src/css/stand.css` | `.canvas-wrap`: флекс-центрирование + `box-sizing: content-box`; мобильный `.canvas-wrap`: `background: var(--stage-card)` вместо `transparent`; `.views { padding-top }` — 30→32 в `@media (max-width:560px)` и новый `5px` в `@media (max-width:900px)` |
| `index.html`, `src/js/main.browser.js`, `src/js/ui/app.browser.js`, `src/js/ui/zone-editor.browser.js` | токен буста `20260901a` → `20260902a`, 21 место |
| `tests/CanvasFit.test.js` | +3 теста (рамка одна на восемь линеек; Champion заполняет её целиком, высокие плавают внутри; холст не вылезает за рамку, включая случай без пропорции) |
| `tests/test_frame_aspect.py` | НОВЫЙ, +2 теста |

### Три места, где легко наступить снова

1. **`this.wrapEl` берётся ДО `new fabric.Canvas`.** Fabric оборачивает `<canvas>` в свой
   `.canvas-container`, и после этого `canvasEl.parentElement` указывал бы уже на обёртку Fabric,
   а не на `.canvas-wrap` — стили ушли бы не туда.
2. **База ширины — `this.displayWidth`, а НЕ `this.canvas.getWidth()`.** `setBackground` вызывается
   повторно при каждой смене расцветки; если считать от текущей ширины холста, он усыхает шагами.
3. **`box-sizing: content-box` у `.canvas-wrap` обязателен.** Глобальный `* { box-sizing:
   border-box }` (stand.css L72) втянул бы `padding: 12px` и рамку 1 px внутрь ширины, которую
   ставит JS, — карточка вышла бы на 26 px уже холста.

Плюс мобильный фон: пока `.canvas-wrap` был `transparent`, в боковых белых полях светился
кремовый фон страницы (`--bg: #fbf1db`), и «одной белой рамки» на телефоне не получалось.

## Почему зоны не трогаются

Холст сохраняет СВОЮ пропорцию кадра, добивку до общей рамки даёт белая карточка вокруг него.
Проверено арифметикой прямо на замере: 465.4/501 = 0.9289 = 836/900 · 465.4/447 = 1.0412 = 937/900 ·
465.4/439 = 1.0601 = 954/900. `zoneToRect` умножает доли на `canvas.width/height`, значит доли
ложатся один в один. **Перерисовывать 45 WebP и пересчитывать доли не нужно** — в отличие от того,
что предполагалось при постановке («пересчёт долей зон по X» из таблицы задач отменяется).

## Замер: было / стало

Скрипты `.localdev/measure-frame.mjs` и `.localdev/measure-gap.mjs`, headless Chromium, порт 8778.
Базовая линия снята честно: `git stash push` шести исходных файлов → прогон → `git stash pop`.

| | было | стало |
|---|---|---|
| ПК 1366×768: разных ширин карточки / высот | **8 / 6** | **1 / 1** (527×491.4) |
| Телефоны 320/360/375/412/768: разных высот карточки | **8** на каждом | **1** на каждом (412: 388×360.4) |
| Холст Champion на ПК | 501×465.4 | 501×465.4 — **не изменился ни на пиксель** |
| Холст Star на ПК | 439×471 | 439×465.4, по бокам белое поле |
| Перелив по вертикали на ПК (8 линеек) | 0 | 0 |
| Перелив по горизонтали | 0 | 0 |

Цель клиента («чтобы рамка была одна и не гуляла вверх-вниз») выполнена по числам.

## Регресс зазора — найден собственной проверкой и закрыт

Собственная проверка нашла просадку ровно в том, что клиент назвал **единственным условием**
к шагу 1: «главное, чтобы верхние кнопки слишком близко не приближались, не тыкались там в них».

Зазор «ряд плашки линейки → чернила формы», 320/360/375/412/768:

```
волна 4 (база):  13.1 / 16   / 17   / 19   / 17
после рамки:     11.7 / 14.5 / 14.6 / 17.6 / 12.6
```

**Это не ошибка, а свойство «одной рамки».** Белая полоса над формой — 60 пикселей КАДРА
(`TOP_PX = 60`), а кадры у линеек разной высоты (836 у Champion … 954 у Star). Когда все кадры
вписаны в карточку ОДНОЙ высоты, у высокого кадра те же 60 px дают на экране меньше, и форма
подходит к ряду ближе. Регулятор — просвет над карточкой, тот же, которым волна 4 чинила
эту же жалобу (18 → 30).

Сделаны две правки `stand.css`, **обе промерены**:
- `@media (max-width: 560px)`: `.views { padding-top: 30px → 33px }`;
- `@media (max-width: 900px)`: новое правило `.views { padding-top: 5px }` — у полосы 561-900
  своего отступа не было вовсе, а просела она сильнее всех (17 → 12.6).

⚠️ **Промежуточные 32 px НЕ хватило, и это видно только замером:** на 375 оставалось 16.8 против
базовых 17. Взято 33. Итог, мин по восьми линейкам:

```
база (волна 4):  13.1 / 16   / 17   / 19   / 17
после рамки:     11.7 / 14.5 / 14.6 / 17.6 / 12.6   ← регресс
итог 33 + 5:     14.5 / 17.5 / 17.8 / 20.6 / 17.6   ← база превышена всюду
```

Контрольный прогон «рамка одна» после этих правок: **1 ширина и 1 высота карточки на всех шести
профилях**, перелив на ПК 0, по горизонтали 0 везде. На телефонах вертикальный перелив законен —
`_availHeight()` ниже 900 px намеренно возвращает 0, мобильная страница скроллится.

## Проверка: как повторить

`.localdev/` удалена по правилу проекта, **но код замера сохранён** в хвосте
`docs/ГРАБЛИ-инструментов.md` («Замер зазора ряд → чернила»), вместе с базой и двумя ловушками
(чернила искать по пикселям холста, профиль обязан быть с `hasTouch`/`isMobile`).

```bash
npx --yes serve . -l 8778    # ждать ~15 с, проверить curl-ом (первый ответ 000 — норма)
npm test                     # 92/0
python -m pytest             # 41 passed
```

⚠️ `npm test` гонять именно так: tdd-guard читает вывод через `./tests/tdd-guard-reporter.mjs`,
подключённый только к `npm test`; голый `node --test` красный не регистрирует и хук продолжает
блокировать правки.

## Осталось

Ветка → PR → кнопку жмёт владелец (`gh` без учётки, GitHub закрыт доменной политикой Claude
in Chrome). После вливания — сверить sha256 демо с `origin/main` по правленым файлам.

## Что НЕ входило в шаг 1

Клиент сам разделил работу: шаг 2 — «формы внутри растянуть по размеру рамки» — он готов делать
сам, из приёмки шага 1 это вынуто. Star и после правки останется с двойным отступом снизу: его
держит сырое фото `Star чёрный` (пункт 1 таблицы задач, за клиентом).

## Клиенту ещё не написано

Ответ на его голосовые 13:44/13:52 не написан. В нём должны быть два числа (836/900 → Чемпион
не трогаем, против буквальных 900×1200 → −30 % на ПК) и **срок** — он уже сказал «получается
долго и сложно, наверное придётся мне это всё подгонять», то есть забирает работу себе.
