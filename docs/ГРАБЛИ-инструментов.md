# Грабли инструментов и переиспользуемые замеры

Вынесено из `CLAUDE.md` 31.08 при сжатии до потолка 25 КБ: нужно по случаю, а не при каждом
старте. Побайтовый исходник — `docs/HISTORY-jetron-constructor-2026-08-31.md`.
Грабли БОЕВОГО деплоя лежат отдельно, в `docs/ДЕПЛОЙ-боевой.md`.

## Инструменты

- **Edit/Write иногда падают `Not logged in`** → обход: node-скрипт точечных замен через Bash
  (однострочные якоря, `includes`-проверка, `process.exit(1)` при непопадании). ⚠️ У `deploy/*.php`
  в дереве **CRLF**: нормализовать перед поиском якоря и вернуть перед записью. ⚠️ Большой heredoc
  в Bash рвётся на спецсимволах — надёжнее Write в scratchpad + короткий node-скрипт вставки.
- **Windows autocrlf:** сверять деплой с HEAD-блобами (`git show HEAD:путь | sha256sum`), не с
  рабочим деревом. SHA-256 надёжнее GitHub API (60 запросов/час, raw-CDN отстаёт на минуты).
- **Кириллица в git:** `ls-files` отдаёт её экранированной (`\320\261…`) и `xargs rm` молча
  не удаляет ничего. Всегда `git -c core.quotepath=false`.
- **Кириллица в URL при проверке демо:** список читать через `tr -d '\r'` и percent-кодировать,
  иначе curl ответит «Malformed input to a URL function» и покажет ложные битые ссылки.
- **Фильтр вывода режет значения по виду и по имени ключа** (хэши, `hasNewToken`, `href` с query).
  Лечение: сравнивать ВНУТРИ страницы и возвращать булево под нейтральным именем.
- **`mcp__claude-in-chrome__javascript_tool`** требует top-level `await` (асинхронная IIFE вернёт
  пустой объект); в `mcp__Claude_Browser__javascript_tool` наоборот — оборачивать в `new Promise`.
- **Вкладка в фоне / панель браузера:** `visibilityState: "hidden"` → `requestAnimationFrame` не
  идёт, `ResizeObserver` молчит, `getBoundingClientRect` нули, холст Fabric пустой. Артефакт среды,
  не баг: проверять через объекты и таймеры либо форсировать `canvas.renderAll()`.
- **`app.views` это Map:** `Object.keys(app.views)` всегда `[]`, мерить `.size` и `[...keys()]`.
- **Claude Preview MCP:** `localhost` в его браузере не открывается, **брать `127.0.0.1`**.
  ⚠️ Он читает `C:\Projects\.claude\launch.json` (РОДИТЕЛЬСКИЙ), а не проектный — добавлять
  конфигурацию туда. Порт 8777 держит осиротевший процесс, для превью заведена вторая
  конфигурация `jetron-preview` на 8778. `preview_screenshot` подвисает по 30 с при живом
  `preview_eval`.
- **Смена формы в превью:** метода `setForm` нет. `app.formId = '<id>'; await app.buildViews();
  await app.renderAll();`. `setOptData(opt, patch)` принимает ОБЪЕКТ опции из
  `availableOptions()`, а не строку id — со строкой молча пишет в `optCache[undefined]`.
- **Расшифровка голосовых:** `PYTHONIOENCODING=utf-8 python docs/transcribe_one.py <файл>` либо
  `faster-whisper` (small, ru). Звук из mp4 — ffmpeg из
  `python -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"`, отдельного ffmpeg
  на машине НЕТ. ⚠️ Вывод в файл: кириллица в консоли бьётся. ⚠️ Кадры видео брать в ПОЛНОМ
  разрешении (`-ss N -frames:v 1`), даунскейл делает интерфейс нечитаемым.
- **tdd-guard:** stub-first (пустой export → красный тест → реализация); слеп к голому `node --test`.
- **`serve` роняет query на `/index.html`** — `?zones=edit` цеплять к корню:
  `http://localhost:8777/?zones=edit`.

## Замеры, которые переиспользуем (не переделывать)

**Холст в конфиге:** `mock-config.json` → `canvas { width: 900, height: 1200,
displayWidth: 450 }`. ⚠️ Число 1200 здесь — только эталон для долей зон и стартовое значение:
реальную высоту `setBackground` подгоняет под картинку (см. «Контракт кадра» в `CLAUDE.md`).
Клиент на слух называл «900 на 1000» — ошибся.

**Кадр композитный:** слева перёд, справа зад, белый фон, все 45 исходников квадратные.
**Пропорция самой формы 0.48-0.62 (в среднем 0.58), пары «перёд + зад» 1.05-1.34.**
Отсюда всё остальное: упирается ШИРИНА, высоту рамки задаёт зазор поставщика между видами.
После перегона 30.08 заполнение 56-71 % высоты, нижний зазор 38..125 px (было 130..310),
вес файла 25..132 КБ.

**Размерные сетки.** У каждой модели своя таблица в ACF на таксономии `pa_model`
(term_id: Champion 54, New 59, Rich 42, Space 43, Star 44, Winner 45, Волна 70).
Подполя: `sizes_adult` — `size` (объект term, брать `$v->name`), `size_rus`, `height`,
`shirt_height`, `shirt_width`; `sizes_child` — `size`, `height`, `age`, `shirt_height`,
`shirt_width`, `shorts_height`, `shorts_width`. В конструктор отдаём три столбца.
ACF Pro 6.4.1, активен. ⚠️ В карточках товаров атрибут «Размер» это **БИРОЧНАЯ** шкала
(S…5XL), а российский размер и рост берутся из таблицы по букве — это корень старой путаницы.

📊 **Три подробных замера уехали в `docs/HISTORY-jetron-constructor-2026-08-28.md`, переделывать
не надо, читать по надобности:** таблица 45 присланных файлов по группам размеров и масштабам;
инвентарь 55 боевых мокапов (19 размеров, 15 пропорций); расхождение каталога и конструктора
от 22.08, из которого выросли «убрать Rich» и «Venom → Фаворит».

## 01.09 — Preview MCP и Claude in Chrome отвалились оба, чем мерить вместо них

Вкладка Preview MCP встала намертво на `chrome-error://chromewebdata/` после попытки уйти
на `https://andrrreyiv.github.io/...` (демо из этого браузера не открывается). После этого
`location.replace`, `location.assign`, `history.go(-1)`, `preview_stop` + `preview_start`
и даже новый `serverId` возвращают ту же ошибочную страницу: вкладка не переиспользуется.
`preview_screenshot` при этом отдаёт пустой белый кадр, а `preview_snapshot` — голый
`RootWebArea`. Claude in Chrome в тот же момент отвечал «extension isn't reachable»,
chrome-devtools MCP в сессии не поднят.

**Обход, которым сделаны все замеры 01.09** — headless Chromium напрямую, минуя MCP:

```js
import { chromium } from 'file:///C:/Users/Андрей/AppData/Roaming/npm/node_modules/@playwright/mcp/node_modules/playwright/index.mjs';
const browser = await chromium.launch({ headless: true });
const page = await browser.newPage({ viewport: { width: 1366, height: 768 }, deviceScaleFactor: 1 });
await page.goto('http://localhost:8778/', { waitUntil: 'networkidle' });
await page.waitForFunction(() => window.__jetronApp && document.querySelectorAll('.model-card').length > 0);
```

Пакет `playwright` глобально НЕ установлен, он лежит внутри `@playwright/mcp`; браузеры
уже скачаны в `~/AppData/Local/ms-playwright`. Скрипт класть во временную папку и удалять
после замера, в репозиторий не коммитить.

⚠️ Мерить приходится на localhost, поэтому ДО замеров сверять sha256 живого демо с
`git show origin/main:<файл>` и убеждаться, что рабочее дерево чистое и равно `origin/main` —
иначе замер описывает не то, что видит клиент.

⚠️ Селекторы, на которых легко промахнуться: палитра ЦВЕТА ФОРМЫ — это `#colorpick
.color-palette .pcolor` (15 штук), а `.color-sw` внутри карточки «Фамилия и номер» это
7 цветов ТЕКСТА, другое. Горизонтально прокручивается `.model-carousel` ВНУТРИ `#modelpick`,
сам `#modelpick` имеет `overflow-x: visible` и всегда показывает `scrollLeft = 0`.
Строки образцов считать кластеризацией `top` с допуском в половину высоты: у активного
свотча рамка даёт разброс в 2 px, и наивный `new Set(tops)` рисует «две строки» на пустом месте.

⚠️ **Ловушка волны 4, самая дорогая из всех: узкий viewport — это НЕ телефон.**
`newPage({ viewport: { width: 360 } })` без `hasTouch`/`isMobile` оставляет `pointer: fine`,
и весь блок `@media (pointer: coarse)` выключен. В нём у нас живут разные размеры: пилюля
`.line-badge-pill` получает `padding 7px 8px` + `min-height: 36px` вместо `padding 5px 11px`,
и ряд становится **36 px вместо 28.3**. Замер без касания описывает пользователя узкого ОКНА
десктопного браузера, а не покупателя с телефоном. Практический итог 02.09: без касания зазор
на 412 читался как 15.2 px, с касанием — **7**; зато «перенос текста на две строки при 360»
существовал ТОЛЬКО без касания. Оба вывода были наполовину ложными.
Правильный запуск:
```js
const page = await browser.newPage({
  viewport: { width: 412, height: 900 }, deviceScaleFactor: 2,
  hasTouch: true, isMobile: true,          // ← без этих двух строк меряется ПК
});
```
⚠️ Если правка касается только телефона — класть её в мобильный медиазапрос
(`max-width: 560px`), тогда критерий приёмки 1366×768 она не заденет и перемерять восемь
линеек не придётся. Но перемерять всё равно дешевле, чем гадать: `.localdev/v.mjs`, минута.

⚠️ **Две ловушки замера третьей волны, обе уже сработали — не повторять.**
**(1)** «Зазор до правого края» мерить до края РЯДА, а не до края экрана: до экрана всегда
12 px, это отступ страницы, и он ошибочно читается как регресс. До края ряда — **0**.
**(2)** Прокрутку палитры мерить на НАСТОЯЩЕМ скроллере — ближайшем предке `.pcolor`,
у которого `scrollWidth > clientWidth`. Взять `.colorpick` мало: он не прокручивается,
даёт `0 → 0` и создаёт видимость успеха при ЛЮБОМ состоянии кода. Тем же способом врал
замер карусели (у дальнего цвета всего одна модель — мерить на цвете по умолчанию).

---

## ⚠️ Не переоткрывать — проверено замером

Вынесено из `CLAUDE.md` 02.09 при сжатии до потолка 25 КБ (побайтовый исходник —
`docs/HISTORY-jetron-constructor-2026-09-02-volna5-otvet.md`). Всё ниже уже проверено
замером или ответом клиента: заново не расследовать, вывод не переоткрывать.

- ⚫ **Пункт 9 (ряд плашки лежал на вороте) — причину заново не искать:** виновато СТОЛКНОВЕНИЕ
  двух правок волны 2. «Вернуть как было» невозможно (60 кадровых px = 19.5 экранных при 320,
  ряд 36 px), 18 из 58 px отдали осознанно. Разбор — ПЕРЕДАЧА 01.09. ⚠️ Волна 4 открыла его снова.
- ⚠️ **Не принимать за баг:** на ПК ряд плашки шире картинки на 13 px с каждой стороны — это
  `padding: 12px` + рамка 1 px у `.canvas-wrap`, то есть ряд выровнен по краю БЕЛОЙ КАРТОЧКИ,
  а не по кромке фото (проверено скриншотом 1366×768). На телефоне карточки нет и совпадение
  с картинкой точное. Так было всегда, правка ни при чём.
- 📌 **Урок, стоивший двух дефектов подряд: медиазапрос НЕ повышает специфичность.**
  `.colorpick .pcolor` (0,2,0) молча бил мобильные `.pcolor` (0,1,0) — свотчи на телефоне стали
  26 px вместо 44. Тем же способом проигрывал `padding` в правиле для 320 px. Оба раза увидел
  ЗАМЕР, а не чтение кода. ⚠️ **Долг по тестам:** вёрстку они не стерегут вообще, покрыт
  1 пункт раскладки из 7 — после правки CSS смотреть глазами, зелёный `npm test` не значит тут
  ничего.
- ⚫ **«Зоны champion-white уехали вверх после среза кадра» — НЕ подтверждено, 02.09.** Форма
  и зоны сдвинуты на одни и те же 156 px (≤ 0.1 px), обратный прогон `zones.json` через боевую
  картинку клиента сходится: фамилия 13.65 % против 13.65 %, номер 22.05 % против 22.12 %.
  Так их поставил РУКАМИ клиент, не чинить. Непроверено одно — посадка чернил `fitInkToRect`.
- ⚫ **Отступ номера = 1 px, 2 px не ставить.** Клиент назвал 1 (его же вторая ветка указания:
  на отступе 0 верх цифры срезала рамка зоны). Прогон по 23 шрифтам: у **20** отклонение **0**,
  у трёх теряется только строка сглаживания (Chrome квантует `actualBoundingBoxAscent`).
  Таблица прогона — в чек-листе приёмки.
- 🟡 **Осознанно НЕ делали, клиент не просил** (всплывёт на созвоне — правки мелкие): внутренняя
  прокрутка панели настроек (со вскрытой карточкой страница 881-890 px); бахрома сглаживания
  у трёх шрифтов из 23 (лечится отступом 2 px, но он спорит с «1 пиксель»).

---

## Замер зазора «ряд плашки линейки → чернила формы» (волны 4 и 5)

Это единственный числовой сторож условия клиента «верхние кнопки слишком близко не приближались».
Тесты вёрстку не стерегут вообще, поэтому после КАЖДОЙ правки отступов над макетом гонять его.
База, ниже которой опускаться нельзя (мин по восьми линейкам на 320/360/375/412/768):
**13.1 / 16 / 17 / 19 / 17**. Замер 02.09 после «одной рамки» и `padding-top: 33px`:
14.5 / 17.5 / 17.8 / 20.6 / 17.6.

⚠️ Чернила ищутся по ПИКСЕЛЯМ холста (верхняя строка, где есть не-белый пиксель), а не по
рамке элемента: у `<canvas>` рамка включает белую полосу кадра, и по ней зазор выходит вдвое
больше настоящего. ⚠️ Профиль обязателен с `hasTouch`/`isMobile`, иначе `@media (pointer: coarse)`
выключен и меряется не телефон. Сервер: `npx --yes serve . -l 8778`.

```js
import { chromium } from 'file:///C:/Users/Андрей/AppData/Roaming/npm/node_modules/@playwright/mcp/node_modules/playwright/index.mjs';
const URL = 'http://localhost:8778/';
async function зазор(page) {
  return await page.evaluate(() => {
    const row = document.querySelector('.line-badge');
    const cv = document.querySelector('.canvas-wrap canvas');
    if (!row || !cv) return null;
    const rowBox = row.getBoundingClientRect(), cvBox = cv.getBoundingClientRect();
    const off = document.createElement('canvas');
    off.width = cv.width; off.height = cv.height;
    const ctx = off.getContext('2d', { willReadFrequently: true });
    ctx.drawImage(cv, 0, 0);
    let inkRow = -1;
    const H = Math.min(off.height, 400);
    const d = ctx.getImageData(0, 0, off.width, H).data;
    for (let y = 0; y < H && inkRow < 0; y++)
      for (let x = 0; x < off.width; x++) {
        const i = (y * off.width + x) * 4;
        if (d[i + 3] > 8 && (d[i] < 245 || d[i + 1] < 245 || d[i + 2] < 245)) { inkRow = y; break; }
      }
    const k = cvBox.height / (off.height || 1);
    return inkRow < 0 ? null : +(cvBox.top + inkRow * k - rowBox.bottom).toFixed(1);
  });
}
const browser = await chromium.launch({ headless: true });
for (const w of [320, 360, 375, 412, 768]) {
  const page = await browser.newPage({ viewport: { width: w, height: 780 }, deviceScaleFactor: 1, hasTouch: true, isMobile: true });
  await page.goto(URL, { waitUntil: 'networkidle' });
  await page.waitForFunction(() => window.__jetronApp && document.querySelectorAll('.model-card').length > 0);
  const n = await page.evaluate(() => document.querySelectorAll('.model-card').length);
  const out = [];
  for (let i = 0; i < n; i++) {
    await page.evaluate((k) => document.querySelectorAll('.model-card')[k].click(), i);
    await page.waitForTimeout(450);
    out.push(await зазор(page));
  }
  console.log(`${w}: ${out.join(' ')}  мин ${Math.min(...out.filter((v) => typeof v === 'number'))}`);
  await page.close();
}
await browser.close();
```

Второй скрипт, «рамка одна» (волна 5): по каждой из восьми линеек снимать
`.canvas-wrap` и `canvas` через `getBoundingClientRect()` плюс
`scrollHeight - clientHeight`, затем считать `new Set(...)` по ширинам и высотам карточки.
Критерий приёмки: **1 ширина и 1 высота** на каждом профиле, перелив на 1366×768 равен 0.
На телефонах вертикальный перелив ЗАКОНЕН — `_availHeight()` ниже 900 px намеренно возвращает 0.

## Замер ПРОБЛЕСКА: одного финального снимка мало (02.09, волна 5)

Скрипт «рамка одна» снимает состояние **после** загрузки картинки и поэтому не видит,
что между кликом и готовым WebP карточка успевает постоять в неправильном размере.
На живом демо так пряталось `326 → 468 → 326` длиной 200-950 мс на 14 расцветках из 15 —
ровно то «гуляние рамки», против которого клиент и возражал.

Ловится покадровой съёмкой: наблюдатель ставится **до** клика, иначе первые кадры теряются.

```js
await page.evaluate(() => {
  window.__след = [];
  window.__тик = setInterval(() => {
    const b = document.querySelector('.canvas-wrap')?.getBoundingClientRect();
    if (b) window.__след.push(+b.height.toFixed(1));
  }, 50);
});
// на каждой итерации заново querySelectorAll: палитра перерисовывается после клика,
// и закэшированный список свотчей отдаёт undefined («Cannot read properties of undefined»)
await page.evaluate((k) => document.querySelectorAll('.pcolor')[k]?.click(), i);
await page.waitForTimeout(1500);
const уник = [...new Set(await page.evaluate(() => window.__след))];  // > 1 значит проблеск
```

Причину показывает тот же тик, если печатать не только высоту, а `wrap.style.height`,
`canvas.width/height` и число `.canvas-container`. Здесь он показал: инлайновый стиль ПУСТ,
холст `351x468` — то есть карточка обтягивала свежесозданный Fabric-холст по дефолту 1200/900,
потому что её размер ставился после `await fabric.FabricImage.fromURL(...)`.

⚠️ **Локальный сервер отдаёт картинки мгновенно и может СКРЫТЬ проблеск, а не вылечить его.**
Проверять правку только с торможением сети, иначе «стало чисто» ничего не доказывает:

```js
await page.route('**/*.{webp,png,jpg,jpeg}', async (route) => {
  await new Promise((r) => setTimeout(r, 1500));
  await route.continue();
});
```

⚠️ И до выводов — разведка DOM. Скрипт показывал `351x468` при кликах по `.pcolor`, и это
легко принять за переключение линеек. `.pcolor` оказались 15 свотчами цвета внутри
`.color-palette` — линейка не менялась вовсе. Один `page.evaluate` с перечислением классов
и родителей дешевле неверного диагноза.
