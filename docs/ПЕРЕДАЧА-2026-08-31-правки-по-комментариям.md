# ПЕРЕДАЧА 31.08 вечер — правки по 14 комментариям клиента

Написано для другого аккаунта Claude на этом же ПК. Всё, что нужно, чтобы поднять работу
с середины, не перечитывая транскрипт.

**Читать в таком порядке:** этот файл → `docs/КОММЕНТАРИИ-КЛИЕНТА-2026-08-31.md` (чек-лист
приёмки, 14 строк) → план `C:\Users\Андрей\.claude\plans\foamy-popping-moler.md` (номера строк,
причины дефектов, порядок работ) → `CLAUDE.md` проекта.
Расшифровки голосовых — `docs/voice-client-2026-08-31.txt`.

---

## Где сейчас работа

- Ветка **`fix/client-comments-31-08`** от `main` (`620fa31`). Дерево чистое, всё закоммичено.
- Коммиты этой сессии поверх `0ae0476` (расшифровки голосовых):

| Коммит | Что |
|---|---|
| `4bcbd10` | чек-лист из 13 комментариев |
| `fe0292e` | шрифт номера на груди привязан к спине (пункт 13) + разбор пункта 14 |
| `11db6be` | три мелкие правки: полоска над «Итого», шрифт «Комплектации», размер стрелок (пункты 10-12) |
| `72ab2f5` | отступ у номера = 0 и удаление `champion-sinij3` (пункты 1-2) |

- Замер после последнего коммита: `npm test` → **84/0** (было 83), `pytest` → **36 passed**.

## 🟢 Главное: проект РАСБЛОКИРОВАН

Два вопроса, которые держали выкладку на боевой, клиент закрыл сам голосовыми 31.08:

1. **Отступ у номера сверху — ноль.** «цифра должна касаться верхнего края рамки и нижнего…
   сделать 0, а если не будет обрезаться, то так оставляем. Если будет всё-таки обрезаться
   на 0, тогда сделайте 1 пиксель». Сделано: `NUMBER_TOP_INSET_RATIO = 0`.
2. **`champion-sinij3` — удалить.** «это просто как тест был». Сделано: ключ убран из корневого
   `zones.json`. ⚠️ В `mock-config.json` этой расцветки не было никогда — она жила только
   в `zones.json` и в `admin.json` на боевом, поэтому **с сайта она исчезнет только при выкладке**.

Прежняя догадка «клиент спутал синий с синий3» снята — он сказал прямо.

## 🔴 Что осталось сделать (по порядку из плана)

### 1. Мокапы: хвост от шортов снизу (пункт 3 чек-листа) — делать ПЕРВЫМ

Причина порядка: Space и Star — самые высокие формы своих линеек, поэтому чистка уменьшит
их `line_canvas`, и доли зон этих двух линеек уедут. Если сделать это ПОСЛЕ того, как клиент
расставит зоны на 8 карточках-донорах, его ручную расстановку придётся переносить дважды.

- `scripts/normalize_mockups.py`, `ink_bbox()` (:84-96): отбрасывать оторванный хвост —
  разрыв ≥ 24 пустых строк, сам хвост ≤ 4 строк → нижняя граница берётся до разрыва.
  Пороги вынести константами рядом с `WHITE_CUT` и `MIN_INK_PIXELS`, замеренные значения — в комментарий.
- Замер numpy-обходом всех 45 `assets/mockups/*.webp`: затронуто **8 из 45** — Space
  белый/бирюзовый/жёлтый/красный/салатовый/чёрный (H=1101) и Star красный/синий (H=1110).
  Хвост везде 2 строки шириной 69-140 px после белого разрыва 56-64 px.
- Прогнать `scripts/run_normalize.py` (два прохода, 45 файлов).
- 🔴 **Каскад обязателен:** после нормализации перегнать `scripts/rewrite_catalog.py`
  и сверить, что `line_canvas` изменились **ровно у Space и Star**. Champion чист,
  корневой `zones.json` не затрагивается.

### 2. Вёрстка: конструктор в один экран (пункты 4-9)

Сквозной критерий приёмки, названный клиентом прямо: **на ноутбуке 1366×768 конструктор
помещается без вертикальной прокрутки.** Все «убрать надпись» и «уменьшить кнопки» — про это.

- **9 / 3.4 — убрать три надписи:** подзаголовок `app.browser.js:434`, «ВЫБЕРИТЕ ЦВЕТ ФОРМЫ»
  на :528 вместе с обёрткой `.cp-head`, «МОДЕЛЬ N ВАРИАНТОВ» на :535. Имя цвета не теряется:
  у каждого свотча есть `title`/`aria-label` (:532).
- **4 / 3.3 — карусель не прыгает в начало:** обработчик `.model-card` (:555-563) зовёт
  `buildColorPicker()`, тот перерисовывает `innerHTML` и теряет `scrollLeft`. Смена модели
  внутри цвета не меняет ни палитру, ни состав карусели — оставить только перестановку
  `.active` + `buildViews()` + `renderAll()`. Обработчик `.pcolor` (:544-553) **не трогать**.
- **7 + 5 / 3.5-3.6 — кнопки:** «Скачать макет» убрать из `buildColorPicker()` (:526)
  и правила `.cp-download` (`stand.css:478`), добавить третьим элементом в `_renderLineBadge()`;
  `.line-badge` со `space-between` на grid `1fr auto 1fr`; уменьшить на ~20 %
  (`.line-badge-pill` padding `6px 14px`→`5px 11px`, font-size `13px`→`10.5px`, `.stage-btn` тем же).
  ⚠️ `_renderLineBadge()` живёт внутри `updatePrice()` — обработчик `downloadImage()` вешать
  при КАЖДОЙ перерисовке. `ProductLink.js:5-15` — подпись всегда `'перейти в карточку →'`,
  комментарий переписать честно. Зазор под ✕ (`.line-badge { padding-right: 54px }`, :981)
  перенести на сам крестик, чтобы кнопка прижалась к правому краю.
- **8 + 6 / 3.1-3.2 — палитра:** разделить `#colorpick` на `#colorpick` (палитра) и `#modelpick`
  (карусель) в `index.html`, `buildColorPicker()` пишет в два хоста. `#stage` → grid:
  ПК `"pal views" / "models models"`, колонки `auto 1fr`; мобильная (существующий брейкпоинт
  900 px на :969, нового не заводить) `"views" "pal" "models"`. Палитра на ПК —
  `display: grid; grid-template-columns: repeat(2, 34px)` (2 колонки × 8 рядов, ~77×344 px);
  на мобильной — горизонтальный скроллер по образцу `.model-carousel` (:355-367),
  боковой padding ≥ 5 px под кольцо активного свотча. У `.colorpick` (:468-475) снять
  `max-width/margin/border-top` **только на ПК**.

### 3. Проверка глазами (ничего из этого ещё не делалось)

`npx --yes serve .` (порт 8778, Preview MCP по `127.0.0.1`), по списку:

1. 🔴 **цифра номера не срезана сверху** при отступе 0 — на груди, спине и шортах.
   Срежется — вернуть ровно `1 / 1200`, не пиксельную константу.
   ⚠️ Комментарий в `ZoneManager.js:53-55` **уже утверждает «проверено в браузере»** —
   это утверждение пока не подтверждено. Либо проверить, либо смягчить формулировку.
2. на 8 почищенных мокапах под шортами нет оторванного кусочка;
3. **окно 1366×768 — без вертикальной прокрутки** (главный критерий);
4. палитра слева, эскизы под макетом, настройки справа;
5. клик по самой правой модели — карусель не прокручивается, рамка переезжает на нажатую;
6. окно 375 px — палитра в одну строку со скроллом, три кнопки не налезают на ✕;
7. «Комплектация» и «Детализация» одним шрифтом, чёрточки над ценой нет;
8. номер на груди меняет шрифт вместе со спиной.

Плюс оба набора целиком: `npm test` и `python -m pytest` (частично не гонять — провал сбора не виден).

## Пункт 14 работы не требует

«Уберите рамки для пользователей, а в редакторе их оставьте». Разделение уже есть с 27.08:
`canvas.browser.js:102-120` рисует зону прозрачной (`stroke: null`), обводку возвращает только
`zone-editor.browser.js:144`. Клиент смотрит на **боевой**, где живёт сборка ДО той правки —
замер `curl` по обоим адресам в чек-листе. Рамки пропадут при выкладке.

## Грабли, на которые уже наступили в этой сессии

- 🔴 **tdd-guard включён и строгий.** (а) правка кода без ЗАПИСАННОГО красного теста блокируется;
  (б) больше ОДНОГО нового теста за раз блокируется; (в) он читает результаты только из
  проектного репортера `tests/tdd-guard-reporter.mjs`, поэтому **`node --test <файл>` его
  обманывает** — там остаётся чужой `test.json` (у нас показывал результаты
  `andrianov-pr-lawyers`). Красную фазу писать **только `npm test`**, целиком.
- `Edit` требует, чтобы файл (или его кусок) был прочитан в ЭТОЙ сессии.
- Предупреждения git «LF will be replaced by CRLF» на каждом коммите — норма для Windows.

## Что этот заход сознательно НЕ делает

- **Не выкладывает на боевой.** Только по `docs/ДЕПЛОЙ-боевой.md`: нужна пересборка
  `admin.json` (иначе на сайте не изменится ничего) и буст `?v=` во всех 20 местах.
- **Не размножает зоны по линейке** — клиент не прислал ни одной карточки-донора.
- Не трогает `zoneTemplate` — он и эталон долей, и запасной вариант.
- 🔴 Слияние в `main` — **только PR, кнопку жмёт владелец** (`gh` без учётки).
