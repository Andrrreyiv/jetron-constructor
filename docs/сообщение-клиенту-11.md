# Сообщение клиенту №11 (Дмитрий, jetronsport.ru)

⚫ **Отменяет сообщение №10** (и заодно №9). Оба построены на выводе «окно 900×1200 жёсткое,
картинку надо брать в пропорции окна». Вывод НЕВЕРЕН и снят замером.

Формат: ОДНО сообщение, 1 383 знака, без картинок. Длинные тире не использованы намеренно.

## Замер, который всё перевернул (не переспрашивать)

- `CanvasView.setBackground` (`src/js/ui/canvas.browser.js:49-59`): масштаб = ширина холста /
  ширина картинки, затем `canvas.setDimensions({ height: картинка.height * scale })`.
  То есть **высота холста подстраивается под картинку**, пропорция может быть любой.
- Проверено живым браузером на стенде 29.08: подсунули фон 1100×1200, высота холста поехала
  **800 → 654.5** при ширине 600, ровно 600 × 1200 / 1100. Пустой полосы не появилось.
- Второе подтверждение — скриншоты клиента с его собственным файлом 1100×1200.
- ⚠️ Отсюда: белая полоса снизу бралась НЕ из пропорции и НЕ из окна. Её добавляли мы сами,
  добивая каждый кадр белым до высоты 1200 в `normalize_mockups.py`.
- Правка: высота кадра считается по линейке (`line_canvas`) = самая высокая форма линейки
  + верхняя полоса 216 px + нижний зазор 40 px. Внутри линейки кадр общий, между линейками
  разный: формы Champion 674..736 px, Star 776..854, общий кадр вернул бы Champion полосу.

---

Дмитрий, вы всё делаете правильно, а запутал вас я. Свой вопрос снимаю.

Я был уверен, что окно на сайте жёсткое и картинку надо подгонять под него. Полез проверять в браузере и увидел обратное: окно подстраивается под картинку. Какую пропорцию дадите, такая и будет, хоть 1100 на 1200, хоть любая другая. Поэтому у вас всё и встаёт нормально.

А значит та белая полоса снизу, из-за которой мы весь день ходили кругами, бралась не от окна и не от вашей картинки. Её добавлял я сам: когда готовил все 45 файлов, я добивал каждый белым полем до одной общей высоты. Вот эта добивка и торчала снизу. Извините, что гонял вас по кругу с этими пикселями.

Что делаю сейчас. Перегенерирую все 45 картинок так, чтобы лишнего белого снизу не было вообще. Сверху оставлю полосу под плашку с названием и под кнопки, которые вы хотели туда добавить. Снизу оставлю небольшой зазор, чтобы шорты не упирались в край карточки. Внутри одной линейки кадр будет одинаковый у всех расцветок, иначе логотипы будут прыгать, когда вы переключаете цвет.

Вам переделывать ничего не надо. Файлы, которые вы уже сделали, подойдут как есть.

И остался один вопрос, он не про картинки. Меню на сайте два разных, бургерное на телефоне и верхнее на компьютере, правка одного на второе не переносится. Убрать конструкторы только из бургера, или из верхнего тоже, чтобы остались действительно только в футере?
