# Сообщение клиенту №17 — три комментария от 03.09

📌 **Отправлять ПОСЛЕ №16, не вместо него.** №16 закрывает волны 2-5 и уже сверено;
дописывать в него эти три комментария нельзя. В №17 есть строка «про растянуть внутри рамки
ответил в прошлом письме» — она врёт, если №16 не ушло. Порядок обязателен.

🔴 **УСЛОВИЕ ОТПРАВКИ НЕ ВЫПОЛНЕНО на 04.09.** Письмо утверждает, что две правки уже на витрине.
До отправки:
1. влить кнопкой `fix/color-blink-views-reuse` (`ef535db`) и `fix/frame-still-no-hover-lift` (`7f640fb`);
2. дождаться пересборки демо и **перемерять на живом адресе, а не на localhost**:
   наведение и колесо дают сдвиг 0 px, при переборе всех 14 расцветок карточка не пустеет
   и плашка линейки не пропадает;
3. `npm test` и `pytest` на слитом `main`.
⚠️ Урок 04.09 по соседнему проекту: **перед отправкой перемерять всё, что письмо утверждает
числом или словом «работает»** — обещание протухает за сутки.

📎 **Прикрепить ОДНУ картинку:** `docs/вопрос-палитра-ПК-2026-09-04.png` (палитра слева на ПК
крупным планом). Без неё вопрос 1 непонятен. Он сам просил 28.08: «Пришлите мне в картинках,
я не совсем понимаю, о чём речь».

⚠️ **Про «ctrl C и Ctrl V» не гадать.** Это обрывок посреди фразы, распознавание съело слова.
В письме задан открытый вопрос, вариантов ему не подсказываем — иначе он согласится с нашим,
а не скажет своё.

⚠️ **Числа в письме — замеренные, не округлённые «на глаз»:**
подъём рамки 4 px; палитра на ПК 26×26 с просветом 5 px, на телефоне 36×36 с просветом 11 px.

---

Дмитрий, разобрал три ваших комментария. Два оказались настоящими ошибками, обе исправил, они уже на витрине:

https://andrrreyiv.github.io/jetron-constructor/

**«Мышку подвожу, рамка поднимается и наоборот» — нашёл и убрал.** Вы описали точно: белая рамка приподнималась на 4 пикселя, когда на неё попадал курсор, и опускалась обратно, когда курсор уходил. Отдельно проверил и про ролик: если курсор вообще не двигать, а просто крутить колесо, рамка сама проезжает под ним, и она точно так же дёргается вверх-вниз на каждой прокрутке. На сайте конструктор стоит внутри страницы, которая всегда прокручивается, поэтому вы и ловили это постоянно.

Теперь рамка не двигается ни на пиксель. Подсветку при наведении оставил: она никуда не сдвигает картинку, но видно, что конструктор откликается на курсор. Если хотите, чтобы вообще ничего не шевелилось — кнопки и квадратики цветов тоже слегка отзываются на наведение, скажите одно слово, и я заморожу всё.

**«Когда цвет выбираем, блоки начинают скакать» — тоже настоящая ошибка, исправил.** Тут оказалось хуже, чем выглядело. При смене расцветки на секунду пропадала полоса с кнопками «Скачать макет» и «В карточку», а сама карточка на это время оставалась пустой: старая картинка уже стёрта, новая ещё грузится. Плюс карточку подтягивало вверх. Происходило это на каждом клике по цвету. Сейчас при смене расцветки ничего не пересобирается заново, кнопки на месте, старая картинка держится до готовности новой.

Отдельно перепроверил, что не сломал соседнее: смена модели по эскизу по-прежнему меняет и картинку, и зоны, а нанесённые фамилия и номер встают ровно туда же, куда вставали раньше.

**Теперь два вопроса, без них дальше не двинусь.**

**Первый — про «на компе очень близко расположены, чуть раздвинуть».** Не понял, что именно. Прикладываю картинку: самое тесное на компьютере это палитра цветов слева, квадратики 26 пикселей с просветом 5 между ними. На телефоне они крупнее и стоят свободнее — 36 пикселей с просветом 11. Похоже, речь про них? Если нет, обведите на скриншоте, что именно стоит слишком тесно, и я раздвину.

**Второй — про «ctrl C и Ctrl V».** Эта часть фразы оборвалась, и я не понял, что вы хотели скопировать и куда вставить. Догадываться не буду, чтобы не сделать не то. Расскажите своими словами, что вы пытались сделать.

Про «в рамках конструктора максимально растянуть» ответил в прошлом письме, это тот же второй шаг.
