# ПЕРЕДАЧА 2026-08-29 (вечер) — jetron-constructor

Читать первым. Прошлая передача `docs/ПЕРЕДАЧА-2026-08-28.md` жива и не отменена,
но в части «что отправить клиенту» она устарела — см. пункт 2 ниже.

## Где сейчас код

- Ветка **`fix/jetron-size-grid-acf`**, последний коммит **`d2dcf53`**, запушен.
  Дерево чистое.
- `main` = **`963a968`** (PR #1, влит 29.08 утром). **Правок 29.08 в `main` НЕТ.**
- Тесты: `npm test` → **82 pass / 0 fail**; `python -m pytest` → **25 passed**.

## Что сделано 29.08 после трёх голосовых клиента

Расшифровка: `docs/voice-client-2026-08-29.txt`. Он жаловался на три вещи, и по замеру
был прав по всем трём: форма задевала плашку линейки и кнопку, снизу зияла пустота,
между передом и задом далеко.

Коммит **`3a49631`**:

1. **`scripts/normalize_mockups.py`: `TOP_MARGIN` 0.02 → 0.10, `FILL_HEIGHT` 0.90 → 0.86.**
   Причина не косметическая: `.line-badge` в `src/css/stand.css` стоит
   `position:absolute; top:0` поверх холста, а верхнего поля было 24 px из 1200 —
   плашка физически перекрывала плечи. Теперь полоса **120 px**, плашка и кнопка
   помещаются целиком, плюс есть место под кнопки, которые клиент собирается добавлять.
   Инвариант, который нельзя нарушать: `TOP_MARGIN + FILL_HEIGHT <= 1`.
2. **Все 45 мокапов перегенерированы** через `python scripts/run_normalize.py` из
   исходников клиента в `C:/Users/Андрей/Downloads`. Замер после: верх 120..121 px
   (было 24), низ пусто 226..406 px (было 322..502), высота формы 674..854 не изменилась.
3. **`src/config/mock-config.json`:** все 10 боксов `zoneTemplate` сдвинуты по `y` на
   **+0.08** — иначе зоны остались бы на старом месте, они заданы долями от холста.
4. **`zoneSet: "singleFront"` снят с 12 форм** (New / Space / Winner). Это побочный дефект,
   клиент его не называл: набор калиброван под одиночный перёд, а после нормализации
   все 45 кадров композитные, и его центр по x (0.435..0.565) сажал номер ровно в шов
   между передом и задом. Сам `zoneSets` из конфига НЕ удалён — он станет верным снова,
   если клиент выберет вариант В.

Коммит **`d2dcf53`**: письмо клиенту переписано, документация приведена в соответствие,
добавлена картинка `docs/вопрос-пропорция-29-08.png`.

## Что осталось сделать

### 1. 🔴 Влить ВТОРОЙ PR в `main` — иначе демо показывает старое

Демо на GitHub Pages собирается из `main`, workflow'ов нет, слияние = деплой.
`gh` без учётки (`Bad credentials`), прямой `git push origin main` блокирует хук
`~/.claude/hooks/dangerous-actions-blocker.mjs`. **Кнопку жмёт человек:**

```
https://github.com/Andrrreyiv/jetron-constructor/compare/main...fix/jetron-size-grid-acf
```

После слияния подождать ~минуту и проверить, что пересобралось:

```bash
curl -s https://andrrreyiv.github.io/jetron-constructor/index.html | grep -o 'v=[0-9a-z]*' | head -1
```

⚠️ У картинок нет cache-buster (`encodeURI(img)` без `?v=` в `src/js/ui/app.browser.js:1167`),
поэтому вернувшийся посетитель может увидеть старые мокапы. Не чинили.

### 2. 🔴 Отправить клиенту письмо — `docs/сообщение-клиенту-7.md`, **ред. 3**

**ОДНО сообщение, 1698 знаков** (прежние записи про «два сообщения, 3481 и 3722 знака»
неверны). Одна картинка: `docs/вопрос-пропорция-29-08.png`.
⚠️ Старые `docs/вопрос-2-*.png` и `вопрос-3-качество.png` **не слать** — они собраны
до отступа сверху и показывают старую посадку формы.

В письме остался **один вопрос: пропорция A / B / В**, и он блокирует заливку картинок
на боевой. Двумя голосовыми клиент сам закрыл остальные: New розовую делает сам,
палитра остаётся. Плюс в письме согласие на созвон.

### 3. 🔴 Ответить на просьбу о созвоне

Дмитрий просил **дважды**: «я могу вам ссылку на Яндекс Телемост кинуть» и «я всё-таки
рекомендую созвониться… мы очень много будем туда-сюда гонять». Ждёт ответа Андрея.

### 4. 🟡 Заперто ответом про пропорцию, до него не трогать

- Заливка мокапов на боевой `https://jetronsport.ru/constructor/` через WP File Manager.
- Чистка `assets/mockups`: 68 старых файлов + 4 остатка Rich `.webp` (750×1000).
- Если выберет **В** — резать перёд у 45 файлов (машина находит границу у 35, у 10 руками,
  ~полчаса) и вернуть `zoneSet: singleFront` тем 12 формам.
- Если выберет **B** — перекраивать блок на странице, зоны клиент расставляет заново.

### 5. 🟡 Хвосты, не блокирующие

- Бирочные S и M не описаны в размерной таблице клиента → прочерки в конструкторе.
- Нет карточек взрослых Champion, Legend, Фаворит → цена и сетка падают на запасные.
- Оба `CLAUDE.md` переросли потолок 25 КБ (проектный 29,9 КБ, `C:/Projects` 29,6 КБ).
  Резать только по протоколу: `.bak` + побайтовая копия в `HISTORY-*.md` +
  `node C:/Projects/scripts/context-integrity-check.mjs` до строки «ПОТЕРЬ НЕТ».
- Перед любым деплоем PHP — серверный lint `deploy/jetron-zones.php`
  (копия в `constructor/_lint-<random>.php`, валидный отдаёт пустой 200).

## Чего НЕ делать

- ⚠️ Не возвращать этап «разрез 45 → 90 файлов» — клиент опроверг 23.08.
- ⚠️ Не отвечать за клиента на вопрос про пропорцию.
- ⚠️ Не показывать демо без оговорки: без WooCommerce цены идут из конфига, а кнопка
  подписана «в каталог» и ведёт в раздел — ровно то, что клиент опроверг 28.08.
- ⚠️ Не заводить баг по пустому снимку GitHub Pages: 29.08 это был обрыв сети на середине
  загрузки, демо проверено глазами и работает.
