# Сообщение клиенту №21 — три пункта из голосовых 09.09 сделаны

📌 Отвечает на три голосовых 09.09 (08:31, 08:33, 08:42) и на его снимок формы New,
где кнопки показывали «Взрослая · 1 280 ₽ / Детская · 1 090 ₽» рядом с «Итого 780 ₽».
Расшифровка голосовых — `docs/voice-client-2026-09-09.txt`.

## 🔴 УСЛОВИЕ ОТПРАВКИ: только ПОСЛЕ выкладки на боевой

Письмо утверждает «сделал», поэтому до выкладки его слать нельзя. Порядок:

1. Влить `PR #26` (кнопку жмёт человек). Мерж обновит только демо.
2. Выложить на боевой руками по `docs/ДЕПЛОЙ-боевой.md`. Файлы правки:
   `src/js/ui/app.browser.js`, `src/js/core/AgeOptions.js` (новый),
   `src/js/core/CatalogPrices.js`, `src/css/stand.css`. ⚠️ Новый модуль `AgeOptions.js`
   не забыть: без него панель не соберётся вовсе.
3. Перемерить на живом боевом со сброшенным кэшем: детская слева и активна, цен на
   кнопках нет, на Чемпионе кнопка «Взрослая» отсутствует, на белом Фаворите взрослая
   даёт 1 680 ₽.

## ⚠️ Что замерено, а что нет

- ✅ Замерено на стенде (headless Chromium, 1045 и 375 px): детская слева и активна,
  подписи без цен, 16 px, высота кнопки 44, блок выше конструктора, горизонтальной
  прокрутки нет. С подсунутым каталогом Чемпиона остаётся одна кнопка «Детская»,
  выбор «взрослая» молча переносится на детскую. На белом Фаворите взрослая = 1 680 ₽
  вместо 1 280 ₽. `npm test` 157/157, `python -m pytest` 42 passed.
- 🔴 **НЕ замерено: живой боевой.** Каталог с боевого из терминала сейчас не снять,
  `admin-ajax.php` уводит на антибот-капчу `1R AntiBot`. Значит состав каталога в письме
  не утверждается ни одним числом, и это сделано намеренно.
- ⚠️ **Про состав Фаворита в письме ни слова.** Он говорил, что заведёт недостающие
  позиции сам; замер утра 09.09 к вечеру может протухнуть, а требовать сделанное нельзя.
  Формулировка «заведёте позицию, кнопка вернётся сама» верна при любом составе.
- ⚫ Про перенос кнопок он сам сказал «наверное» и «можно даже» — значит это вопрос,
  а не сделанная работа. Спрашиваем прямо, не решаем за него.

---

Дмитрий, добрый день! Сделал все три пункта из ваших голосовых.

**1. Размер выбирается первым.** Кнопки «Детская / Взрослая» и «Таблица размеров»
переехали из «Комплектации» в самый верх, над конструктором. Таблица больше не
справочная: размер выбирается прямо в ней, и в заказ приходит уже выбранным. Человек
видит наличие сразу, до того как начнёт собирать макет. Надпись над блоком убрал.

**2. Цены с кнопок убрал.** Осталось просто «Детская» и «Взрослая», крупнее, детская
слева. Цена на экране теперь одна, внизу в «Итого». Случай с вашего снимка, где на New
кнопки показывали 1 090 и 1 280, а «Итого» 780, больше не повторится.

**3. Взрослая показывается только там, где она есть.** На Чемпионе её теперь нет вовсе,
конструктор открывается на детской. Правило читает ваш каталог на лету: заведёте
взрослую позицию, кнопка вернётся сама, меня звать для этого не нужно.

И про цену, это ваше «приравниваем к фактическим». Раньше, когда у расцветки не было
своей карточки, конструктор подставлял общую запасную цену из настроек: 1 280 за
взрослую, 1 090 за детскую. Теперь он берёт цену по линейке из вашего же каталога, и на
взрослом Фаворите это 1 680 вместо 1 280. Вторую таблицу цен заводить не стал: она бы
разъехалась с магазином, а так цифры всегда ваши.

Один вопрос по «Комплектации». Вы сказали, что гетры и количество можно перенести в
«Детализацию», чтобы не плодить блоки. Сделать так или оставить отдельным блоком?
