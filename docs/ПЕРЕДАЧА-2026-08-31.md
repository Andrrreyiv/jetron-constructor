# ПЕРЕДАЧА 31.08.2026 — jetron-constructor

Читать ПЕРВЫМ. Отменяет `ПЕРЕДАЧА-2026-08-30.md` (там сказано «осталась одна разработка —
пересчёт зон» — она СДЕЛАНА) и `-08-29-вечер.md`.

## Коротко

🟢 **Разработки в проекте не осталось.** Всё закоммичено и запушено.
🔴 **Двигаться дальше нельзя без двух ответов клиента.** Не начинать новый код «чтобы не стоять»:
оба ответа меняют то, что пришлось бы переделывать.

## Состояние кода (сверено замерами 31.08)

- `main` = **`512e4ed`** (слияние PR #2). Ветка `fix/jetron-size-grid-acf` = **`65fddbc`**,
  впереди `main` на 2 коммита — **только документация**, кода там нет.
- Рабочее дерево чистое. `npm test` **83/0**, `python -m pytest` **34 passed**.
- **Демо пересобрано и проверено:** https://andrrreyiv.github.io/jetron-constructor/
  токен `v=20260831a`, все 45 путей из живого конфига отдают 200 (битых 0), линеек 8 /
  расцветок 45, Rich и Venom отсутствуют, `zoneSets` 8 и `zoneSet` у всех 45 форм,
  `zones.json` отдаётся с обоими ключами, кадры 992..1110 по линейкам.
- ⚠️ **На боевой jetronsport.ru НЕ выложено и выкладывать пока нельзя** (см. блокировку).

## Что было сделано 30-31.08

1. **Нормализация мокапов по линейкам.** Кадр больше не константа 900×1200: `line_canvas` =
   самая высокая форма линейки + 216 сверху + 40 снизу. `run_normalize.py` переписан в два
   прохода. Все 45 перегенерированы, рамка краски совпала с блобами git у 45 из 45 — форма
   не сдвинулась ни на пиксель, ушла только белая добивка снизу.
2. **Пересчёт зон** (это и была «оставшаяся разработка»): `y_new = y_old × 1200 / H`, `x` и `w`
   не трогаются. Реализовано в `scripts/rewrite_catalog.py` — `наборы_зон`, `расставить_наборы`,
   `пересчитать_переопределения`. Переопределения клиента живут в **корневом `zones.json`**,
   а не в `mock-config.json`. Проверено в живом браузере: расстановка клиента цела.
3. **Сжатие файлов контекста** (сегодня): проектный `CLAUDE.md` 41 → 25 КБ, строка проекта
   в `C:/Projects/CLAUDE.md` 6,7 → 1,7 КБ. Оба раза проверщик подтвердил «ПОТЕРЬ НЕТ».
   Побайтовые копии — `docs/HISTORY-jetron-constructor-2026-08-31.md`.
   Вынесены `docs/ДЕПЛОЙ-боевой.md` и `docs/ГРАБЛИ-инструментов.md`.

## Что блокирует (ответы клиента)

Текст готов: **`docs/сообщение-клиенту-12.md`**, слать ВТОРЫМ после `-11.md`, оба без картинок.

1. **Отступ у номера сверху.** Два варианта, выбирает клиент: ВНУТРИ поля (цифра опускается
   и уменьшается) или СДВИГОМ (размер прежний, низ выходит за поле и может налезть на лого).
   28.08 он просил поле не уменьшать, поэтому наугад не берём.
   ⚠️ `tests/ZoneManager.test.js:141-143` держит `NUMBER_TOP_INSET_RATIO < 0.05`, строка 30
   хардкодит `{900, 1200}` — пересматривать осознанно, когда придёт ответ.
2. **Расцветка «синий3» в Champion.** Заведена клиентом через админку на `konstruktor2.jpg`,
   в состав 22.08 не входит, исходника нет. Её зоны НЕ пересчитаны намеренно: высота кадра
   неизвестна, тот же множитель выбросил бы три зоны на шортах за нижний край (0.9265 → 1.147).

## Руки Андрея (кода не требуют)

- Отправить Дмитрию `docs/сообщение-клиенту-11.md`, затем `-12.md`. Оба **без картинок**.
- Задать вопрос про меню: на боевом ДВА меню (ID 3 десктоп, ID 55 бургер), правка одного
  не переносится на второе — убирать конструкторы из обоих или только из бургера?
- Ответить на созвон в Телемосте: клиент просил **дважды**.

## Мины, на которые легко наступить

- 🔴 **`admin.json` на боевом ЗАМЕЩАЕТ каталог целиком.** Он держит СТАРЫЕ 43 расцветки с Rich
  и Venom. Залить новый `mock-config.json` и не заметить, что ничего не изменилось, — реальный
  сценарий. Пересобирать только после ответа про «синий3».
- 🔴 **`zones.json` и мокапы выкладывать ТОЛЬКО вместе:** доли пересчитаны под новые высоты,
  поверх старых мокапов вся расстановка уедет вниз на 21 %.
- 🔴 **Боевой деплой — строго по `docs/ДЕПЛОЙ-боевой.md`** (сайт за 1R AntiBot, канал elFinder,
  битый PHP кладёт весь сайт).
- 🔴 **Сливать в `main` только через PR, кнопку жмёт владелец.**
- ⚠️ Этап «разрез 45 → 90 файлов» в объём не возвращать — клиент опроверг его 23.08.

Подробности по всему — `CLAUDE.md` проекта (25 КБ, читается целиком).
