# ПЕРЕДАЧА 31.08.2026 (вечер) — jetron-constructor

Читать ПЕРВЫМ. Отменяет свою прежнюю редакцию (там сказано «слать письма №11 и №12» —
они ОТМЕНЕНЫ письмом №13) и `ПЕРЕДАЧА-2026-08-30.md`, `-08-29-вечер.md`.
Разбор промаха зон целиком — `ПЕРЕДАЧА-2026-08-31-зоны.md`.

## Коротко

🟢 **Разработки в проекте не осталось.** Всё закоммичено, запушено, PR влит владельцем.
🔴 **Дальше нельзя без двух ответов клиента.** Новый код не начинать: оба ответа меняют то,
что пришлось бы переделывать.

## Состояние кода (сверено замерами 31.08 вечер)

- `main` = **`620fa31`** (слияние PR с зонами). Дерево `main` совпадает с деревом
  `fix/jetron-size-grid-acf` (`b273b69`) байт-в-байт, `origin/main^{tree}` =
  `fbec151104ec99df83241715734fd9bc45e485dd`.
- Ветка `fix/jetron-size-grid-acf` на **1 коммит** впереди `main` (`499aada`) — **только документация**.
- Рабочее дерево чистое. `npm test` **83/0**, `python -m pytest` **36 passed**.
- **Демо пересобрано и сверено:** https://andrrreyiv.github.io/jetron-constructor/
  живой `zones.json` отдаёт `champion-white.name.y` = **0.2847**, `champion-sinij3` не тронут
  (`name.y` 0.225, максимум `y+h` 0.9481 — за холст не вышла ни одна зона),
  корень / конфиг / мокап Champion белый отдают 200, токен `v=20260831a`.
- ⚠️ **На боевой jetronsport.ru НЕ выложено и выкладывать пока нельзя** (см. блокировку).

## Что сделано 31.08 вечером (это и был последний код)

**Промах зон закрыт.** Клиент прислал снимки: фамилия садилась на воротник.
Причина: расстановку с боевой картинки (900×1200) переносили на наш мокап (900×992)
множителем `1200/H` только по `y`, не трогая `x`. Комплект на двух картинках стоит
в разных местах и разного размера (задний вид 303×521 против нашего 419×717), плюс
боевая картинка — композит с растянутым по вертикали передним видом.

`пересчитать_переопределения` в `scripts/rewrite_catalog.py` переписана на **анизотропный**
перенос по рамке комплекта, отдельно по каждой оси. Замеренные рамки лежат в самом скрипте
(`РАМКИ_КЛИЕНТА` / `РАМКИ_НАШИ`), а не в тестах. Корневой `zones.json` пересобран:
`champion-white.name.y` 0.2203 → 0.2847 (219 → 282 px по холсту 992). Проверено в живом
браузере на Champion белом: «ИВАНОВ» под воротником, «10» по центру спины.

`champion-sinij3` **намеренно не тронут**: картинка `konstruktor2.jpg` заведена клиентом
через админку, исходника нет, рамку мерить не по чему; слепой перенос выбрасывал три зоны
на шортах за нижний край (0.9265 → 1.147). Это закреплено тестом
`test_форма_без_замеренных_рамок_не_трогается`.

## Что блокирует (ответы клиента)

Текст готов: **`docs/сообщение-клиенту-13.md`**. Слать **только его одно**, без картинок.
⚫ Письма **№11 и №12 отменены**, не отправлять ни при каких обстоятельствах.

⚠️ В файле №13 лежит редакция, написанная ДО починки зон: в ней нет абзаца про воротник,
а пункт про «синий3» сформулирован так, будто расстановка уже забрана без оговорки.
Актуальная редакция с абзацем «Отдельно про ваши скриншоты…» надиктована в чат 31.08 вечером
и в файл ещё не перенесена. Перед отправкой — либо перенести, либо дописать абзац руками.

1. **Отступ у номера сверху.** Два варианта, выбирает клиент: ВНУТРИ поля (цифра опускается
   и уменьшается) или СДВИГОМ (размер прежний, низ выходит за поле и может налезть на лого).
   28.08 клиент просил поле не уменьшать, поэтому наугад не берём. Спросить заодно, на сколько
   и у какого номера: сейчас на груди, спине и шортах отступ ОБЩИЙ.
   ⚠️ `tests/ZoneManager.test.js:141-143` держит `NUMBER_TOP_INSET_RATIO < 0.05`, строка 30
   хардкодит `{900, 1200}` — пересматривать осознанно, когда придёт ответ.
2. **Расцветка «синий3» в Champion.** В состав 22.08 не входит, исходника нет. Либо клиент
   говорит «убираем», либо присылает исходник и расставляет зоны заново.
   ⚠️ **31.08 клиент понял вопрос иначе** и прислал снимок демо с синим Чемпионом: «это же
   синий Чемпион, это и есть исходник, вы её загрузили». Он спутал ДВЕ записи: `champion-blue`
   (из его пакета, в составе 22.08, на демо, всё в порядке) и `champion-sinij3` — отдельную
   седьмую запись, которую он завёл через админку на файле `konstruktor2.jpg`. Спрашивать надо
   не «пришлите синий», а ««синий3» — это та же форма, что синий на демо (тогда удаляем дубль),
   или другая (тогда нужен файл, которого в пакете не было)».

## Обещано клиенту 31.08 (голосовое 11:04) — размножение зон по линейке

Клиент спросил: расставлю рамки в ОДНОЙ карточке линейки, вы раскидаете на остальные
карточки этой же линейки? **Ответили «да»** и попросили 8 карточек вместо 45, по одной
на линейку (Чемпион, Легенда, New, Space, Star, Winner, Волна, Фаворит).

Почему это честно: после нормализации 30.08 внутри одной линейки кадр у всех расцветок
одинаковый, форма стоит пиксель в пиксель на одном месте (сверено с блобами git у 45 из 45).
Значит перенос внутри линейки — **копирование один в один, без всякой математики**.
Между линейками так нельзя: высоты кадров 992..1110.

🔴 **Кода под это ещё НЕТ.** Переопределения в корневом `zones.json` лежат по форме
(`form_id` -> зоны), размножения по линейке нет ни в движке, ни в скриптах. Когда клиент
пришлёт первую готовую линейку, понадобится копирование блока `zones.json` с карточки-донора
на все формы той же `line` из `mock-config.json`. Клиенту обещано: правка отдельной расцветки
после размножения остаётся за ней (то есть копируем один раз по команде, а не затираем всё
на каждой пересборке).

## Новая задача по вёрстке 31.08 (голосовое 11:12) — палитра слева на ПК

Клиент просит на ПК-версии перенести блок цветов **влево от мокапа** (как в его эскизе),
кадр с формой сдвинуть вправо. На мобильной **оставить как сейчас**, цвета под формой.

**Ответили «да»** и объяснили: разметка одна, место меняется по брейкпоинту, дублирования нет.
Заданы два уточнения, ответов пока НЕТ:
1. ширина переключения — предложили ~900 px, ждём его число;
2. влево уезжает **только палитра** или весь блок настроек (по эскизу похоже, что только цвета).

🔴 **Кода нет, в текущий объём это не входило.** Делать вместе с остальными правками
перед боевой выкладкой, отдельно клиента не дёргать.

## Руки Андрея (кода не требуют)

- Отправить Дмитрию **только письмо №13**, без картинок.
- Ответить на созвон в Телемосте: клиент просил **дважды**.
- Вопрос про меню 🟢 **закрыт** ответом клиента 31.08: «везде убрать, оставить только в футере»,
  то есть чистим ОБА меню, ID 3 (верхнее) и ID 55 (бургер). Делается вместе с выкладкой.

## Мины, на которые легко наступить

- 🔴 **`admin.json` на боевом ЗАМЕЩАЕТ каталог целиком.** Он держит СТАРЫЕ 43 расцветки с Rich
  и Venom. Залить новый `mock-config.json` и не заметить, что ничего не изменилось, — реальный
  сценарий. Пересобирать только после ответа про «синий3».
- 🔴 **`zones.json` и мокапы выкладывать ТОЛЬКО вместе:** доли пересчитаны под новые высоты,
  поверх старых мокапов вся расстановка уедет.
- 🔴 **Переопределения клиента живут в КОРНЕВОМ `zones.json`**, а не в `mock-config.json`.
  Движок берёт зоны как `form.zones || config.zoneSets[form.zoneSet] || config.zoneTemplate`,
  затем накладывает `applyZoneOverrides` (`app.browser.js:65-71`).
- 🔴 **GitHub Pages собирает демо ТОЛЬКО из `main`.** Пуш ветки демо не обновляет.
- 🔴 **Боевой деплой — строго по `docs/ДЕПЛОЙ-боевой.md`** (сайт за 1R AntiBot, канал elFinder,
  битый PHP кладёт весь сайт).
- 🔴 **Сливать в `main` только через PR, кнопку жмёт владелец.** У `gh` в этом окружении
  нет учётки («Bad credentials»).
- ⚠️ Локальный запуск: `npx --yes serve .` (`.claude/launch.json`, конфигурация `jetron-preview`,
  порт 8778 с `autoPort`). Порт 8777 может держать `serve` другого аккаунта — не убивать,
  поднимать свой. Preview MCP ходит на `127.0.0.1`, не на `localhost`, и первый запрос
  приходит раньше, чем поднимется `npx`: одна ошибка загрузки в начале — норма.
- ⚠️ Этап «разрез 45 → 90 файлов» в объём не возвращать — клиент опроверг его 23.08.

Подробности по всему — `CLAUDE.md` проекта.
