# Сообщение клиенту №9 (Дмитрий, jetronsport.ru)

Повод: три вопроса 29.08 вечером, после сообщения №8.
1. «Отступ снизу в итоге ушёл?»
2. «Окно конструктора возможно сделать 900*1200?»
3. «Уберите из бургера конструкторы, пусть только в футере пока останутся».

Формат: ОДНО сообщение, 1 987 знаков. Длинные тире не использованы намеренно.

## Замеры, на которых построен ответ (не переделывать)

- `src/config/mock-config.json:27-31` → `canvas { width: 900, height: 1200, displayWidth: 450 }`.
  **Окно УЖЕ ровно 900×1200**, с самого начала. Его просьба фактически уже выполнена,
  и он этого не знает.
- Замер по всем 45 `.webp` (`ink_bbox`): высота формы **674..854**, верх **120..121**,
  пустота снизу **226..406**, нижняя кромка самой высокой формы **974**.
- Отсюда вариант: холст `900×975` → пустота снизу становится **0..180**.
  Высота блока на экране при ширине 450 падает с 600 до **487** px.
- ⚠️ Форма при этом НЕ увеличится: её размер задаёт ШИРИНА холста, а ширина не меняется.
  Уходит именно пустота. Обещать «станет крупнее» нельзя.
- ⚠️ Цена: зоны заданы долями от холста, смена высоты сдвигает все логотипы и номера.
  Расстановку он забрал на себя 28.08, значит работа ляжет на него. Поэтому менять высоту
  надо ДО заливки картинок, иначе он расставит зоны дважды.
- Меню на боевом ДВА: ID 3 (область `header`, десктоп) и ID 55 (`header_mobile`, бургер).
  Правка одного на второе не переносится. «Только в футере» и «убрать из бургера» это
  разные объёмы, поэтому вопрос в конце.

---

Дмитрий, отвечаю по порядку.

1. Отступ снизу не ушёл, и я скажу прямо. Фотографии «только перёд» я удалил, но они нигде не использовались, это были запасные файлы, поэтому на картинку их удаление не повлияло никак.

2. Окно уже 900 на 1200. Оно такое с самого начала, я перепроверил настройки. То есть эта просьба уже выполнена, и именно в ней причина отступа: окно вытянуто вверх, а пара «перёд плюс зад» наоборот шире, чем выше. Форма упирается в боковые края раньше, чем дотягивается до низа, и снизу остаётся пустота от 226 до 406 пикселей, смотря какая расцветка.

Что реально можно сделать: ужать окно по высоте, с 1200 примерно до 975. Тогда у самых высоких расцветок отступ исчезнет совсем, у остальных сократится с 226-406 до 0-180 пикселей.

Две оговорки, чтобы не было сюрприза. Первая: сама форма крупнее не станет. Её размер задаёт ширина окна, а ширину я не трогаю, иначе форма полезет за края. Уйдёт именно пустота, блок станет компактнее по высоте. Вторая: зоны нанесения заданы долями от окна, поэтому при смене высоты все логотипы и номера съедут и их надо расставить заново. Расстановку вы забрали на себя, значит эта часть ляжет на вас.

Скажите, делаем? Если да, я поменяю высоту до заливки картинок, чтобы вам не пришлось расставлять зоны дважды.

3. Конструкторы из бургера уберу. Тут нужно уточнение: меню на сайте два разных, бургерное на телефоне и верхнее на компьютере, и правка одного на второе не переносится. Убрать только из бургера, или из верхнего тоже, чтобы остались действительно только в футере?
